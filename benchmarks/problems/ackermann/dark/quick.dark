// Ackermann Benchmark - Quick check version

let ackermann(m: Int64, n: Int64) : Int64 =
    if m == 0 then n + 1
    else if n == 0 then ackermann(m - 1, 1)
    else ackermann(m - 1, ackermann(m, n - 1))

// Reduced: A(3, 6) instead of A(3, 12)
ackermann(3, 6)
