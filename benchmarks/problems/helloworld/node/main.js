// Hello World Benchmark
// Tests startup overhead and minimal program execution
// Outputs a simple checksum to verify correct execution

// Output a deterministic value
// This tests the minimal path through the runtime
console.log(42);
