// Exercises bitwise absorption in a hot tail-recursive loop.

def repeat(n: Int64, result: Int64) : Int64 =
    if n <= 0 then result
    else
        let absorbedAnd = n & (n ||| result) in
        let absorbedOr = n ||| (n & result) in
        repeat(n - 1, absorbedAnd + absorbedOr)

repeat(1000000, 0)
