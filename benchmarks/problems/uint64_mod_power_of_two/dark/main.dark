// Exercises UInt64 power-of-two modulo in a hot tail-recursive loop.

def sumRemainders(value: UInt64, remaining: UInt64, acc: UInt64) : UInt64 =
    if remaining == 0UL then acc
    else
        sumRemainders(
            value + 6364136223846793005UL,
            remaining - 1UL,
            acc + (value % 64UL))

sumRemainders(1UL, 1000000UL, 0UL)
