let sumTo(n: Int64, acc: Int64) : Int64 =
    if n <= 0L then acc
    else sumTo(n - 1L, acc + n)

let repeat(n: Int64, acc: Int64) : Int64 =
    if n <= 0L then acc
    else repeat(n - 1L, sumTo(10000L, 0L))

Builtin.printLine(Stdlib.Int64.toString(repeat(100L, 0L)))
