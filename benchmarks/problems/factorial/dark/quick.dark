let factorial(n: Int64) : Int64 =
    if n <= 1L then 1L
    else n * factorial(n - 1L)

let repeat(n: Int64, acc: Int64) : Int64 =
    if n <= 0L then acc
    else repeat(n - 1L, factorial(20L))

// Reduced: 100 iterations instead of 10000
repeat(100L, 0L)
