--- a/language/apply/eapply.dark
+++ b/language/apply/eapply.dark
@@ -88,9 +88,8 @@
   (functionWhichDoesntExist 6L) = error="There is no variable named: functionWhichDoesntExist"
 
 
-  // errors around type args. Calling with 0 type args is allowed —
-  // inference fills `'a` in from the actual arg's ValueType.
-  fnWithTypeArgAndOneParam 1L = 1L
+  // errors around type args
+  fnWithTypeArgAndOneParam 1L = error="fnWithTypeArgAndOneParam expects 1 type argument, but got 0 type arguments"
   fnWithTypeArgAndOneParam<Int64, String> 1L = error="fnWithTypeArgAndOneParam expects 1 type argument, but got 2 type arguments"
   // errors around args
   stringFn "str1" "str2" = error="stringFn expects 1 argument, but got 2 arguments"
@@ -142,4 +141,3 @@
   (let partial = outer<Bool, String> true
   partial "ignored") = true
 
-

--- a/language/apply/einfix.dark
+++ b/language/apply/einfix.dark
@@ -3,54 +3,7 @@
 (5L + (3L)) = 8L
 Stdlib.Int64.add_v0 5L 3L = 8L
 
-// Numeric operators are polymorphic over numeric types at runtime
-1.0 + 2.0 = 3.0
-3.5 * 2.0 = 7.0
-10.0 - 4.0 = 6.0
-1y + 2y = 3y
-100UL - 1UL = 99UL
-10L / 3L = 3L
-10.0 / 4.0 = 2.5
-7L % 3L = 1L
--8L % 3L = 1L
-7.5 % 2.0 = 1.5
--1.5 % 2.0 = 0.5
-2.0 ^ 3.0 = 8.0
-2L ^ 3L = 8L
-2.5 < 3.5 = true
-5L >= 5L = true
-2y > 1y = true
-
-// Mixed numeric types are a type error (no implicit coercion)
-5L + true = error="Cannot perform numeric operation on Int64 and Bool"
-1L + 2.0 = error="Cannot perform numeric operation on Int64 and Float"
-
-// Integer +,-,* overflow wraps around (two's complement)
-127y + 1y = -128y
-0uy - 1uy = 255uy
-9223372036854775807L + 1L = -9223372036854775808L
-2L * 9223372036854775807L = -2L
-170141183460469231731687303715884105727Q + 1Q = -170141183460469231731687303715884105728Q
-340282366920938463463374607431768211455Z + 1Z = 0Z
-// Signed integer division overflow (MinValue / -1) wraps back to MinValue
--9223372036854775808L / -1L = -9223372036854775808L
--128y / -1y = -128y
-// Power overflow wraps around (two's complement)
-2L ^ 64L = 0L
-(-9223372036854775808L) ^ 64L = 0L
-// Integer division by zero raises a clean RTE
-10L / 0L = error="Cannot divide by 0"
-// Float overflow follows IEEE (infinity), not an error
-2.0 ^ 100000.0 = Builtin.testInfinity_v0
-
-// `^` (power) works on every integer type and Float; Int128/UInt128 are unsupported
-2y ^ 3y = 8y
-3uy ^ 3uy = 27uy
-2l ^ 10l = 1024l
-2Q ^ 3Q = error="Cannot perform numeric operation on Int128 and Int128"
-// unary `-` works on every signed integer type and Float
--(Stdlib.Int8.add_v0 2y 3y) = -5y
--(Stdlib.Float.add_v0 1.0 1.5) = -2.5
+5L + true = error="Builtin.int64Add's 2nd parameter `b` expects Int64, but got Bool (true)"
 5L + (Builtin.testRuntimeError "error") = error="Uncaught exception: error"
 (Builtin.testRuntimeError "error") + 5L = error="Uncaught exception: error"
 (Builtin.testRuntimeError "one") + (Builtin.testRuntimeError "two") = error="Uncaught exception: one"

--- a/language/basic/eand.dark
+++ b/language/basic/eand.dark
@@ -2,15 +2,15 @@
 (true && false) = false
 (false && true) = false
 (false && false) = false
-(true && Builtin.testRuntimeError "msg") = error="Uncaught exception: msg"
+(true && Builtin.testRuntimeError "msg") = exit=1 stderr="msg"
 (true && 5L) = error="&& only supports Booleans"
-(false && 5L) = false
+(false && 5L) = error="&& only supports Booleans"
 //TODO bring back short-circuiting
 //(false && Builtin.testRuntimeError "msg") = false
 
-(Builtin.testRuntimeError "msg1" && Builtin.testRuntimeError "msg2") = error="Uncaught exception: msg1"
+(Builtin.testRuntimeError "msg1" && Builtin.testRuntimeError "msg2") = exit=1 stderr="msg1"
 (5 && true) = error="&& only supports Booleans"
 (true |> (&&) true) = true
 (true |> (&&) false) = false
 (false |> (&&) true) = false
-(false |> (&&) false) = false
\ No newline at end of file
+(false |> (&&) false) = false

--- a/language/basic/elet.dark
+++ b/language/basic/elet.dark
@@ -10,12 +10,10 @@
   (let (a, b) = (1L, 2L) in 2L) = 2L
   (let (a, b) = (1L, 2L) in b) = 2L
   (let (a, b) = (1L, 2L) in (b, a)) = (2L, 1L)
-  (let (d, d) = (2L, 1L) in d) = 1L
   (let (_, _) = (1L, 2L) in 2L) = 2L
 
   (let (a, b, c) = (1L, 2L, 3L) in b) = 2L
   (let (a, b, c) = (1L, 2L, 3L) in (b, a)) = (2L, 1L)
-  (let (d, d, d) = (2L, 1L, 3L) in d) = 3L
   (let (_, _, _) = (1L, 2L, 3L) in 2L) = 2L
 
   (let (_, _, _) = (1L, 2L, Builtin.testRuntimeError "test") in 2L) = error="Uncaught exception: test"
@@ -70,37 +70,6 @@
     (match Stdlib.Result.Result.Ok(Stdlib.Result.Result.Ok 6L) with
      | Ok(Ok x) -> let x = 9L in x + 2L))) = [ 11L; 11L; 11L; 11L ]
 
-
-module Wildcard =
-  // Simple wildcard - should match any value without binding it
-  (let _ = 5L in 10L) = 10L
-
-  // Wildcard with side effects - the value should still be evaluated
-  (let _ = Builtin.testRuntimeError "test" in 10L) = error="Uncaught exception: test"
-
-  // Wildcard in tuple destructuring
-  (let (x, _) = (1L, 2L) in x) = 1L
-  (let (_, y) = (1L, 2L) in y) = 2L
-  (let (_, _) = (1L, 2L) in 5L) = 5L
-
-  // Multiple wildcards in tuple
-  (let (_, _, _) = (1L, 2L, 3L) in 10L) = 10L
-  (let (x, _, _) = (1L, 2L, 3L) in x) = 1L
-  (let (_, y, _) = (1L, 2L, 3L) in y) = 2L
-  (let (_, _, z) = (1L, 2L, 3L) in z) = 3L
-
-  // Nested wildcards in tuples
-  (let ((_, x), _) = ((1L, 2L), 3L) in x) = 2L
-  (let (_, (_, y)) = (1L, (2L, 3L)) in y) = 3L
-
-  // Wildcard should not bind variable
-  // This should fail because _ is not a variable
-  // (let _ = 5L in _) should give a variable not found error
-
-  // Wildcard with complex values
-  (let _ = [ 1L; 2L; 3L ] in "done") = "done"
-
-
 module Errors =
-  (let (a, _) = 1 in a) = error="Could not deconstruct value 1 into pattern ([variable], _)"
-  (let (a, _) = (1, 2, 3) in a) = error="Could not deconstruct value (1, 2, 3) into pattern ([variable], _)"
+  (let (a, _) = 1L in a) = error="Undefined variable: a"
+  (let (a, _) = (1L, 2L, 3L) in a) = error="Undefined variable: a"

--- a/language/basic/eor.dark
+++ b/language/basic/eor.dark
@@ -3,7 +3,7 @@
 (false || true) = true
 (false || false) = false
 
-(true || 5L) = true
+(true || 5L) = error="|| only supports Booleans"
 (false || 5L) = error="|| only supports Booleans"
 (5L || true) = error="|| only supports Booleans"
 (true |> (||) true) = true
@@ -13,5 +13,5 @@
 
 // TODO bring back short-circuiting
 //(true || Builtin.testRuntimeError "msg") = true
-(false || Builtin.testRuntimeError "msg") = error="Uncaught exception: msg"
-(Builtin.testRuntimeError "msg1" || Builtin.testRuntimeError "msg2") = error="Uncaught exception: msg1"
\ No newline at end of file
+(false || Builtin.testRuntimeError "msg") = error="|| only supports Booleans"
+(Builtin.testRuntimeError "msg1" || Builtin.testRuntimeError "msg2") = error="|| only supports Booleans"

--- a/language/basic/estring.dark
+++ b/language/basic/estring.dark
@@ -2,11 +2,11 @@
 
 (let one = "1" in $"test {one}") = "test 1"
 
-(let one = 1.0 in $"test {one}") = error="Expected String in string interpolation, got a Float (1.0) instead. Try wrapping it with `Stdlib.Float.toString`."
-(let one = 1L in $"test {one}") = error="Expected String in string interpolation, got an Int64 (1) instead. Try wrapping it with `Stdlib.Int64.toString`."
+(let one = 1.0 in $"test {one}") = error="Expected String in string interpolation, got a Float instead"
+(let one = 1L in $"test {one}") = error="Expected String in string interpolation, got an Int64 instead"
 (let name = "John"
  let age = "30"
  $"Name: {name}, Age: {age} years old.") = "Name: John, Age: 30 years old."
 
 (let two = 2L in "test 1" == $"test {one}") = error="There is no variable named: one"
-(let one = 1L in $"test {Stdlib.Int64.toString one}") = "test 1"
\ No newline at end of file
+(let one = 1L in $"test {Stdlib.Int64.toString one}") = "test 1"

--- a/language/basic/evariable.dark
+++ b/language/basic/evariable.dark
@@ -1,3 +1,3 @@
 (let x = 5L in x) = 5L
 
-myvar = error="There is no variable named: myvar"
\ No newline at end of file
+myvar = error="Undefined variable: myvar"

--- a/language/flow-control/eif.dark
+++ b/language/flow-control/eif.dark
@@ -1,20 +1,20 @@
-(if true then "correct" else 0L) = "correct"
+(if true then "correct" else 0L) = error="Type mismatch: if branches must have same type: expected String, got Int64"
 
-(if true then Builtin.testRuntimeError "a" else 0L) = error="Uncaught exception: a"
-(if false then 0L else Builtin.testRuntimeError "a") = error="Uncaught exception: a"
+(if true then Builtin.testRuntimeError "a" else 0L) = exit=1 stderr="Uncaught exception: a"
+(if false then 0L else Builtin.testRuntimeError "a") = exit=1 stderr="Uncaught exception: a"
 (if false then Builtin.testRuntimeError "a" else 0L) = 0L
 (if true then 0L else Builtin.testRuntimeError "a") = 0L
 (if false then "" else "correct") = "correct"
 (if () then "" else "") = error="Encountered a condition that must be a Bool, but got an Unit (())"
-(if Builtin.testRuntimeError "msg" then "" else "") = error="Uncaught exception: msg"
+(if Builtin.testRuntimeError "msg" then "" else "") = exit=1 stderr="Uncaught exception: msg"
 (if 5L then "" else "") = error="Encountered a condition that must be a Bool, but got an Int64 (5)"
 (if true then ()) = ()
-(if 1L > 3L then 4L) = ()
-(if 1L < 3L then 4L) = 4L
-(if 1L < 3L then "msg") = "msg"
+(if 1L > 3L then 4L) = error="Type mismatch: if branches must have same type: expected Int64, got Unit"
+(if 1L < 3L then 4L) = error="Type mismatch: if branches must have same type: expected Int64, got Unit"
+(if 1L < 3L then "msg") = error="Type mismatch: if branches must have same type: expected String, got Unit"
 
 (if () then "msg") = error="Encountered a condition that must be a Bool, but got an Unit (())"
 
-(if true then Builtin.testRuntimeError "a") = error="Uncaught exception: a"
+(if true then Builtin.testRuntimeError "a") = exit=1 stderr="Uncaught exception: a"
 
-(if Builtin.testRuntimeError "msg" then "") = error="Uncaught exception: msg"
\ No newline at end of file
+(if Builtin.testRuntimeError "msg" then "") = error="Type mismatch: if branches must have same type: expected String, got Unit"

--- a/language/flow-control/ematch.dark
+++ b/language/flow-control/ematch.dark
@@ -348,7 +348,7 @@
 
   (match [ 1L; 2L; 3L ] with
    | var ->
-     let length = (Stdlib.List.length var) |> Stdlib.Int.toString
+    let length = (Stdlib.List.length var) |> Stdlib.Int64.toString
 
      $"pass with length: {length}"
    | [ 1L; 2L; 3L ] -> "fail"
@@ -376,7 +376,7 @@
   // duplicate variable name
   (match [ 1L; 2L; 5L ] with
    | [ a; a; a ] -> a
-   | name -> name ++ "var") = 5L
+   | name -> name ++ "var") = error="Duplicate binding 'a' in the same pattern"
 
 
 
@@ -526,7 +526,7 @@
 
    match tpl with
    | (a, b, (a, b), a) -> (a, b)
-   | name -> name ++ "var") = (5L, 4L)
+   | name -> name ++ "var") = error="Duplicate binding 'a' in the same pattern"
 
 
 
@@ -649,7 +649,7 @@
 
 module Errors =
   (match "nothing matches" with
-   | "not this" -> "fail") = error="No matching case found for value \"nothing matches\" in match expression"
+   | "not this" -> "fail") = exit=1 stderr="Non-exhaustive match: No matching case found for value \"nothing matches\" in match expression"
   (match Builtin.testRuntimeError "cond is error" with
    | 5L -> "fail"
    | 6L -> "pass"
@@ -689,10 +689,10 @@
 
   // enum with more pattern params than actual args
   (match TestType.NoArgs with
-   | NoArgs _ -> "wrong number") = error="No matching case found for value TypeErrors.TestType.NoArgs in match expression"
+   | NoArgs _ -> "wrong number") = error="Non-exhaustive match: No matching case found for value TestType.NoArgs in match expression"
   // enum with fewer pattern params than actual args
   (match TestType.OneArg 1L with
-   | OneArg -> "wrong number") = error="No matching case found for value TypeErrors.TestType.OneArg(1) in match expression"
+   | OneArg -> "wrong number") = error="Non-exhaustive match: No matching case found for value TestType.OneArg(1) in match expression"
   // TODO implement MPIgnored
   // // enum with a single wildcard
   // (match TestType.TwoArgs(1L, 2L) with
@@ -717,7 +717,7 @@
   (match 5L with
    | x when x > 0L -> "positive"
    | x when x < 0L -> "negative"
-   | _ -> false) = "positive"
+   | _ -> false) = error="Type mismatch in match body: expected String, got Bool"
 
   (match (5L, "hello") with
    | (x, y) when x > 0L && y == "hello" -> "pass"
@@ -733,7 +733,7 @@
    | _ -> "fail") = "fail"
 
   (match (5L, "hello") with
-   | (x, y) when x > 0L && Stdlib.String.length y == 5I -> "pass"
+   | (x, y) when x > 0L && Stdlib.String.length y == 5L -> "pass"
    | _ -> "fail") = "pass"
 
   (match -5L with
@@ -741,7 +741,7 @@
    | _ -> false) = error="There is no variable named: y"
   (match 5L with
    | 5L -> false
-   | x when y > 0L -> true) = false
+   | x when y > 0L -> true) = error="There is no variable named: y"
 
   (match 5L with
    | x when (x + 1L) == 6L -> true
@@ -751,11 +751,12 @@
   (match 5L with
    | (x, y) when x > 0L -> true
    | _ -> false) =
-     false
+     error="There is no variable named: x"
 
   (match 5L with
    | 2L when x > 2L -> false
-   | 3L -> true) = error="No matching case found for value 5 in match expression"
+   | 3L -> true) =
+    error="There is no variable named: x"
 
   (match Stdlib.Result.Result.Error 5L with
    | Ok x when x > 2L -> false
@@ -811,14 +812,14 @@
 
   (match ([1L;2L], 3L) with
    | ([x], y) | (x :: xs, y) when x > 0L -> "pass"
-   | _ -> "fail") = "pass"
+   | _ -> "fail") = error="Pattern matches require all branches to provide the same variables - expected [x, y], got [x, xs, y]"
 
   (match (1L,2L) with
    | (x,2L) | (2L,y) when x == y -> "fail"
-   | _ -> "fail") = error="There is no variable named: x"
+   | _ -> "fail") = error="Pattern matches require all branches to provide the same variables - expected [x], got [y]"
   (match (1L,2L) with
     | (x,y) | (x,_) when y > 0L -> "fail"
-    | _ -> "pass") = error="There is no variable named: y"
+    | _ -> "pass") = error="Pattern matches require all branches to provide the same variables - expected [x, y], got [x]"
   // CLEANUP these should fail when we have at-rest checking
   (match [1L;2L] with
    | [x;y] | x::y -> "fail"
@@ -826,7 +827,7 @@
 
   (match (1L, "test") with
    | (x, "test") | ("test", x) -> x
-   | _ -> "fail") = 1L
+   | _ -> "fail") = error="Type mismatch in string literal: expected Int64, got String"
 
   (match (Stdlib.Result.Result.Ok 1L, Stdlib.Result.Result.Error "error") with
    | (Ok _ , Ok _) -> "pass"
