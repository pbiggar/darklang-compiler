---
format: 1
event-id: 019ffc2248b170a88543701cd47e37a9
entity-id: 019ff58b47c67a868f397bf7db4dd4a1
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-13T17:18:53.1054911+00:00
author: worker:65eb66d2e522:3456704:019ffc1c5d2e72c2b5e7572e39b03b33
previous: 019ff7d76bc871e8a75e4dcddd9dab90
base-commit: 551950386fd108823fe4b51407b4bf7da3e5d448
constraints-hash: e646027115bd63ae6634df307340bf0add1f07691802d085d332d9e6fd77a483
revision: 019ff7d76bc871e8a75e4dcddd9dab90
---
# Breakdown

Read-only comparison of compiler 51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3 with darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d, using DCB1 8a402797ccccda0ca47b516b356ae1de4d670038 only as a lead. Copy interpreter-authored .dark implementations wherever compatible; add native compiler/runtime implementations only for builtins, F#/.NET-backed behavior, host integration, or unsupported runtime values. Bool, Fun, Tuple2, and Tuple3 are exact; resolved historical findings and non-observable performance differences are excluded.

## Proposed issue 019ffc2248b17421863bab1ad32957b1

Title: Complete prelude parity

Outcome:

Expose the interpreter's polymorphic equality and explicit printing prelude.

Constraints:

- Parity gaps: Stdlib.equals, Stdlib.notEquals, Stdlib.print, Stdlib.printLine, and Stdlib.printLines.
- Copy the interpreter's .dark declarations and compositions; implement only equality dispatch and output primitives in the compiler runtime.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/CompilerLibrary.fs:1017-1053. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/noModule.dark, packages/darklang/stdlib/print.dark, and backend/testfiles/execution/stdlib/nomodule.dark.

## Proposed issue 019ffc2248b17afd87b14e25d0f1f7ee

Title: Add AltJson and Json parity

Outcome:

Provide structural AltJson values and type-directed Json serialization and parsing.

Constraints:

- AltJson gaps: Json; ParseError.ParseError and toString; format; parse; Helpers.getString, getBool, getValue, getInt, getIntOption, getNumber, getArray, and getObject; Builder.Fields, empty, addString, addNumber, addInt, addBool, addJson, addArray, all six addOptional variants, and toJson.
- Json gaps: serialize<'a> and parse<'a>; ParseError.JsonPath.Part.Part and toString; ParseError.JsonPath.JsonPath and toString; ParseError.ParseError and toString, including canonical record, enum, and path-bearing failures.
- Copy all portable parser, formatter, builder, path, and error-rendering code from alt-json.dark and json.dark. Implement the JSON runtime value plus type-directed reflection/serialization primitives natively where the interpreter uses backend builtins.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/CompilerLibrary.fs:1017-1053. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/alt-json.dark, packages/darklang/stdlib/json.dark, and backend/testfiles/execution/stdlib/alt-json.dark and json.dark.

## Proposed issue 019ffc2248b17e8cb76364a0345f7f29

Title: Align Blob, Base64, Crypto, and X509

Outcome:

Use the interpreter's immutable Blob contract throughout binary encoding, hashing, and certificate operations.

Constraints:

- Blob gaps: empty, length, concat, slice, toHex, fromHex, toBase64, fromBase64, toString, fromString, toList, and fromList.
- Align Base64.decode, encode, and urlEncode plus Crypto.sha256, sha384, md5, sha256hmac, and sha1hmac to Blob signatures; add X509.pemCertificatePublicKey.
- Copy portable .dark wrappers and validation. Implement immutable Blob storage, codecs, cryptographic primitives, and certificate extraction natively because the interpreter backs them with runtime libraries.
- Classify Bytes.create, fromList, get, hexEncode, length, set, and toList; Base64.urlDecode; Crypto.sha1 and bytesToHex; and every debug-prefixed Crypto declaration as compiler extensions rather than Blob parity.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Bytes.dark, Base64.dark, Crypto.dark, and src/DarkCompiler/Stdlib.fs:144-148. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/blob.dark, base64.dark, crypto.dark, x509.dark, and matching backend/testfiles/execution/stdlib tests.

## Proposed issue 019ffc2248b178de93cf562d5fc80da8

Title: Align Char, String, and Regex

Outcome:

Provide canonical EGC-aware text behavior and the interpreter's regular-expression operations.

Constraints:

- Char gap: fromCodepoint. Align toUppercase, toLowercase, isLowercase, isUppercase, isDigit, isASCIILetter, isASCII, toAsciiCode, comparisons, and toString to extended-grapheme-cluster behavior.
- String gaps: articleFor, ellipsis, map, toList, replaceAll, toUppercase, splitFirst, splitN, fromList, fromChar, the interpreter's Base64, digest, and random helpers, htmlEscape, Bytes/Blob conversions, pluralize, indexOfEgc, lastIndexOf, lastIndexOfEgc, toOrdinal, splitOnNewline, and charAt.
- Align String.length, slice, first, last, dropFirst, dropLast, head, padding, split, trim, casing, reverse, and indexOf to the interpreter's EGC and NFC contracts.
- Regex gaps: isMatch, find, findAll, replace, replaceAll, and split.
- Copy portable .dark algorithms and Unicode data. Implement Unicode segmentation, normalization, casing, randomness, digest/codec primitives, and regex execution natively where the interpreter uses backend libraries.
- Classify Char.isLetter, isWhitespace, isAlphanumeric, toCode, and fromCode plus String.getByteAt, substring, take, drop, toCodepoints, codepointLength, fromCodepoints, toUpperCase, toGraphemes, graphemeLength, replace, and equals as compiler extensions.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Char.dark, String.dark, and unicode_data.dark; Regex is absent. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/char.dark, string.dark, regex.dark, and matching execution tests. A decomposed-EGC probe reports compiler byte length 3 versus interpreter EGC length 1.

## Proposed issue 019ffc2248b17bbeb4480039be3f7c52

Title: Align DateTime and Duration

Outcome:

Provide a distinct DateTime value and canonical duration parsing and arithmetic.

Constraints:

- Align DateTime.addSeconds, subtractSeconds, toSeconds, fromSeconds, subtract, year, month, day, weekday, hour, minute, second, toMilliseconds, fromMilliseconds, addMilliseconds, subtractMilliseconds, millisecond, and subtractMs to Int signatures and a distinct DateTime runtime value instead of an Int64 alias.
- Duration gap: parse, including its Int result, recognized units, and exact error contract.
- Copy Duration parsing and all portable DateTime wrappers from the interpreter. Implement clock/calendar conversion and the distinct DateTime representation natively.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/DateTime.dark:6-305; Duration is absent. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/dateTime.dark, duration.dark, and backend/testfiles/execution/stdlib/date.dark and duration.dark.

## Proposed issue 019ffc2248b17492a11fe7b8516907e3

Title: Complete Uuid parity

Outcome:

Provide canonical UUID parsing and a distinct UUID runtime value.

Constraints:

- Parity gaps: ParseError containing only BadFormat, canonical parse, and distinct Uuid behavior across generate and toString.
- Compiler parse_v0 and String-backed UUID results are compatibility behavior, not substitutes for the canonical API; UnsupportedUuidParseError is a compiler extension.
- Copy portable parsing validation and formatting wrappers; implement UUID storage and generation natively.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Uuid.dark:8-38. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/uuid.dark and backend/testfiles/execution/stdlib/uuid.dark.

## Proposed issue 019ffc2248b177e299067a5cb5d0eb8e

Title: Align Dict parity

Outcome:

Expose the interpreter's string-keyed Dict API and duplicate-key contracts.

Constraints:

- Missing functions: filterMap, fromListOverwritingDuplicates, and iter.
- Align empty, isEmpty, singleton, size, get, set, setOverridingDuplicates, remove, merge, keys, values, toList, fromList, member, map, and filter to Dict<'v>, string keys, empty as a value, Int size, duplicate-rejecting set, and Option-returning fromList.
- Copy portable .dark combinators and duplicate handling. Reuse or adapt the compiler HAMT behind a string-keyed public value only if it preserves the interpreter's observable order, equality, and duplicate behavior.
- Classify generic-key Dict<K,V>, entries, fold, and getOrDefault as compiler extensions.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Dict.dark, __HAMT.dark, and src/DarkCompiler/Stdlib.fs:150-166. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/dict.dark and backend/testfiles/execution/language/collections/edict.dark and stdlib/dict.dark.

## Proposed issue 019ffc2248b17dd6a8ce523189ecd888

Title: Complete the integer module family

Outcome:

Provide the interpreter's arbitrary-width, fixed-width, and 128-bit integer conversion matrix.

Constraints:

- Int gaps: ParseError with only BadFormat; mod, absoluteValue, sum, max, min, clamp, and random; canonical Result<Int,_> behavior for remainder and parse. Classify compare and equals as extensions.
- Int8 gaps: ParseError, subtract, multiply, divide, remainder, random, sqrt, toFloat, parse, and conversions from UInt8, Int16, UInt16, Int32, UInt32, Int64, UInt64, Int128, and UInt128. UInt8 has the analogous surface plus fromInt8, excluding remainder.
- Int16 gaps: ParseError, remainder, random, sqrt, toFloat, parse, and conversions from Int8, UInt8, UInt16, Int32, UInt32, Int64, UInt64, Int128, and UInt128. UInt16 has analogous conversions including Int16 plus random, sqrt, toFloat, and parse.
- Int32 gaps: ParseError, subtract, multiply, divide, remainder, random, sqrt, toFloat, parse, and conversions from smaller widths, UInt32, Int64, UInt64, Int128, and UInt128. UInt32 has the analogous unsigned surface without remainder.
- Int64 gaps: ParseError, remainder, random, sqrt, parse, fromFloat, and conversions from every other width. UInt64 gaps: subtract, multiply, divide, random, sqrt, toFloat, parse, fromFloat, and conversions from every other width.
- Int128 gaps: ParseError; mod and remainder; arithmetic; absoluteValue and negate; comparisons; sqrt; toFloat; sum, max, min, and clamp; toString and parse; conversions from 8-, 16-, 32-, and 64-bit types; and bitwise and shift functions. UInt128 has the analogous unsigned surface, unsigned conversions, and no signed-only operations.
- Copy the interpreter's conversion checks, parsing rules, error values, aggregations, and wrappers. Implement machine arithmetic, arbitrary/128-bit representation, checked conversions, shifts, bitwise operations, square roots, and random generation natively.
- Classify sub, mul, and div compatibility names, popcount, isEven, isOdd, and unsigned absoluteValue and negate as extensions wherever the interpreter has no declaration.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Int.dark through UInt64.dark and src/DarkCompiler/CompilerLibrary.fs:1019-1027; 128-bit modules are absent. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/int.dark through uint128.dark and backend/testfiles/execution/stdlib/ints. Canonical Int128.add is unresolved in the compiler.

## Proposed issue 019ffc2248b1755e8415c8a0041484d7

Title: Complete Float and Math parity

Outcome:

Provide the interpreter's floating-point formatting, rounding, parsing, and transcendental contracts.

Constraints:

- Float gaps: ParseError, ceiling, roundUp, floor, roundDown, round, roundTowardsZero, truncate, clamp, parse, power, and isNaN.
- Align Float.toString to shortest-roundtrip formatting, including negative zero and nonfinite values.
- Math gaps: acos, asin, atan, atan2, cos, cosh, sin, sinh, tan, and tanh. Retain pi, tau, degrees, turns, and radians as parity.
- Copy portable .dark wrappers and unit conversions. Implement parsing, shortest-roundtrip formatting, rounding, classification, and transcendental primitives through the native runtime.
- Classify Float.abs, toInt, and toBits plus Math.e, abs, sqrt, truncate, floor, ceiling, and round as compiler extensions.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Float.dark, Math.dark, and src/DarkCompiler/Stdlib.fs:19-33. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/float.dark, math.dark, and matching execution tests. Compiler Float.toString caps decimal generation, and canonical Float.ceiling is unresolved.

## Proposed issue 019ffc2248b17fb1a9e5f764048b13d7

Title: Complete List parity

Outcome:

Provide the interpreter's collection operations, Int indexing, and canonical sorting behavior.

Constraints:

- Missing type: ChunkBySizeError.
- Missing functions: empty, repeatUnsafe, uniqueBy, unique, sort, sortBy, groupByWithKey, iter, randomElement, chunkBySize, chunkBySizeHelper, map2shortestHelper, and SortByComparatorHelpers.validate, merge, splitList, and mergeSort.
- Align length, findFirstIndex, repeat, range, drop, take, indexedMap, getAt, sortByComparator, and chunkBySize to Int arguments/results, canonical argument order, comparator currying, validation, and error behavior.
- Copy interpreter .dark implementations for list algorithms and sorting helpers; add native support only for primitives the copied functions call, including randomness if required.
- Classify equals, flatMap, setAt, and forAll as compiler extensions.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/List.dark. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/list.dark and backend/testfiles/execution/language/collections/dlist.dark and stdlib/list.dark.

## Proposed issue 019ffc2248b1791db582b05e1e2cf574

Title: Complete Option, Result, and Retry

Outcome:

Provide the remaining result-control combinators and retry helpers.

Constraints:

- Missing combinators: Option.and, Result.and, and Result.or.
- Retry gaps: withBackoffLoop, withBackoff, withFixedDelayLoop, and withFixedDelay, preserving callback results, retry termination, backoff calculation, and delay behavior.
- Copy Option, Result, and Retry .dark implementations directly where supported; implement only the delay/runtime effect invoked by Retry.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/stdlib/Option.dark and Result.dark; Retry is absent. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/option.dark, result.dark, retry.dark, and backend/testfiles/execution/stdlib/option.dark and result.dark.

## Proposed issue 019ffc2248b174378477bbaf6fa4ad62

Title: Add Html and Http value modules

Outcome:

Provide canonical HTML nodes and HTTP request, response, and cookie values.

Constraints:

- Html types: Attribute, Attributes, HtmlTag, and Node. Add voidElements, escapeHtml, text, stringNode, htmlTagNode, nodeToString, comment, classes, style, dataAttr, document, br, and the interpreter's public constructors for document, text, grouping, table, form, media, semantic, and metadata tags.
- The constructor set includes html, body, head, title, a, div, span, h1-h6, p, ul, ol, li, table, tr, td, th, tbody, thead, tfoot, caption, colgroup, col, button, img, svg, path, meta, link, script, input, label, form, section, i, code, pre, nav, footer, article, aside, header, main, figure, figcaption, video, audio, picture, strong, em, b, u, s, small, mark, del, ins, dl, dt, dd, textarea, select, option, fieldset, legend, details, summary, and dialog.
- Http gaps: Request, parseQueryString, parseHeaders, Request.queryParams, Request.queryParam, Response, response, responseWithHeaders, success, responseWithHtml, responseWithText, responseWithJson, redirectTo, badRequest, notFound, unauthorized, forbidden, serverError, and Cookie.
- Copy the interpreter's Html and Http .dark types, constructors, encoders, parsers, and response helpers. Add native support only for primitive value representation or JSON serialization dependencies.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/CompilerLibrary.fs:1017-1053; both modules are absent. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/html.dark, http.dark, and backend/testfiles/execution/stdlib/html.dark and http.dark.

## Proposed issue 019ffc2248b17b89a3c9ccbbdf14dd19

Title: Add the HTTP runtime family

Outcome:

Provide buffered and streaming clients, SSE parsing, routing, and server execution.

Constraints:

- HttpClient gaps: BadHeader, BadUrlDetails, RequestError, Response, StreamResponse, toString, request, get, post, put, options, delete, head, stream, basicAuth, and bearerToken; ContentType.form, json, plainText, and html.
- Sse gaps: Event, ParseState, splitField, applyLine, tryEmitBlock, pullNextEvent, and parse.
- HttpServer gaps: Handler, RouteMatch, RouteSegment, parseRouteSegments, matchSegment, matchRoute, findHandler, getMethod, getPath, routeRequest, and getPathParam; Config.Config, defaultMaxBodyBytes, and defaults; serve.
- Copy the interpreter's request wrappers, authentication helpers, content types, SSE parser, route matcher, and configuration code. Reimplement network I/O, streaming handles, cancellation/cleanup, and server execution from backend/src/Builtins/Builtins.Http.Client and Server against compiler runtime facilities.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: no HTTP modules or runtime registration. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/httpclient.dark and httpserver.dark, backend/src/Builtins/Builtins.Http.Client and Server, and backend/testfiles/execution/stdlib/httpclient.dark and sse.dark.

## Proposed issue 019ffc2248b173a8a4af1073ad0f86f2

Title: Add Stream parity

Outcome:

Provide lazy resource-backed streams with deterministic close behavior.

Constraints:

- Gaps: Stream<'a>; fromList, unfold, next, toList, toBlob, close, map, filter, take, and concat.
- Match single-consumer state transitions, callback-error propagation, exhaustion, idempotent close behavior, and resource disposal.
- Copy portable .dark combinators. Implement the opaque stream state, resource handle, next/close primitives, and disposal guarantees natively from the interpreter backend contract.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: Stream is absent from the AST, Runtime.fs, and CompilerLibrary.fs:1017-1053. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/stream.dark, backend/src/Builtins/Builtins.Pure/Libs/Stream.fs, and backend/testfiles/execution/stdlib/stream.dark.

## Proposed issue 019ffc2248b17914942252f379c97055

Title: Add DB and Sqlite parity

Outcome:

Provide typed database values, query execution, and SQLite operations.

Constraints:

- DB gaps: DB<'a>; set, get, getMany, getExisting, getManyWithKeys, delete, deleteAll, getAll, getAllWithKeys, count, generateKey, keys, query, queryWithKey, queryOne, queryOneWithKey, and queryCount.
- Sqlite gaps: Value, asText, asInt, execP, exec, queryP, query, textField, intField, queryOne, queryOneP, column, scalarInt, and scalarIntP.
- Copy portable .dark wrappers, decoders, and error composition. Implement DB value representation, query IR/type integration, persistence primitives, SQLite connection/parameter/value handling, and host library calls natively.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: no DB value, declaration, query IR, or Sqlite module. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/db.dark and sqlite.dark, backend/src/Builtins/Builtins.Matter/Libs/DB.fs and Sqlite.fs, and backend/testfiles/execution/cloud/db and stdlib/sqlite.dark.

## Proposed issue 019ffc2248b17b488481d3a26320b55b

Title: Add Diff and ValueSearch utilities

Outcome:

Provide the interpreter's value-diffing and typed value-search utilities.

Constraints:

- Diff gaps: DiffLine, tableGet, buildTable, trace, and lines.
- ValueSearch gaps: FoundValue, parseNamespace, isValueInNamespace, buildPathFromLocation, and findByType.
- Copy both interpreter .dark modules as the implementation source; introduce native support only if copied code exposes a concrete missing reflection primitive.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/CompilerLibrary.fs:1017-1053; both modules are absent. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/diff.dark and valueSearch.dark.

## Proposed issue 019ffc2248b172c798dfccc1befa1a77

Title: Add CLI execution, host, process, and input

Outcome:

Provide process execution, host discovery, system information, and keyboard input.

Constraints:

- Cli gaps: ExecutionOutcome, execute, executeWithUnitOrStdErr, and executeWithStdOutOrStdErr.
- Host gaps: OS.OS and getOS; Architecture.Architecture and getArchitecture; Shell.Shell and getDefaultShell; Host.Host and getRuntimeHost. Sys gaps: currentUser, hostname, os, arch, cpuCount, currentPid, and isRoot.
- Process gaps: ProcessHandle, Output, spawn, communicate, terminate, shellQuote, run, exec, kill, forceKill, runIn, runSilent, runWithEnv, runWithTimeout, pipe, shellPipeline, isRunning, and waitFor.
- Stdin gaps: readKey plus Key, Modifiers, KeyRead, and their public conversion and inspection helpers.
- Copy portable execution wrappers, shell quoting, pipelines, host composition, and result mapping. Reimplement process creation, pipes, signals, timeouts, environment/host inspection, and terminal key decoding using compiler runtime host APIs.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: no Cli module family is exposed. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/cli/execution.dark, host.dark, process.dark, stdin.dark, sys.dark, and backend/testfiles/execution/stdlib/cli-process.dark.

## Proposed issue 019ffc2248b17cf280aa202e76686df8

Title: Add CLI filesystem, POSIX, shell, and daemon modules

Outcome:

Provide canonical environment, filesystem, POSIX, shell configuration, installation, and daemon APIs.

Constraints:

- Environment gaps: Env.get and getAll; Cli.Env.get, getOr, home, set, and unset.
- Directory gaps: create, createRecursive, current, setCurrent, list, listFullPaths, delete, home, listRecursive, createTemp, deleteRecursive, walk, size, and withTemp.
- File gaps: delete, exists, size, getModifiedTime, copy, move, readBytes, readText, readLines, countNewlines, dropThroughFirstNewline, readFully, readTailChunks, readLastLines, writeText, appendText, touch, setPermissions, createTemp, createSymlink, readSymlink, areIdentical, transform, getOwner, withTemp, writeAtomic, withLock, globMatchSegments, glob, watchLoop, and watch.
- FileSystem gaps: FileError and toString, getDirectoryContents, isDirectoryAtPath, readFile, readFileAsString, and writeFile. Path gaps: stripTrailing, join, parent, basename, extension, withExtension, isAbsolute, normalize, resolve, resolveToAbsolute, and relativeTo. Also add Curl.downloadFileToPath, Gunzip.unzipToFile, and PowerShell.Gzip.unzipToFile.
- POSIX gaps: Error, StatResult, Modes.defaultDir/defaultFile, OpenFlags.rdonly/wronly/rdwr/creat/trunc/append, openFile, fdRead, fdReadAll, fdSeekFromStart, stat, getCwd, chdir, setenv, unsetenv, mkdir, rmdir, unlink, rename, chmod, utimesNow, symlink, readlink, isSymlink, mkstemp, mkdtemp, fnmatch, flock, fileOwner, listDir, getenv, kill, sigterm, sigkill, sigint, sleep, isDirectory, isProcessRunning, and mkdirRecursive; Errno.einval/esrch/eperm; StatMode.ifmt/ifdir.
- Shell/install gaps: Bash, Fish, and Zsh read/write/alias/addToPath functions; PowerShell home/directory/file/permission/alias/addToPath functions; Unix file/directory/execution/diff/command/path/alias/home functions; Shim.binDir, path, script, install, and remove.
- Daemon gaps: runDir, pidfilePath, logPath, tailLog, claimPidfile, recordedPid, isRunning, statusLine, stop, launcher, launchDetached, waitUntilRunning, and start.
- Copy high-level .dark path, glob, temporary-resource, shell-file, shim, and daemon workflows. Reimplement POSIX syscalls, descriptors, locking, signals, file watching, ownership/permissions, environment mutation, compression, and downloads through explicit compiler runtime primitives.
- Do not substitute compiler Stdlib.File or Stdlib.Path intrinsics for differently named interpreter APIs without matching their full observable contracts.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/Stdlib.fs:40-70 supplies only compiler-specific File/Path intrinsics. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/env.dark and cli/bash.dark, curl.dark, daemon.dark, dir.dark, env.dark, file.dark, fileSystem.dark, fish.dark, gunzip.dark, path.dark, posix.dark, powershell.dark, shim.dark, unix.dark, and zsh.dark; focused tests include cli-glob.dark and cli-path.dark.

## Proposed issue 019ffc2248b171d599a088d4e40cff33

Title: Add CLI presentation modules

Outcome:

Provide logging, terminal styling, progress, prompts, spinners, and tables.

Constraints:

- Log gaps: info, warn, error, debug, and success.
- UI.Color gaps: esc, red, green, yellow, blue, magenta, cyan, white, gray, bold, dim, italic, underline, strikethrough, bgRed, bgGreen, bgYellow, and bgBlue.
- UI.Progress gap: bar. UI.Prompt gaps: ask, confirm, and select. UI.Spinner gap: run. UI.Table gap: print.
- Copy ANSI formatting, table rendering, and portable presentation logic from the interpreter .dark modules. Reimplement terminal input/output, cursor updates, timing, and TTY-sensitive behavior through runtime primitives.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: no corresponding modules. Baseline at I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d: packages/darklang/stdlib/cli/log.dark and cli/ui/color.dark, progress.dark, prompt.dark, spinner.dark, table.dark; focused evidence includes backend/testfiles/execution/stdlib/cli-color.dark.

## Proposed issue 019ffc2248b1702fa3313fe1a4b5c7c4

Title: Classify the compiler-only utility surface

Outcome:

Separate supported compiler extensions from internal implementation utilities.

Constraints:

- Classify Stdlib.File.readText, exists, writeText, appendText, delete, setExecutable, and writeFromPtr; Stdlib.Path.combine and tempDir; Stdlib.Platform.isMacOS and isLinux; Stdlib.Random.int64; AWS.urlencode_v0; and Twitter.urlencode_v0.
- Record each name as either a deliberate compiler public extension or a private compiler/runtime facility; do not rename it into parity or treat a similarly purposed interpreter Cli, Blob, or package function as evidence of matching behavior.
- No interpreter implementation is available to copy for these names. Reuse existing compiler code while separating visibility and documenting the extension contract.
- Module-specific aliases and extensions already classified by the Blob, text, Dict, integer, Float/Math, List, and Uuid children remain owned by those children.
- Evidence at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3: src/DarkCompiler/Stdlib.fs:40-93 and src/DarkCompiler/stdlib/Path.dark, Platform.dark, and AWS.dark. At I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d, packages/darklang/stdlib has no matching public modules or names.
