---
format: 1
event-id: 01a001f8c1ae76e6860d64ee0f3e315b
entity-id: 019ffc309edd7d59a562a72a37d8a4bb
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T20:31:14.8623507+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc3515e578ad980ee7923f16619d
base-commit: 859978a184a214bdd96fe7fdb48edf2f7efe024f
constraints-hash: 9e4dcb8e3e4fec07897829642f4a326f71c8c5f929e2aa14a2173f7d87a24e63
revision: 019ffc3515e578ad980ee7923f16619d
---
# Implementation plan

Single-change scope pinned to compiler b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 and darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d; checkout 859978a184a214bdd96fe7fdb48edf2f7efe024f was inspected only for current file locations. DCB1 8a402797 is discovery evidence. The accepted single-entry rule is stricter than the pinned interpreter’s execute-all-and-return-last behavior and must be recorded as an intentional divergence.

## Work item 01a001f8c1ab7da98e039e27295963ff

Title: Create the program-structure parity matrix

Category: behavior

Evidence: Compiler coverage is fragmented across SyntaxInteropTests.fs, TypeCheckingTests.fs, and name-resolution.e2e; interpreter anchors are LibParser/Parser.fs, SourceFile.fs, NameResolver.fs, and Builtins.CliHost/Libs/Cli.fs at the pinned revision.

Outcome:

Add focused same-source acceptance, resolution, execution, and diagnostic probes covering declaration order, duplicate names, forward references, recursion, nested modules, package qualification, source composition, and entry cardinality.

## Work item 01a001f8c1ab7603a379e702d85e5712

Title: Introduce source-accurate program types

Category: type

Evidence: AST.fs:351-357 currently models only FunctionDef, TypeDef, and Expression; NameSyntax.fs:43-52 has a temporary richer tree that is discarded during normalization.

Outcome:

Replace the flat Program/TopLevel representation with recursive modules, function/value/type declarations, source-unit identity and purpose, and explicit entry-expression candidates. Use a separate validated executable-program type that contains exactly one entry.

## Work item 01a001f8c1ab78e1adbe125759386c7d

Title: Preserve modules and values across parsing

Category: behavior

Evidence: NameSyntax.fs:232-265 flattens module paths and rejects top-level values; Parser.fs:2227-2280 and InterpreterParser.fs:2536-2631 already expose partial declaration parsing. Pinned interpreter WrittenTypes.fs:475-489 preserves DFunction, DValue, DModule, DType, and expressions.

Outcome:

Make both parser modes produce the shared program tree for interpreter declaration forms: let functions, val values, types, file module headers, nested module blocks, and trailing executable expressions. Stop flattening modules into dotted strings and stop rejecting SourceValue during normalization.

## Work item 01a001f8c1ab73c6be1a8bd08a550298

Title: Remove legacy top-level declaration syntax

Category: behavior

Evidence: At compiler b2e1f3d1, Parser.fs:2141-2174 and InterpreterParser.fs:2610-2695 accept def; pinned interpreter GRAMMAR.md:262-279 defines let functions, val values, and treats rec/private/internal as ordinary identifiers.

Outcome:

Reject compiler-only def declarations in every public parser path, retain let exclusively for functions and local/script bindings, and require val for declaration-scope values. Keep explicit negative probes for removed forms.

## Work item 01a001f8c1ab71c9a18fbf6863553fbe

Title: Compose source units structurally

Category: type

Evidence: CompilerLibrary.fs CompileRequest has one Source and SourceFile; loadStdlib merges flattened TopLevel lists, while preamble tooling concatenates source text. Interpreter SourceFile.items carries each declaration’s module path.

Outcome:

Change compilation input from one source string to a non-empty ordered collection of named source units. Parse units independently, compose their declaration trees without text concatenation, preserve caller and declaration order, and prohibit entries in dependency/package units.

## Work item 01a001f8c1ab7a41b9f42bfe54528713

Title: Match declaration namespaces and duplicate selection

Category: behavior

Evidence: TypeChecking.summarizeTopLevelDeclarations currently overwrites maps while current name-resolution.e2e:13 expects duplicate-function ambiguity. Pinned ProgramTypes.PackageManager.withExtras uses Map.ofList for each category, giving deterministic last-wins behavior.

Outcome:

Build separate type, value, and function inventories keyed by package/module location. Within the same namespace and category, make the last declaration at a repeated location win as in the interpreter overlay; allow same-spelled values and functions to coexist and resolve by expression context.

## Work item 01a001f8c1ab7812b47d68b3f756e43d

Title: Implement hierarchical module visibility

Category: function

Evidence: Current NameSyntax normalization erases module boundaries. Pinned LibDB/NameLookup.fs:13-41 defines the current-module-to-parent candidate order, and NameResolver.fs applies it independently to types, values, and functions.

Outcome:

Resolve names from the current module outward through parent modules, followed by explicit package locations and the interpreter’s Stdlib shortcut. Child declarations remain unqualified only inside their module; parent and sibling access requires the corresponding path.

## Work item 01a001f8c1ab7cb2b4601dbc34a068eb

Title: Use contextual value, function, type, and constructor resolution

Category: function

Evidence: Current TypeChecking.declarationResolutionEnvironment creates global constructor candidates; pinned WrittenTypesToProgramTypes.fs resolves bare names value-first, calls function-first, and retains package type identities for enum cases.

Outcome:

Apply lexical shadowing before package lookup; resolve bare references value-first, applied references function-first then value, and types in their own namespace. Resolve constructors through their declaring type identity so equal case names remain type-contextual instead of becoming global string collisions.

## Work item 01a001f8c1ab7272b2d266d3d536237a

Title: Support immutable top-level values

Category: behavior

Evidence: NameSyntax.normalizeSource currently reports that native top-level values are unsupported. Pinned interpreter WrittenTypes.fs:591-607 and CLI declarationsToModule collect package values alongside types and functions; ProgramTypesToRuntimeTypes.PackageValue materializes their runtime bodies.

Outcome:

Type-check value declarations, establish their package/module identities, evaluate each accepted value once before executable entry use, and expose the resulting immutable value to functions, later values, and entries under interpreter visibility rules. Diagnose unsupported value cycles deterministically during whole-program validation.

## Work item 01a001f8c1ab75409dc46ccd3dea3d87

Title: Align forward references and recursive groups

Category: behavior

Evidence: TypeChecking.checkResolvedProgramInternal pre-registers flat function/type maps but lacks module/value boundaries. Pinned CLI declarationsToModule performs a two-pass overlay for sibling references, and WrittenTypesToProgramTypes recognizes package-function self calls.

Outcome:

Inventory all sibling declarations before resolving bodies so supported function, value, and type forward references are order-independent. Preserve implicit function self-recursion, derive mutually recursive function SCCs, and do not introduce rec declaration syntax.

## Work item 01a001f8c1ab7bbaaffe1d54f688040b

Title: Generalize the AOT package snapshot

Category: type

Evidence: CompilerLibrary.fs:761-816 models only PackageValueCatalog for ValueSearch. Pinned ProgramTypes.PackageManager and NameResolver resolve separate type/value/function locations against a branch-aware package view; an immutable snapshot is the retained AOT-specific extension.

Outcome:

Replace the specialized package-value catalog with an immutable compile request snapshot containing branch-visible package types, values, functions, locations, hashes, and availability. Resolve all package references to stable identities at compile time and fail if required snapshot content is absent.

## Work item 01a001f8c1ab7780addf8f90438d4066

Title: Validate the composed whole program

Category: function

Evidence: CompilerLibrary.compileUserWithPlan type-checks user code against a separately built base environment; TypeChecking.checkProgramInternal builds registries per Program. Preambles and stdlib currently bypass normal entry/declaration checks through synthetic expressions.

Outcome:

Run declaration-shape, duplicate-policy, name-resolution, type, constructor, specialization, package-availability, value-cycle, and entry checks over the complete composed program before ANF conversion. Validate unused declarations statically rather than reproducing interpreter runtime deferral.

## Work item 01a001f8c1ab7d96a2d30a680f2b3fa4

Title: Require one executable entry

Category: behavior

Evidence: TypeChecking.fs:6844-6856 accepts the first Expression or falls back to main; CompilerLibrary.fs:1784-1801 synthesizes _start from MainExpr. Pinned interpreter CLI execute accepts no expressions only as a runtime error and executes every expression, returning the last, so multiple-entry rejection is the accepted intentional divergence.

Outcome:

Accept exactly one executable entry expression across executable source units, reject zero or multiple candidates with deterministic whole-program errors, and remove the main(): Int64 fallback. Construct _start only from the validated entry.

## Work item 01a001f8c1ab7550bded5ad3d1c4aa76

Title: Align entry completion behavior

Category: behavior

Evidence: Pinned Builtins.CliHost/Libs/Cli.fs maps script results from DUnit, DInt, or DInt64 and reports other results; current CompilerLibrary.compileUserWithPlan rewrites and prints arbitrary non-Unit program values through ValueRendering and PrintInsertion.

Outcome:

For script/file execution, map Unit to exit code zero and Int or Int64 to a checked process exit code; reject other statically known entry result types. Keep inline expression evaluation as an explicit eval mode that renders non-Unit values rather than treating rendering as file-entry selection.

## Work item 01a001f8c1ab73ef92a8c5f154c1da18

Title: Lower resolved identities through the backend

Category: function

Evidence: AST_to_ANF.splitTopLevels and CompilerLibrary conversion paths assume a flat list and string-keyed registries; FunctionTreeShaking roots the synthetic string name _start.

Outcome:

Update AST-to-ANF registry construction, monomorphization, lambda lifting, tree shaking, and entry synthesis to consume validated declaration identities and top-level values without re-flattening them into ambiguous strings. Keep native symbol mangling internal and deterministic.

## Work item 01a001f8c1ab748183303256db2b5af8

Title: Remove dummy-entry synthesis from library paths

Category: behavior

Evidence: CompilerLibrary.buildStdlibWithTrace appends UnitLiteral; analyzePreamble and buildPreambleContext append synthetic terminators; package catalog validation also constructs a synthetic program with UnitLiteral.

Outcome:

Allow declaration-only stdlib, package, and test-preamble units to validate and compile without appended Unit or zero expressions. Keep executable entry requirements confined to validated executable programs.

## Work item 01a001f8c1ab734f927e1f6c6ca4372b

Title: Adapt CLI and test composition APIs

Category: other

Evidence: Program.fs reads exactly one source and populates CompileRequest.Source; E2ETestRunner.fs parsePreambleAsProgram and combinePreambleWithTestSource append terminators and concatenate text.

Outcome:

Have CLI file, stdin, and inline-expression paths construct explicit source units and execution modes. Extend the e2e harness to submit preamble/library and test/executable units structurally, enabling whole-program and multi-unit parity fixtures without source concatenation.

## Work item 01a001f8c1ab7b7db76a672ed9268968

Title: Migrate repository programs and fixtures

Category: other

Evidence: Compiler b2e1f3d1 contains 106 .dark/.e2e files with def declarations; the current tree still has def in src/Tests/e2e/upstream/stdlib/base64.dark and synthetic-entry assumptions throughout the e2e runner.

Outcome:

Convert supported repository programs to interpreter declaration spelling, explicit source-unit roles, and explicit executable entries. Retain legacy syntax only inside rejection tests, and remove fixtures whose success depends on main fallback or injected terminators.

## Work item 01a001f8c1ab7e99a6a7f8c5f63102a6

Title: Publish the program-structure parity ledger

Category: other

Evidence: docs/interpreter-parity.md at DCB1 8a402797 identifies the flat program model only as an inventory; current docs/name-resolution.md records duplicate ambiguity and other rules that conflict with the pinned interpreter overlay.

Outcome:

Document the implemented declaration, visibility, duplicate, recursion, module, package, composition, validation, and entry rules. Label immutable AOT package snapshots and multi-unit composition as compiler extensions, static whole-program rejection as an AOT divergence, and singular-entry enforcement as an intentional divergence.
