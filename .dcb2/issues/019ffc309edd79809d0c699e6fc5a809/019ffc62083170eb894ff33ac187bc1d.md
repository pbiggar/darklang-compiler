---
format: 1
event-id: 019ffc62083170eb894ff33ac187bc1d
entity-id: 019ffc309edd79809d0c699e6fc5a809
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-13T18:28:30.8979823+00:00
author: worker:65eb66d2e522:3476523:019ffc477bd07f42bc37b64ed73e8581
previous: 019ffc3515e47a2bbc1dd02e2868451d
base-commit: 25bc0cc78de28e6cc5385b5bc54c739a9b603b7c
constraints-hash: c47aea199c7856d409fc9774668412e6d52ab3982f5d916a282054ae5118b9fa
revision: 019ffc3515e47a2bbc1dd02e2868451d
---
# Implementation plan

One contained parser-to-runtime parity change, comparing compiler b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 with darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d; DCB1 report 8a402797 and current parity documents are supporting leads, while focused same-source probes define the retained behavior.

## Work item 019ffc62083178fea5d9ce941c87211c

Title: Establish the ADT parity test matrix

Category: behavior

Evidence: Existing ADT coverage is under src/Tests/e2e/; docs/features/sum-types.md and the parity material derived from report commit 8a402797 provide candidate cases requiring revalidation against both pinned revisions.

Outcome:

Add focused same-source end-to-end cases covering interpreter declaration and construction syntax, nullary and payload cases, recursive and generic types, qualified references, collisions across types and modules, duplicate and invalid declarations, payload arity and evaluation, rendering, and nominal equality. Encode the interpreter-observed result or semantic rejection for each case.

## Work item 019ffc6208317029bedc092725d6b9f8

Title: Model interpreter-compatible declarations and constructor references

Category: type

Evidence: src/DarkCompiler/passes/1_Parser.fs owns type-declaration and constructor-expression parsing; its output feeds src/DarkCompiler/passes/1.5_TypeChecking.fs and src/DarkCompiler/passes/2_AST_to_ANF.fs, so syntax distinctions must survive beyond parsing.

Outcome:

Replace compiler-specific declaration and construction shapes with AST cases that distinguish type parameters, declaring module and type, constructor name, nullary cases, and the interpreter's payload shape. Represent qualified and unresolved constructor references explicitly without sentinel strings or inferred defaults.

## Work item 019ffc620831752fac3b80489d6e9a73

Title: Match declaration grammar and remove compiler-only syntax

Category: behavior

Evidence: The relevant compiler grammar is in src/DarkCompiler/passes/1_Parser.fs; current ADT e2e sources and src/DarkCompiler/Stdlib.fs are the principal consumers to compare with the darklang/dark type-declaration parser at 04fbe9dcc995c6188757d583e273cbd30a3e2d3d.

Outcome:

Accept exactly the interpreter-supported type declaration, generic parameter, constructor, payload, recursive reference, and qualified construction forms. Reject the migrated compiler-only declaration and constructor extensions through normal parse or declaration diagnostics.

## Work item 019ffc6208317c8daf026e5ec92bc214

Title: Build and validate nominal type and constructor declarations

Category: function

Evidence: Declaration information currently crosses src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, and src/DarkCompiler/passes/2_AST_to_ANF.fs; duplicate, invalid, recursive, and colliding declarations require a shared symbol model rather than independent name handling.

Outcome:

Create a pure declaration-validation pass that predeclares type identities for recursion, validates generic parameters and payload type references, and builds constructor symbols containing module path, declaring type identity, case name, type parameters, and payload types. Reject empty or malformed declarations and duplicates in the same interpreter-defined namespace while allowing legal same-named constructors belonging to different types or modules.

## Work item 019ffc62083178aa840959bc61f35e0b

Title: Implement interpreter-compatible constructor resolution

Category: function

Evidence: src/DarkCompiler/passes/2_AST_to_ANF.fs performs name resolution and lowering, while collision and qualification probes expose whether lookup is currently based only on constructor text instead of module and declaring-type identity.

Outcome:

Resolve constructor references using lexical module scope plus declaring-type context, supporting the interpreter's unqualified and qualified forms. Select a unique nominal constructor, report unresolved or ambiguous references, and keep constructors with identical display names or payload shapes distinct across types and modules.

## Work item 019ffc62083173d8a7a9770bab6160f4

Title: Type constructors, recursion, and generics consistently

Category: function

Evidence: src/DarkCompiler/passes/1.5_TypeChecking.fs owns expression and declaration validation; generic instantiation, recursive ADTs, nullary use, payload application, and incorrect-arity e2e probes exercise this boundary.

Outcome:

Instantiate fresh generic arguments at every constructor use, tie recursive payload references to the predeclared nominal type, and unify payload types with the constructed result type. Treat nullary constructors as values and payload constructors according to the interpreter's callable shape, including the distinction between multiple fields and a single tuple where applicable, with exact payload-arity rejection.

## Work item 019ffc62083174068dae5cf6ed004568

Title: Lower construction with interpreter evaluation semantics

Category: function

Evidence: src/DarkCompiler/passes/2_AST_to_ANF.fs is the construction-lowering boundary; payload side-effect and failure probes distinguish lookup, arity checking, and evaluation-order behavior.

Outcome:

Carry the resolved nominal constructor symbol into ANF, evaluate payload expressions exactly once in the interpreter-observed order and error timing, and emit one construction operation for both nullary and payload cases. Remove lowering paths that reinterpret constructor names, accept compiler-only application forms, or compensate for invalid arity.

## Work item 019ffc6208317e169547d414088097fb

Title: Preserve nominal constructor identity in native values

Category: type

Evidence: src/DarkCompiler/Runtime.fs defines runtime support, with native construction consumed by src/DarkCompiler/passes/arm64/6_CodeGen.fs and src/DarkCompiler/passes/x64/6_CodeGen.fs; collision probes require distinct values for equal-looking cases from different nominal declarations.

Outcome:

Define a runtime ADT representation containing a canonical declaring-type identity, constructor identity, and payload, and carry it consistently through shared lowering and both native backends. Ensure recursive payload ownership and reference counting remain valid and that identity cannot collapse to an unqualified case name, ordinal, or payload shape.

## Work item 019ffc620831778abe74df7aa0a0882b

Title: Align ADT rendering and equality

Category: function

Evidence: src/DarkCompiler/Runtime.fs owns observable runtime operations; ADT rendering and equality cases in src/Tests/e2e/ can be run unchanged against both pinned revisions to verify public output and nominal identity.

Outcome:

Render nullary, payload, qualified, recursive, and generic constructor values in the interpreter's public format. Make equality compare nominal declaring-type and constructor identities before recursively comparing payloads, so same-name or same-shape constructors from different types or modules are unequal.

## Work item 019ffc62083177a0996a7dbaa0bf7f2f

Title: Migrate sources and retire obsolete ADT forms

Category: other

Evidence: Likely migration surfaces include src/DarkCompiler/Stdlib.fs, src/Tests/e2e/, docs/features/sum-types.md, docs/compiler-passes.md, and examples or fixtures located by searches for the obsolete forms; parser rejection tests demonstrate that removed syntax is no longer accepted.

Outcome:

Rewrite affected stdlib, tests, examples, and documentation to interpreter-compatible declarations and construction expressions, delete obsolete parser, type-checker, and lowering branches, and document any revalidated observable divergence that cannot be eliminated.

## Work item 019ffc6208317a4a8b19d29b96bfe77f

Title: Verify the integrated parity surface

Category: behavior

Evidence: ./run-tests --ai is the repository-wide verification entry point; the revision-pinned same-source corpus supplies direct evidence for declarations, resolution, typing, construction, evaluation, rendering, and equality.

Outcome:

Run the complete AI test suite and the focused programs through both pinned implementations, confirming matching results or equivalent semantic rejection for every matrix entry and checking that no existing language feature regresses through the revised constructor namespace and runtime representation.
