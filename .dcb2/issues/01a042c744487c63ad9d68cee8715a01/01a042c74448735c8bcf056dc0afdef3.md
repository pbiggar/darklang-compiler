---
format: 1
event-id: 01a042c74448735c8bcf056dc0afdef3
entity-id: 01a042c744487c63ad9d68cee8715a01
entity-kind: issue
event-type: created
occurred-at: 2026-08-27T10:32:30.5367124+00:00
author: worker:65eb66d2e522:2723051:01a040883a7676b8be602d3f750e253d
batch: 01a042c740c37421ad74e9a0ace20a34
problem: 019ffcba981b77209b745b1ac3cd7d26
workflow: trial-first
---
# Hoist heap-end setup within Fasta’s safe allocation regions

Remove repeated ARM64 heap-end materialization only where it demonstrably reduces Fasta’s hot allocation cost without register-pressure regressions.

## Constraints

- Target ARM64 only; leave x64 behavior unchanged.
- Do not reserve any register globally or remove heap-overflow checks.
- Require a local proof that the chosen region preserves the heap-end register across every emitted instruction and control-flow edge.
- Preserve allocation order, free-list behavior, OOM behavior, and leak-check behavior.
- Use focused IR and assembly evidence, the full test suite, and routine benchmarks; retain only a material Fasta gain without meaningful regressions.
- Record a rejected trial with measurements and remove the active candidate when evidence is insufficient.

## Trial work

### Trial item 01a042c74448716992eaf251343c21a6

Title: Trial local heap-end commoning in Fasta

Category: ARM64 allocator code generation

Evidence: Current `withHeapBoundsCheck` recomputes heap end with `MOVZ X11` plus `ADD X11, X27, X11` for every bump allocation; the investigation identifies local low-pressure regions as the remaining alternative after the global dedicated-register experiment regressed Fasta.

Outcome:

Confirm repeated `MOVZ`/`ADD` heap-end setup in a current Fasta allocation region, then measure a locally scoped commoning transform against the existing per-allocation guard.
