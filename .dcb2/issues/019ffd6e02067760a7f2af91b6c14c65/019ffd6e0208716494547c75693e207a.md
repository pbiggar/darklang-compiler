---
format: 1
event-id: 019ffd6e0208716494547c75693e207a
entity-id: 019ffd6e02067760a7f2af91b6c14c65
entity-kind: issue
event-type: created
occurred-at: 2026-08-13T23:21:12.9689936+00:00
author: worker:65eb66d2e522:3659890:019ffcb6af5973a3bed1d8501af1ef62
batch: 019ffd6e019d7f3ca172031826d0567b
problem: 019ff54dc66279a38e5ae60d59e804a3
workflow: trial-first
---
# Reuse SSA definition summaries

Reduce SSA Construction time by deriving definition sites and renaming bounds from one per-function definition scan.

## Constraints

- Limit retained changes to pass 3.1 definition analysis shared by phi insertion and initial renaming; retain one immutable summary.
- Use an isolated worktree with a pinned revision, toolchain, host conditions, warm-up policy, and alternating run order.
- Profile getAllDefs, getBlockDefs, and initial-renaming scans first; discard the trial if duplicated work is not material.
- For every routine benchmark program, collect 10 paired before/after -vv runs and report raw SSA Construction and total-compilation results, medians, absolute deltas, and percentage deltas.
- Collect 10 paired before/after ./run-tests --ai wall-clock runs with raw results, medians, and deltas; require all runs to pass.
- Require identical phi placement, renamed MIR, binaries, benchmark outputs, and instruction counts; run ./run-tests --ai and ./benchmarks/run_benchmarks.sh --verify routine and report the RESULTS.md ratio.
- Retain only a clear improvement beyond a predeclared noise threshold; record discarded summary shapes and remove all profiling and instrumentation.

## Trial work

### Trial item 019ffd6e020672e3af18745e634895a0

Title: Profile repeated SSA definition scans

Category: SSA profiling

Evidence: Current fasta SSA Construction is 17.0 ms; insertPhiNodes computes definition sites, then createInitialRenamingState scans definitions again after phi insertion.

Outcome:

Measure definition traversal counts and exclusive time across phi insertion and renaming initialization.

### Trial item 019ffd6e02067e6696057bda8a1baeed

Title: Trial one definition summary

Category: SSA analysis reuse

Evidence: Phi insertion and initial renaming consume compatible facts from the same function CFG, and no existing .dcb2 issue represents this reuse boundary.

Outcome:

Carry exact per-register definition sites and maximum register identity into both consumers without changing phi or version semantics.
