---
format: 1
event-id: 01a001c40e167e7abb173baa74babacc
entity-id: 019ffc31d4127ec682e5afc51bc35997
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T19:33:41.0147350+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc347e947e1696c487d371e1cef6
base-commit: 05bc4ed048de385b424fe3919d7939129b659bda
constraints-hash: 582b37462cf0a1ca5984a8bef0def3655a10003b5db12d3ad57d93c5fa145bbb
revision: 019ffc347e947e1696c487d371e1cef6
---
# Implementation plan

One contained stdlib/runtime change. The accepted gap is confirmed at C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3 against I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d; current integration HEAD is 05bc4ed048de385b424fe3919d7939129b659bda. Interpreter calls evaluate arguments eagerly from left to right, so Option/Result short-circuiting selects which already-evaluated value is propagated; Retry obtains true laziness from its Unit callback.

## Work item 01a001c40e167b3c978fcb0e7fb68994

Title: Implement Option, Result, and Retry parity

Category: behavior

Evidence: C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3 `src/DarkCompiler/stdlib/Option.dark:1-118`, `Result.dark:1-125`, and its tree contain none of the requested additions. I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d defines [Option.and](https://github.com/darklang/dark/blob/04fbe9dcc995c6188757d583e273cbd30a3e2d3d/packages/darklang/stdlib/option.dark#L187-L195), [Result.and/or](https://github.com/darklang/dark/blob/04fbe9dcc995c6188757d583e273cbd30a3e2d3d/packages/darklang/stdlib/result.dark#L197-L214), and [all Retry loops](https://github.com/darklang/dark/blob/04fbe9dcc995c6188757d583e273cbd30a3e2d3d/packages/darklang/stdlib/retry.dark#L4-L52); interpreter application lowering evaluates arguments sequentially at `backend/src/LibExecution/ProgramTypesToRuntimeTypes.fs:937-963`. Current upstream fixtures already contain Option cases at lines 288/291/294/297 and Result cases at 223/227/231/235 and 240/244/248/252, but `TestRunner.fs:494-546` does not enable them. At current HEAD, `CliPosix.dark:29-30` invokes a shell, while `Stdlib.fs:82`, `2_AST_to_ANF.fs:152-163`, and ANF/MIR/LIR define `Sleep`; both code generators still return Unit without delaying (`arm64/6_CodeGen.fs:6106-6107`, `x64/6_CodeGen.fs:4277`). Historical DCB1@8a402797ccccda0ca47b516b356ae1de4d670038 identifies the same missing surface but supplies no retained behavioral proof.

Outcome:

First add failing focused coverage, then add `Option.and<'a,'b>` returning the second option only for `Some`, `Result.and` returning the second result only for `Ok`, and `Result.or` returning the second result only for `Error`, preserving the selected input and its payload. Enable the pinned upstream truth-table cases and add same-source probes for left-to-right argument evaluation and differing payloads. Add `Stdlib.Retry` to stdlib loading with the four pinned functions and signatures: both wrappers begin at attempt 1; `withBackoff` begins at 100.0ms and doubles after each eligible failure; fixed delay remains unchanged; callbacks run at least once, stop on first `Ok` or when `attempt >= maxAttempts`, return the final callback result unchanged, and sleep only between attempts. Replace `Cli.Posix.sleep` shell execution with the existing internal `Stdlib.Cli.__sleep` boundary, carrying its Float argument through a typed sleep operation rather than the current untyped/no-op CLI case, and implement blocking millisecond delay on Linux ARM64, Linux x86_64, and macOS ARM64 with normalized native timeout storage and interrupt-safe completion. Add deterministic Retry E2E tests using observable callbacks and zero or tiny delays for immediate success, exhausted failure, nonpositive maximums, explicit loop starting attempts, attempt counts, final payloads, and fixed/backoff entry points; add backend-focused assertions for timeout conversion and syscall emission where host E2E cannot cover another target. Record a revision-pinned parity matrix, update the stdlib catalog and CLI sleep documentation, and classify `Stdlib.Cli.__sleep` as an internal compiler-only execution boundary and ahead-of-time type-error timing as the retained divergence. Verify with the complete test suite and routine benchmark profile.
