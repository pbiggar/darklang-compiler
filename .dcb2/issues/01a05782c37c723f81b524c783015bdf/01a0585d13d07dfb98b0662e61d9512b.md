---
format: 1
event-id: 01a0585d13d07dfb98b0662e61d9512b
entity-id: 01a05782c37c723f81b524c783015bdf
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-31T15:08:10.0649108+00:00
author: worker:65eb66d2e522:2803808:01a05855875373aebf83b550886cf76a
previous: 01a05782c37d7e89bc63fdb735b531be
base-commit: 1dc3ad83a87a57fe5c39496a7f504e182d7946f7
constraints-hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
reply-to: 01a0585c75bc717e8cc8bf00392be3fa
revision: 01a05782c37d7e89bc63fdb735b531be
---
# Breakdown

The container refresh remains independent. Compiler parity is currently pinned to upstream `04fbe9d`; candidate upstream `main` was `7fc6d80`, 191 commits later. Known potential deltas include package-root parsing, bare Option/Result constructors, diagnostic naming, and package-authoring type checking.

## Proposed issue 01a0585d13cd77d8b7c1403349f322b6

Title: Refresh the container’s released Darklang interpreter

Outcome:

Container builds install and expose Darklang CLI v0.0.34 (`d5076d6`) rather than `a70f30d`, with validation tooling confirmed against that binary.

Constraints:

- Do not change native compiler semantics as part of this issue.
- Keep the installer selecting the latest matching published release rather than hard-coding a transient binary URL.
- Run the full repository test suite and the interpreter-validation workflow after the update.
- Execution preference: economy

## Proposed issue 01a0585d13cd7e8da2a844c27e60f679

Title: Analyze the upstream Darklang compatibility delta

Outcome:

A read-only report pins a candidate upstream SHA, classifies every relevant change since `04fbe9d`, and recommends the exact compiler, test, documentation, and exclusion scope required for alignment.

Constraints:

- Do not modify repository files or write compiler code.
- Compare immutable upstream commits, not a moving branch name.
- Distinguish language/runtime semantics from CLI UX, performance, release engineering, and package-authoring infrastructure.
- For each proposed compiler change, provide upstream evidence, affected local components, observable behavior, and a proposed focused E2E case.
- State explicitly whether at-rest package type checking belongs in the native compiler scope.
- Execution preference: frontier

## Proposed issue 01a0585d13cd7cb5823e7972b66ac81a

Title: Implement the approved upstream compatibility changes

Outcome:

Only the compiler changes approved from the compatibility analysis are implemented, tested, documented, and validated against its pinned upstream target.

Constraints:

- Do not begin until the analysis issue’s report and implementation scope are approved.
- Use focused failing E2E tests before each compiler behavior change.
- Implement only items classified as required in the approved report.
- Run full tests, interpreter comparison, and routine benchmarks before completion.
- Execution preference: frontier

# Shared dossier

## Shared context

The released-binary refresh and semantic compatibility work remain separate. The semantic migration is now explicitly gated by a read-only analysis so that implementation follows evidence rather than preliminary commit-message inspection.

## Decisions

- Keep the release refresh as a standalone low-risk issue.
- Replace the direct compiler-alignment issue with analysis first, then a dependent implementation issue.
- Treat at-rest package type checking as undecided until the analysis establishes whether it affects the native compiler’s supported scope.

## Exclusions

- No compiler implementation is authorized by the analysis issue.
- Do not use a moving upstream branch as the final target.
- Do not inherit CLI UX, performance, release, or package-authoring changes without demonstrated native-compiler relevance.

## Tools

### Tool upstream-compare

Purpose: Enumerate the exact commit and file delta from the current parity pin to the selected upstream SHA.

Command: `git ls-remote https://github.com/darklang/dark.git refs/heads/main && curl -fsSL -H 'Accept: application/vnd.github+json' 'https://api.github.com/repos/darklang/dark/compare/04fbe9dcc995c6188757d583e273cbd30a3e2d3d...<selected-sha>'`

Max output bytes: 4096

### Tool local-parity-scan

Purpose: Locate all local references, parity documentation, tests, and compiler components affected by the upstream target.

Command: `rg -n '04fbe9dcc995c6188757d583e273cbd30a3e2d3d|darklang/dark|darklang-interpreter' docs src scripts Dockerfile`

Max output bytes: 4096

### Tool upstream-source-inspection

Purpose: Inspect only the upstream parser, execution, stdlib, and test diffs that the compare identifies as potentially semantic.

Command: `curl -fsSL -H 'Accept: application/vnd.github+json' 'https://api.github.com/repos/darklang/dark/compare/04fbe9dcc995c6188757d583e273cbd30a3e2d3d...<selected-sha>' | jq -r '.files[] | select(.filename | test("^(backend/src/Lib(Parser|Execution)/|packages/darklang/(stdlib|languageTools)/|backend/testfiles/execution/)")) | [.status, .filename, .patch] | @json'`

Max output bytes: 4096

## Child 01a0585d13cd77d8b7c1403349f322b6

### Context

The existing installer selects the latest matching published GitHub-release asset. v0.0.34 (`d5076d6`) is one commit older than the existing compiler parity pin, so the refresh is tooling maintenance.

### Dependencies

- none

### Acceptance

- `darklang-interpreter --version` reports the v0.0.34 release revision after a clean container build.
- `./run-tests --ai` passes.
- The repository’s interpreter-validation workflow completes with the refreshed binary.

### Tools

- none

## Child 01a0585d13cd7e8da2a844c27e60f679

### Context

Candidate upstream head was `7fc6d80`, 191 commits after the current `04fbe9d` pin. Preliminary evidence identifies parser root-scope changes, bare `Some`/`None` and `Ok`/`Error` resolution, type-error display naming, and at-rest package checking as areas requiring classification.

### Dependencies

- Refresh the container’s released Darklang interpreter

### Acceptance

- The report names an immutable candidate SHA and the exact compared range.
- Every relevant upstream change is classified as required compiler work, test-only/documentation work, or excluded, with rationale.
- The report identifies affected repository files/passes and proposed E2E tests for each required behavior.
- The report makes a clear go/no-go recommendation and provides a bounded implementation checklist.
- No repository files are changed.

### Tools

- upstream-compare
- local-parity-scan
- upstream-source-inspection

## Child 01a0585d13cd7cb5823e7972b66ac81a

### Context

This issue intentionally has no predetermined code scope. Its authoritative scope is the approved output of the preceding compatibility analysis.

### Dependencies

- Analyze the upstream Darklang compatibility delta

### Acceptance

- All approved analysis findings have corresponding passing E2E coverage.
- Parity documentation records the new immutable upstream target and explicit exclusions.
- `./run-tests --ai` passes.
- Interpreter comparison succeeds for the adopted behaviors.
- Routine benchmarks complete without failure or regression.

### Tools

- none

## Response to feedback

The operator requires a presented analysis of differences and required compiler changes before authorizing the semantic alignment work.

## What changed

- Split the former direct compiler-alignment child issue into a read-only compatibility analysis and a dependent implementation issue.
- Added acceptance criteria requiring a commit-pinned, behavior-by-behavior recommendation and explicit exclusions before implementation.
- Added bounded read-only research tools for the analysis issue.

## Why

A separate analysis creates a reviewable decision record, keeps the eventual implementation narrowly scoped to observable compiler behavior, and preserves the repository’s test-first change discipline.

## Conversation response

Agreed. The compiler-alignment work is now staged behind an explicit, read-only compatibility analysis. That analysis must identify the actual semantic delta and recommend a bounded implementation scope before any compiler changes begin.

Understanding: the second proposed issue should investigate and present the required compiler changes first, rather than directly authoring them.

Changes: the former compiler-alignment issue is split into an analysis issue and a dependent implementation issue.

Reasoning: this makes the target-SHA choice and scope evidence-based, and prevents upstream infrastructure or performance-only changes from being mistaken for compiler requirements.
