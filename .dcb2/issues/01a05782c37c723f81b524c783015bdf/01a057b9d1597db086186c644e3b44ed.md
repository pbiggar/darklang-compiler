---
format: 1
event-id: 01a057b9d1597db086186c644e3b44ed
entity-id: 01a05782c37c723f81b524c783015bdf
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-31T12:09:50.6818332+00:00
author: worker:65eb66d2e522:2648420:01a057b3185e74dc8f08680a7efebfda
previous: 01a05782c37d7e89bc63fdb735b531be
base-commit: e0bb757c861640c5020ab6483864b69cd273f774
constraints-hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
reply-to: 01a05799c63d7bf89a0979f38bcddb35
revision: 01a05782c37d7e89bc63fdb735b531be
---
# Breakdown

The container has Darklang CLI `alpha-a70f30d`; the latest released artifact is v0.0.34 / `d5076d6`. Compiler parity is pinned to `04fbe9d`, one commit newer than that release. Current upstream `main` is `7fc6d80`, 191 commits beyond the pin.

## Proposed issue 01a057b9d15477efa4d441bcd113cece

Title: Refresh the container’s released Darklang interpreter

Outcome:

Container builds install and expose Darklang CLI v0.0.34 (`d5076d6`) rather than `a70f30d`, with validation tooling confirmed against that binary.

Constraints:

- Do not change native compiler semantics as part of this issue.
- Keep the installer selecting the latest matching published release rather than hard-coding a transient binary URL.
- Run the full repository test suite and the interpreter-validation workflow after the update.
- Execution preference: economy

## Proposed issue 01a057b9d1547d12bed69838c01d0513

Title: Align compiler parity with current upstream Darklang

Outcome:

The compiler’s authoritative upstream target advances from `04fbe9d` to a deliberately pinned current-upstream SHA, with supported observable language behavior matched and documented.

Constraints:

- Select and record one immutable upstream SHA before implementation; do not target a moving branch.
- Use E2E tests first for every supported behavior change.
- Do not copy upstream package-authoring infrastructure unless it is relevant to native compilation/runtime behavior.
- Run full tests and the routine benchmark profile before completion.
- Execution preference: frontier

# Shared dossier

## Shared context

The released-interpreter refresh and the upstream compiler-parity migration are intentionally separate: the former updates validation infrastructure to v0.0.34, while the latter advances the semantic target beyond the repository’s existing `04fbe9d` pin.

## Decisions

- Use v0.0.34 / `d5076d6` as the released interpreter update target.
- Use an immutable current-upstream SHA, selected at execution time, for the compiler parity migration.
- Treat at-rest package type checking as out of scope unless the audit demonstrates a compiler/runtime requirement.

## Exclusions

- Do not make native compiler semantic changes in the release-refresh issue.
- Do not use an unpinned moving `main` branch as validation authority.
- Do not adopt package-authoring infrastructure merely because upstream added it.

## Tools



## Child 01a057b9d15477efa4d441bcd113cece

### Context

v0.0.34 (`d5076d6`) predates the existing compiler reference pin `04fbe9d` by one commit, so this is a tooling refresh rather than a compiler-target migration.

### Dependencies

- none

### Acceptance

- `darklang-interpreter --version` reports the v0.0.34 release revision after a clean container build.
- `./run-tests --ai` passes.
- The repository’s Darklang interpreter-validation workflow runs successfully with the refreshed binary.

### Tools

- none

## Child 01a057b9d1547d12bed69838c01d0513

### Context

The candidate upstream head is `7fc6d80`. Known changes beyond `04fbe9d` include package-root declaration parsing, bare `Some`/`None` and `Ok`/`Error` resolution, at-rest package type checking, and type-error naming improvements. At-rest checking is not automatically in scope because it is package-authoring infrastructure.

### Dependencies

- Refresh the container’s released Darklang interpreter

### Acceptance

- An immutable upstream target SHA is documented and used by parity evidence.
- New supported behaviors have E2E tests that failed before implementation and pass afterward.
- Package-root parsing and bare Option/Result constructors are either implemented with tests or explicitly documented as exclusions with upstream evidence.
- The full test suite passes.
- Routine benchmarks complete with no failures or regression.

### Tools

- none

## Conversation response

I’ve split the approved work into a safe released-interpreter refresh followed by a pinned upstream compatibility update. The first establishes reliable validation tooling; the second moves the compiler target deliberately to current upstream behavior.

Understanding: you approved both recommended updates.

Changes: this replaces the earlier recommendation with two executable child issues, sequenced so the released binary update cannot obscure language-compatibility changes.

Reasoning: v0.0.34 is already behind the current compiler reference pin, while current upstream `main` contains material language and validation changes that require test-first compatibility work.
