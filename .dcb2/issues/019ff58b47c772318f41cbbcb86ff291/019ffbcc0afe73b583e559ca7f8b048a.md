---
format: 1
event-id: 019ffbcc0afe73b583e559ca7f8b048a
entity-id: 019ff58b47c772318f41cbbcb86ff291
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-13T15:44:41.2149787+00:00
author: worker:65eb66d2e522:3283386:019ffbb58c0f782a839bc1e19f4b29b5
previous: 019ff7da378172779c278159a935a462
base-commit: ba387463337a3ea8765e6054be4f9cdd97aa2353
constraints-hash: e646027115bd63ae6634df307340bf0add1f07691802d085d332d9e6fd77a483
revision: 019ff7da378172779c278159a935a462
---
# Breakdown

Read-only audit with C=e677bb5edfaf6ac7f04cbde92f7a3e13609b6f9e, I=darklang/dark@04fbe9dcc995c6188757d583e273cbd30a3e2d3d, and supplemental installed interpreter I2=a70f30de21302caa91fd98a2d15aefb1ec423a4e. Resolved 56fe54e behavior and sibling-owned module/language gaps are excluded. RawPtr, Bytes, generic-key Dict, bitwise/!, numeric tuple projection, record patterns, and compiler-dialect syntax remain compiler extensions under sibling scopes.

## Proposed issue 019ffbcc0afe7b4d89681eb823795c1c

Title: Align runtime comparison failures

Outcome:

Make heterogeneous equality and numeric ordering produce the interpreter's runtime errors after evaluating both operands.

Constraints:

- Cover == and != with EqualityCheckOnIncompatibleTypes and <, >, <=, >= with NumericOperationOnIncompatibleTypes, including compound and generic call sites.
- C passes/1.5_TypeChecking.fs:1850-1962 and 1963-1982 currently force compatible static types; I Builtins.Pure/Libs/NoModule.fs:169-186 and 600-703 dispatch on runtime value types.
- Focused `5.7 != 6L` yields a compiler type mismatch but I testfiles/execution/language/error-syntax.dark:3 requires `Cannot perform equality check on Float and Int64`; I2 confirms the runtime equality error.

## Proposed issue 019ffbcc0afe7574b9dcc244d6d789c8

Title: Align Boolean operator edge semantics

Outcome:

Match the interpreter's eager operand evaluation, determining-value behavior, and Boolean error precedence for && and ||.

Constraints:

- Evaluate both operands left-to-right; after evaluation, false && value returns false and true || value returns true even when the right value is non-Boolean, while other non-Boolean combinations raise the matching Boolean error.
- C passes/1.5_TypeChecking.fs:1986-2035 statically requires Boolean operands and folds selected runtime errors; I ProgramTypesToRuntimeTypes.fs:853-866 and Interpreter.fs:1847-1878 evaluate both sides before applying determining-value rules.
- Pinned cases are testfiles/execution/language/basic/eand.dark:5-12 and eor.dark:6-17; focused probes currently produce compiler errors for `false && 5L` and `true || 5L` while I2 returns false and true.

## Proposed issue 019ffbcc0afe7539bf095872660495a6

Title: Align division and modulo edge behavior

Outcome:

Make supported / and % operations return or fail exactly like the interpreter across numeric types and native targets.

Constraints:

- Add integer divide-by-zero and zero-modulus failures, retain negative-modulus and Euclidean remainder behavior, and lower supported Float modulo instead of rejecting it during LIR conversion.
- C passes/4_MIR_to_LIR.fs:230-269 and 1734-1772 check only negative signed divisors, while :381-382 rejects Float modulo; I Builtins.Pure/Libs/NoModule.fs:350-493 defines the complete contracts.
- Focused C probes return 0 for `1L / 0L`, return 1 for `1L % 0L`, and reject `7.5 % 2.0`; I@04f tests require divide/modulus errors and its runtime implementation returns Float remainder.

## Proposed issue 019ffbcc0afe7cfeb6ae6e4189960257

Title: Propagate eager construction failures

Outcome:

Allow runtime-failing expressions in supported collection and enum construction while preserving first-error source order.

Constraints:

- Cover list literals, list-cons heads/tails, and enum payloads; retain the already-correct tuple, record, record-update, and call-argument ordering as regressions.
- C passes/2_AST_to_ANF.fs:4654-4690 and 4896-4905 force payloads and list elements through toAtom, causing ANF conversion errors; I ProgramTypesToRuntimeTypes.fs:642-658 and 1138-1171 evaluates fields left-to-right before construction.
- Focused C probes for `[Builtin.testRuntimeError("one"), Builtin.testRuntimeError("two")]` and an enum payload fail during ANF conversion; I testfiles/execution/language/collections/dlist.dark:9-10 and custom-data/enums.dark:7-11 surface the first runtime error.

## Proposed issue 019ffbcc0afe7a278619f26d802450ae

Title: Align unmatched-match errors

Outcome:

Report unmatched matches with the interpreter's exact error and complete runtime value representation.

Constraints:

- Remove the compiler-only `Non-exhaustive match:` prefix and render the actual scrutinee recursively, including qualified enum identity, instead of `<unknown>` or reduced static forms.
- C passes/2_AST_to_ANF.fs:7588-7635 synthesizes a string from limited AST forms; I ProgramTypesToRuntimeTypes.fs:1059-1060 emits MatchUnmatched and Interpreter.fs raises it from the runtime value.
- The shared string probe currently prints `Non-exhaustive match: No matching case...`; I testfiles/execution/language/flow-control/ematch.dark:633-679 and I2 require `No matching case found for value ... in match expression`.

## Proposed issue 019ffbcc0afe7936aa1cdfd924b10aaa

Title: Expose structured execution failures

Outcome:

Return typed execution failures from compiler execution boundaries and confine text formatting and process termination to CLI adapters.

Constraints:

- Represent every supported compiler-generated and builtin runtime failure with an exhaustive error value, preserving actual values and types needed by public diagnostics.
- C ANF.fs:232-234 and passes/4_MIR_to_LIR.fs:1221-1222 reduce failures to strings that become native print-and-exit instructions; I RuntimeTypes.fs:1152-1437 defines the error algebra and :1473-1477 exposes ExecutionResult.
- Keep library, eval, and embedding boundaries capable of distinguishing successful values from runtime failures without parsing stderr or process status.

## Proposed issue 019ffbcc0afe7d2faaef1f5e79308e8d

Title: Generate Dark call stacks

Outcome:

Attach interpreter-compatible Dark function and lambda frames to native runtime failures.

Constraints:

- Track source entry, resolved named-function identity, lambda parent and expression identity, and applied-call frames without exposing native addresses or generated symbols.
- C ANF.RuntimeError carries only message:string at ANF.fs:234; I RuntimeTypes.fs:1449-1477 defines ExecutionPoint and CallStack, with frame reconstruction in LibExecution/Execution.fs:65-83.
- Validate nested named calls, lambdas, partial applications, builtin failures, and failures raised while evaluating arguments.

## Proposed issue 019ffbcc0afe77969831b51a025c8fb2

Title: Separate native run and eval output

Outcome:

Make script execution silent on successful final values while retaining interpreter-compatible value rendering for explicit eval.

Constraints:

- Introduce an explicit execution mode or boundary so run returns script status and explicit side effects, while eval renders the resulting value or error.
- C passes/2.6_PrintInsertion.fs:109-130 and CompilerLibrary.fs:1420,1958 unconditionally insert final-value printing; I packages/darklang/cli/execution/run.dark:59-80 does not render a final value, while eval.dark:5-18 and 38-45 does.
- Preserve the recursive renderer delivered by 56fe54e for eval and remove its implicit use from native script execution.
