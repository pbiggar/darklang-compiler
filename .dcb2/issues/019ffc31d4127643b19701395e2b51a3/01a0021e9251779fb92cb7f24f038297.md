---
format: 1
event-id: 01a0021e9251779fb92cb7f24f038297
entity-id: 019ffc31d4127643b19701395e2b51a3
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T21:12:33.1050068+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc347e8f7f29908a9b41c89a1f91
base-commit: bafbdfcddc1cf2a5b666049889f88cfcea6dec55
constraints-hash: d14ac1ab12dddb1f20090a7edc9c511cccd26be479507979158fe1c76153052c
revision: 019ffc347e8f7f29908a9b41c89a1f91
---
# Implementation plan

Read-only inspection at compiler HEAD eb18ec57d287dcc7d51028468b2b1b249f2cbfc0 against darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d. Existing typed structural equality and ordered stdout effects provide the native boundaries; the contained gap is the public Stdlib prelude, portable list traversal, focused coverage, and revision-stamped classification.

## Work item 01a0021e925076f4bd7c92a40ebe9b31

Title: Add failing public equality probes

Category: behavior

Evidence: Current src/Tests/e2e/comparison-parity.e2e:8-107 covers only operators and a local generic helper. Pinned backend/testfiles/execution/stdlib/nomodule.dark:311-363 exercises the missing public functions; the repository copy still uses obsolete _v0 names and is not allowlisted.

Outcome:

Extend the equality parity matrix with unversioned Stdlib.equals and Stdlib.notEquals calls covering every shared scalar type, DateTime, nested lists/tuples, nominal records and sums, Dict mappings, functions, and Blob identity. Include NaN, signed zero, unequal nested leaves, named and lambda identities, left-to-right single evaluation, and AOT rejection of mixed or distinct nominal operand types.

## Work item 01a0021e92507290b324dfe85ccf85f2

Title: Expose polymorphic equality prelude

Category: function

Evidence: No module Stdlib function exists in src/DarkCompiler/stdlib or CompilerLibrary.fs:1092-1162. TypeChecking.fs:1175-1359 and 5317-5844 already classify admissible types, retain generic equality plans, and generate recursive helpers; 2_AST_to_ANF.fs:22-63 materializes concrete plans. Pinned packages/darklang/stdlib/noModule.dark:4-9 defines the public API.

Outcome:

Add a root Stdlib Dark source defining equals<'a>(left, right): Bool over the compiler's typed equality boundary and define notEquals as portable Boolean negation of equals. Load and monomorphize it with the stdlib so compatible concrete instantiations use the existing exhaustive equality plan and generated structural helpers without runtime type-tag dispatch or a second backend comparison path.

## Work item 01a0021e92507d7abeb4b2e9249d2031

Title: Provide portable ordered List.iter

Category: function

Evidence: src/DarkCompiler/stdlib/List.dark:10-51 has isEmpty, length, equals, and map but no iter. Pinned packages/darklang/stdlib/list.dark:559-565 defines recursive left-to-right iteration, and packages/darklang/stdlib/print.dark:12-14 depends on it.

Outcome:

Add Stdlib.List.iter<'a>(list, fn): Unit as a recursive head-then-tail traversal over the existing skew-list abstraction, invoking the callback exactly once per element and stopping at the empty list. Use this public helper for printLines instead of adding native list traversal.

## Work item 01a0021e92507d5a9a7a4bec811c2f66

Title: Expose explicit printing prelude

Category: function

Evidence: CompilerLibrary.fs:1092-1162 loads no root printing source. Stdlib.fs:113-122 already registers Builtin.print and Builtin.printLine, and 2_AST_to_ANF.fs:173-179 lowers them to ordered StdoutWrite effects. Pinned packages/darklang/stdlib/print.dark:4-14 supplies the missing public compositions.

Outcome:

Add a root Stdlib Dark source with print(String): Unit and printLine(String): Unit forwarding to the existing Builtin effects, plus printLines(List<String>): Unit composed with Stdlib.List.iter and Stdlib.printLine. Register the source in stdlib load order after List; retain String-only input so explicit printing emits supplied UTF-8 bytes rather than invoking host result rendering.

## Work item 01a0021e925079c890025d065f9f263a

Title: Add byte-exact public output probes

Category: behavior

Evidence: src/Tests/e2e/interpreter/cli_presentation.e2e:1-6 currently probes only Builtin output. PrintInsertion.fs:36-57 already suppresses final Unit rendering, while pinned Builtins.Cli/Libs/Output.fs:18-50 establishes the newline and raw-output primitives.

Outcome:

Extend the pinned presentation fixture with Stdlib.print, printLine, and printLines cases for empty and Unicode strings, embedded and trailing newlines, empty and multi-element lists, mixed sequential writes, optimized execution, and final Unit suppression. Assert exact stdout bytes, zero stderr, source order, one newline per printLine call, and no implicit Unit text.

## Work item 01a0021e92507a53a5d88b13eb061944

Title: Record the prelude parity boundary

Category: other

Evidence: docs/comparison-parity.md and docs/cli-presentation-parity.md describe the underlying engines but not the public Stdlib wrappers. Pinned NoModule.fs:33-175 includes DUuid, DDB, DBlob, DStream, and function identity; current AST.fs:23-57 instead includes TBlob, TRawPtr, and TRuntimeError, while docs/diff-value-search-parity.md:119 records Uuid as a String alias.

Outcome:

Update the equality, CLI presentation, stdlib, and Darklang-differences documentation with the implementation comparison revision and executable probes. Record shared equality categories; AOT mismatch timing; RawPtr and RuntimeError as rejected compiler-internal categories; the current String-backed Uuid model as a compiler extension rather than distinct DUuid parity; DDB and Stream as interpreter-only unsupported categories; and implicit final-result rendering as separate from String-only explicit output.

## Work item 01a0021e925079d0a3445b82fbaed981

Title: Run complete regression verification

Category: other

Evidence: docs/verification.md:1-28 defines the repository-wide test and benchmark gate; the change adds generic specializations and ordered effects that require whole-pipeline and both-backend regression coverage.

Outcome:

Run ./run-tests --ai and ./benchmarks/run_benchmarks.sh --verify routine after the focused tests pass, resolving all warnings or failures and reporting the routine performance ratio from benchmarks/RESULTS.md.
