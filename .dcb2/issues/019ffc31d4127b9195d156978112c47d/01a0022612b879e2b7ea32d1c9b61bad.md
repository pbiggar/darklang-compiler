---
format: 1
event-id: 01a0022612b879e2b7ea32d1c9b61bad
entity-id: 019ffc31d4127b9195d156978112c47d
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T21:20:44.7287894+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc347e9371b4ac892375576bfb54
base-commit: 331329658e95164fc87ed5f737adf1e405e93f52
constraints-hash: f729ba376fb1c537c611627a8245f2e6c15718906f605f0a3313d5abb51c6c9c
revision: 019ffc347e9371b4ac892375576bfb54
---
# Implementation plan

Single-change scope revalidated at compiler C@bafbdfcddc1cf2a5b666049889f88cfcea6dec55 against current interpreter I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d; refs/heads/main still resolves to that interpreter revision. DCB1@8a402797 was used only as historical inventory. The shared representation, primitive semantics, eleven public modules, conversions, and conformance corpus are coupled and require no child issues.

## Work item 01a0022612b87125a2b362cc32ed7b99

Title: Make the pinned integer corpus executable

Category: other

Evidence: TestRunner.fs:212-215,257-258 includes only int64.dark and int.dark; TestRunner.fs:508 enables only three Int64 lines. All eleven files already exist under src/Tests/e2e/upstream/stdlib/ints; int128.dark:53 contains the unresolved 170141183460469231731687303715884105726Q + 4Q case.

Outcome:

Register all eleven checked-in upstream integer files in the normal E2E corpus, remove integer line gating, and add focused failing E2E cases for ownership through aggregates and closures, full-range/equal-bound randomness, Float conversion edges, every conversion boundary, failure text, and the signed Int128 wrapping-add boundary.

## Work item 01a0022612b877ef975fe1d297c7f9ba

Title: Create the shared integer semantics layer

Category: other

Evidence: Int.dark:15-396 currently contains the reusable arbitrary-width algorithms inside the public Int module; no corresponding shared layer or 128-bit modules exist. I@04fbe9d packages/darklang/stdlib/int.dark through uint128.dark supplies the public wrappers.

Outcome:

Extract an internal target-neutral layer for canonical decimal normalization, signed and magnitude comparison, add/subtract/multiply/divmod, bounds checks, width wrapping, two's-complement conversion, bitwise operations, shifts, parsing classification, formatting, and Float conversion. Keep it internal and have Int, Int128, UInt128, parsing, and checked conversions delegate to it.

## Work item 01a0022612b870dcbb11e9efed1369d0

Title: Correct managed ownership for large integer representations

Category: type

Evidence: ANF.fs:385-387 classifies Int as DynamicString, but ANF.fs:346-351 classifies Int128 and UInt128 as Immediate even though 2_AST_to_ANF.fs:4792-4820 lowers their literals as StringLiteral buffers and :393-398 implements conversion views as no-ops.

Outcome:

Treat Int, Int128, and UInt128 canonical buffers as managed dynamic values in retain/release classification, including parameters, returns, branches, lists, tuples, sums, records, dictionaries, and closure captures. Keep typed representation-view intrinsics ownership-neutral and ensure newly computed values return canonical owned buffers on ARM64 and x64.

## Work item 01a0022612b8780894e3a4b8fecf538b

Title: Align fixed-width primitive lowering

Category: behavior

Evidence: 4_MIR_to_LIR.fs:598-622 lowers every right shift through Lsr, so signed negative shifts cannot match the upstream -1 >> 1 cases. integer-overflow.e2e covers basic wrapping but not the upstream division, remainder, modulus, and signed-shift contracts in backend/testfiles/execution/stdlib/ints.

Outcome:

Make 8/16/32/64-bit add, subtract, multiply, negate, power, divide, modulus, remainder, bitwise operations, and shifts honor declared-width wrapping. Add deterministic zero-divisor handling, signed minimum divided by -1 wrapping, Euclidean modulus validation, truncating signed remainder, arithmetic signed right shift, logical unsigned right shift, and interpreter-compatible shift-count masking.

## Work item 01a0022612b87dedb3f68a41b404875d

Title: Complete arbitrary-width Int parity

Category: function

Evidence: Int.dark:9 has the extra UnsupportedIntParseError; :229-246 implements remainder and only mod_v0; no absoluteValue, sum, max, min, clamp, or random exists. I@04fbe9d packages/darklang/stdlib/int.dark:11-210 declares the target surface; local int.dark:72-140 covers key Result, modulus, shift, Float, and rendering cases.

Outcome:

Reduce ParseError to BadFormat; expose canonical mod, absoluteValue, sum, max, min, clamp, and random; preserve remainder as Result with Error("Cannot divide by 0"); and align power, division, shifts, parsing, formatting, Float conversion, square root, bitwise behavior, and all fixed-width conversions with interpreter values and exact runtime failures. Retain compare and equals only as documented compiler extensions.

## Work item 01a0022612b874af84b37888efc58f64

Title: Complete signed fixed-width modules

Category: function

Evidence: The four compiler modules currently stop at arithmetic/comparison/bitwise/formatting/parity helpers and sum; Int8 and Int32 still expose only sub/mul/div. I@04fbe9d packages/darklang/stdlib/int8.dark through int64.dark declares the missing surface; local upstream files contain parse, random, overflow, divide, remainder, and shift probes.

Outcome:

For Int8, Int16, Int32, and Int64, add ParseError.BadFormat and OutOfRange, canonical arithmetic names, signed remainder, random, sqrt, toFloat, parse, and the declared Int64.fromFloat. Copy the portable power, aggregation, min/max, and clamp wrappers so negative-exponent handling, wrapping powers, signed-minimum absoluteValue/negate, and Result/runtime-error shapes match the pinned interpreter.

## Work item 01a0022612b8762c94441bee201316a9

Title: Complete unsigned fixed-width modules

Category: function

Evidence: The four compiler UInt modules lack parse types, parsing, random, sqrt, Float conversion, and conversions; UInt8, UInt32, and UInt64 use short arithmetic names. I@04fbe9d packages/darklang/stdlib/uint8.dark through uint64.dark defines the surface, including uint64.dark:69-71 for fromFloat.

Outcome:

For UInt8, UInt16, UInt32, and UInt64, add ParseError.BadFormat and OutOfRange, canonical subtract/multiply/divide names, random, sqrt, toFloat, parse, and UInt64.fromFloat with the interpreter's Float.round-to-Int then checked UInt64 conversion. Copy the wrapping power, aggregation, min/max, and clamp wrappers; do not add unsigned remainder or other undeclared parity functions.

## Work item 01a0022612b8738a9587f602c476c3be

Title: Implement the checked cross-width conversion matrix

Category: function

Evidence: Int.dark:418-507 implements much of the Int matrix, but the eight fixed modules expose none of their declared cross-width conversions and the 128-bit modules are absent. The pinned interpreter package files enumerate the matrix; local int8.dark:192-282 and int32.dark:196-278 include representative boundaries.

Outcome:

Expose every interpreter-declared conversion with the same return shape: direct values only for provably range-preserving widening and Option for potentially lossy signed/unsigned or narrowing conversions. Cover all 8/16/32/64 targets from every other fixed width and both 128-bit types, Int conversions in both directions, Int128 widening from all 8-64 types, and UInt128 widening from unsigned 8-64 types. Return None exactly outside inclusive target bounds.

## Work item 01a0022612b87a26aeda597247eb7a48

Title: Provide bound-correct integer randomness

Category: behavior

Evidence: Stdlib.fs:93-100 and ANF/Runtime currently provide only unbounded Random.int64. I@04fbe9d Builtins.Random/Libs/Random.fs:14-221 declares inclusive, order-independent Int and 8-64 behavior; checked-in upstream tests exercise ordinary and reversed bounds.

Outcome:

Build an internal random-word primitive over the existing native entropy path and target-neutral bounded sampling for Int and signed/unsigned 8-64 modules. Normalize reversed endpoints, include both endpoints, return the sole endpoint for equal bounds, avoid range arithmetic overflow at full-width limits, and keep Int128/UInt128 without public random functions.

## Work item 01a0022612b87e889790ac1ec6a38757

Title: Add the operational Int128 module

Category: type

Evidence: AST.fs and 1_Parser.fs already recognize Int128 and Q literals, but CompilerLibrary.fs:1093-1103 loads no Int128 module. I@04fbe9d packages/darklang/stdlib/int128.dark:4-186 declares the API; local int128.dark:1-221 records values and failures, including line 53's required wrap to -170141183460469231731687303715884105726Q.

Outcome:

Add ParseError.BadFormat and OutOfRange plus mod, remainder, add, subtract, multiply, divide, absoluteValue, negate, four comparisons, sqrt, toFloat, sum, max, min, clamp, toString, parse, declared widening conversions, bitwise operations, and shifts. Normalize every result to signed 128-bit two's-complement range, including minimum-value negate/absoluteValue/divide behavior and the canonical wrapping addition result.

## Work item 01a0022612b87bccba9f588eeb6a7a7e

Title: Add the operational UInt128 module

Category: type

Evidence: UInt128 literals and types exist in AST.fs and 1_Parser.fs, but CompilerLibrary.fs:1093-1103 loads no UInt128 module. I@04fbe9d packages/darklang/stdlib/uint128.dark:4-153 and local uint128.dark:1-144 define the observable contract, including maximum-plus-one wrapping to zero.

Outcome:

Add ParseError.BadFormat and OutOfRange plus mod, add, subtract, multiply, divide, four comparisons, sqrt, toFloat, sum, max, min, clamp, toString, parse, unsigned widening conversions, bitwise operations, and shifts. Normalize arithmetic and bitwise results modulo 2^128 and preserve exact unsigned formatting and bounds.

## Work item 01a0022612b87a91a8b68ed7f4e6fa39

Title: Integrate 128-bit operations with typing, lowering, and rendering

Category: behavior

Evidence: 2_AST_to_ANF.fs:5076-5084 special-cases only equality as string equality while other 128-bit operators still reach primitive lowering; 1.6_ValueRendering.fs:244-245 returns raw values instead of module formatting; TypeChecking.fs:1377-1387 already routes ordering through Int views.

Outcome:

Load both 128-bit modules, route typed 128-bit arithmetic, unary operations, comparisons, bitwise calls, and shifts through the shared semantics instead of machine-word primitives, and render values through each module's toString. Preserve compile-time rejection of wrong operand and conversion types rather than adding interpreter-style runtime dispatch.

## Work item 01a0022612b87b2dabb016f2492ba4cc

Title: Classify the parity surface and retained extensions

Category: other

Evidence: docs/features/arbitrary-precision-integers.md documents only Int; docs/darklang-differences.md:241-251 currently misclassifies integer bitwise and helper surfaces and has no 128-bit contract. Current module files contain the retained aliases and helpers.

Outcome:

Add a revision-pinned integer parity document and update arbitrary-precision and Darklang-differences documentation. Mark existing sub/mul/div aliases, Int compare/equals, popcount, isEven/isOdd, unsigned absoluteValue/negate, integer operator spellings, and internal representation helpers as compiler extensions without adding them to the parity API; record static type enforcement as the intentional AOT divergence.

## Work item 01a0022612b874aea1fb05467148effe

Title: Verify both behavior and routine performance

Category: other

Evidence: docs/verification.md requires ./run-tests --ai and ./benchmarks/run_benchmarks.sh --verify routine; AGENTS.md requires reporting the RESULTS.md performance ratio.

Outcome:

Run the complete AI test suite and routine benchmark verification, require every newly enabled upstream integer case and focused ownership/failure probe to pass on the available native target, and report the routine Performance ratio from benchmarks/RESULTS.md with any cross-target coverage limitation.
