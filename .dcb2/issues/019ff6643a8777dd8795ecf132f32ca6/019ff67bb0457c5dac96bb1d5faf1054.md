---
format: 1
event-id: 019ff67bb0457c5dac96bb1d5faf1054
entity-id: 019ff6643a8777dd8795ecf132f32ca6
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-12T14:58:49.0290321+00:00
author: worker:65eb66d2e522:40713:019ff640f1e77ef78c5242339394e833
previous: 019ff6643a89728182d62e389e0856a5
base-commit: 44e1b0addf38753585a5a2b66ef7bc893985f3c4
constraints-hash: 75eecb56c4fd9e5e9a29a50f4be0990f153e6b1e8dc0fc09628c720337cf9102
revision: 019ff6643a89728182d62e389e0856a5
---
# Work plan

`getBlockUses` feeds SSA liveness precomputation and currently creates temporary singleton sets for register operands, then performs dozens of unions. `-vv` exposes SSA Construction and total-compilation timings; post-SSA MIR requires a temporary capture because the standard MIR dump occurs after MIR optimization.

## Work item 019ff67bb0457b06b2464ff1e88e8376

Title: Fold operand uses into the block accumulator

Category: function

Evidence: `src/DarkCompiler/passes/3.1_SSA_Construction.fs` defines `getOperandUses` as `Set.singleton`/`Set.empty`; `getBlockUses` invokes it about 69 times and contains about 72 `Set.union` operations before `computeLiveness` calls it once per CFG block. `src/Tests/compiler-passes/SSAConstructionTests.fs` currently has no direct block-use-set coverage. `CompilerLibrary.fs` reports SSA and compilation timings at `-vv`, but dumps MIR only after MIR optimization. `benchmarks/profiles/routine.txt` lists 19 programs, verification compares their deterministic instruction counts, and the inspected `benchmarks/RESULTS.md` ratio is 2.74x.

Outcome:

After explicit individual human approval, predeclare materiality and noise thresholds, then temporarily measure allocation bytes and elapsed time attributable to `getBlockUses`; stop if allocations are not material. Add a focused exact-set characterization covering every operand-bearing MIR instruction and terminator, with a temporary allocation/performance check serving as the failing optimization test. Remove profiling, build matched baseline and candidate compilers, and retain only a local accumulator helper inside `getBlockUses` that pattern-matches `Register vreg` and calls `Set.add vreg uses`; route instruction arguments and terminators through that fold while preserving direct VReg fields and all resulting block-use sets. Collect interleaved, warmed, same-host measurements: 10 raw fasta `-vv` SSA Construction and total-compilation samples per version; 10 samples per version for every routine program, reported as before/after medians and absolute/percentage deltas; and 10 raw external wall-clock runs of unfiltered `./run-tests --ai` per version with medians and deltas. Reject and remove the candidate if the selected-pass result is neutral, noisy, or regressive under the predeclared rule. Before final verification, remove every temporary timer, allocation probe, and profiling test; compare formatted post-SSA MIR and SHA-256/byte content of generated binaries against baseline, run `./run-tests --ai` and `./benchmarks/run_benchmarks.sh --verify routine`, and report all routine instruction counts plus the current `RESULTS.md` performance ratio.
