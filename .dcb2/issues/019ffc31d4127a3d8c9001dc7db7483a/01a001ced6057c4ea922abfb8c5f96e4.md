---
format: 1
event-id: 01a001ced6057c4ea922abfb8c5f96e4
entity-id: 019ffc31d4127a3d8c9001dc7db7483a
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T19:45:27.5578179+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc347e9574fda49cb11b3ce1647c
base-commit: bcd7d7721916a1c9317a15d15d4f3ed3b9ab9367
constraints-hash: 4a2239310d1126977e265ec50d792844bf46f4505722560925bd7252f3a16859
revision: 019ffc347e9574fda49cb11b3ce1647c
---
# Implementation plan

One contained change. Parity comparisons use compiler C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3 and darklang/dark I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d; integration locations were inspected at compiler main@bcd7d7721916a1c9317a15d15d4f3ed3b9ab9367. DCB1@8a402797 is inventory only. Interpreter values and errors remain the baseline; synchronous callback-failure cleanup, reference-counted abandonment cleanup, and deterministic rejection of overlapping pulls are documented compiler lifecycle extensions, while invalid types remain AOT errors.

## Work item 01a001ced605729e8f0a62506b46b92e

Title: Create the Stream parity ledger

Category: other

Evidence: I@04fbe9d: packages/darklang/stdlib/stream.dark; backend/src/Builtins/Builtins.Pure/Libs/Stream.fs; backend/src/LibExecution/{RuntimeTypes,Stream}.fs; backend/tests/Tests/Stream.Tests.fs. DCB1@8a402797 only reports the broad gap in docs/interpreter-parity.md.

Outcome:

Add a Stream contract table mapping every public operation and lifecycle transition to pinned source and test anchors. Record identity equality, opaque rendering, non-persistability, AOT error phase, deterministic cleanup extensions, and omission of the interpreter's chunked byte fast path as behavior-neutral.

## Work item 01a001ced6057913a7b61e46b48b4ec7

Title: Add opaque Stream<'a> typing

Category: type

Evidence: C@51093e0: AST.fs has TList/TDict/TRawPtr but no Stream. Current anchors include AST.fs:22-59, passes/1_Parser.fs, passes/1_InterpreterParser.fs, passes/1.5_TypeChecking.fs, ASTPrettyPrinter.fs, passes/1.6_ValueRendering.fs, ANF.fs, and passes/2_AST_to_ANF.fs. I@04fbe9d NoModule.fs compares DStream handles by identity.

Outcome:

Introduce TStream of Type and propagate it through both parsers, type formatting, alias resolution, substitution, unification, generic specialization, mangling, inference, rendering, and exhaustiveness-sensitive traversals. Permit same-handle equality, reject ordering, expose no constructors or patterns, and render a stream without forcing it.

## Work item 01a001ced6057ae7918753f6c1643dd7

Title: Define the native stream handle ABI

Category: type

Evidence: I@04fbe9d RuntimeTypes.fs defines DStream and mutable StreamImpl nodes but Stream.fs notes that concurrent single-consumer enforcement is latent. Current ANF.fs:120-204 models ownership through RcKind, RcShape, and RcReleasePlan but has no resource-bearing shape.

Outcome:

Model an owned opaque handle containing explicit lifecycle state, erased producer state, step closure, disposer, release metadata, and refcount. Use typed state cases for ready, advancing, close-requested, exhausted, failed, and closed states, with a documented transition table that prevents double disposal and overlapping advancement.

## Work item 01a001ced605746c82cd9e632fdd97e9

Title: Lower native stream primitives through every IR

Category: function

Evidence: Current Stdlib.fs:202-238 registers intrinsic modules, while ANF.fs:229-294 and passes/2_AST_to_ANF.fs:300-470 show the intrinsic-to-IR pattern. MIR.fs, LIR.fs, passes/3_ANF_to_MIR.fs, passes/4_MIR_to_LIR.fs, and arm64/x64 6_CodeGen.fs have no Stream operations.

Outcome:

Register internal typed construction, next, and close primitives. Construction carries the concrete hidden state type plus step and disposer closures; next atomically performs the state transition and returns Option<'a>; close requests or performs closure idempotently. Add corresponding ANF, MIR, and LIR forms and update inference, optimization, inlining, dead-code analysis, register allocation, printers, and both native backends.

## Work item 01a001ced605778a9f2ea3638a87a5b3

Title: Propagate callback failures through cleanup

Category: behavior

Evidence: Current ANF.RuntimeError and RuntimeErrorString lower directly to process termination in both backends. I@04fbe9d Builtins Stream callbacks propagate executeApplicable errors, but LibExecution.Stream.readNext has no synchronous disposer path when pullImpl fails.

Outcome:

Make producer and transformation callback invocation error-aware: preserve the existing runtime error payload, transition the active handle to failed, invoke its disposer exactly once, release hidden producer state, and then emit the existing language-visible failure. Carry nested active-pull cleanup context without process-global mutable state, and give overlapping or reentrant pulls one stable runtime error.

## Work item 01a001ced605740f884d24248da85c8d

Title: Implement the portable Stream module

Category: function

Evidence: I@04fbe9d packages/darklang/stdlib/stream.dark declares fromList, unfold, next, toList, toBlob, close, map, filter, take, and concat. Current CompilerLibrary.fs:1043-1103 has no Stream.dark entry, and Stdlib.fs has no Stream module.

Outcome:

Add stdlib/Stream.dark and load it with the stdlib. Implement fromList and unfold over the native constructor; implement map, filter, take, and concat as lazy producer-state transformations whose disposers close their remaining sources; implement toList by tail-recursive draining and toBlob through the existing Blob construction surface; keep only next, close, state advancement, and disposal native.

## Work item 01a001ced6057b019929c543ccc602e7

Title: Integrate deterministic ownership release

Category: behavior

Evidence: Current passes/2.5_RefCountInsertion.fs:535-585 derives cleanup from RcShape, while both code generators provide specialized list, Dict, closure, and dynamic-buffer release helpers but no resource finalizer. docs/features/reference-counting.md lists all current managed shapes and omits Stream.

Outcome:

Add a Stream-specific managed root and release plan. Retain handles across aliases and containers; on the last release invoke the same idempotent close path before freeing the shell; release captured producer state, callbacks, and nested streams exactly once after exhaustion, explicit close, early termination, failure, or abandonment. Support leak accounting and reusable allocation on ARM64 and x64.

## Work item 01a001ced6057bf49430b9d8eab3edd0

Title: Enable public parity coverage

Category: behavior

Evidence: Current src/Tests/e2e/upstream/stdlib/stream.dark:1-153 covers materialized results but omits unfold and callback failures. TestRunner.fs:230-296 does not register that file in upstream or default execution sets. I@04fbe9d backend/testfiles/execution/stdlib/stream.dark supplies the same-source value corpus.

Outcome:

Enable the existing upstream Stream corpus and extend it with unfold, infinite-source take, exact pull boundaries, laziness, repeated draining, exhaustion stability, idempotent close, callback error text, Stream<Blob>, UInt8-to-Blob, identity equality, composition, and AOT type failures. Use failing probes that demonstrate the absent type and module as the initial test state.

## Work item 01a001ced6057e7ea4f986eef5a8fac2

Title: Test lifecycle transitions directly

Category: behavior

Evidence: I@04fbe9d backend/tests/Tests/Stream.Tests.fs tests exhaustion, transforms, GC finalization, explicit close, and chunk draining, but not synchronous callback-failure disposal. The compiler's public API has no resource constructor, so src/Tests/compiler-passes and native leak-check coverage are needed to observe internal disposal.

Outcome:

Add focused internal tests using an instrumented synthetic resource to assert state and disposer counts for natural exhaustion, empty streams, explicit and repeated close, partial-drain abandonment, callback failure, early take, concat handoff, nested transformations, close during advancement, and overlapping pulls. Add RC/leak and backend tests for handles captured in lists, tuples, records, sums, Dicts, and closures.

## Work item 01a001ced6057066984fca18a9f4daeb

Title: Document the delivered runtime contract

Category: other

Evidence: Current docs/current-language-features.md:5-18, docs/features/{stdlib,type-checking,reference-counting}.md, and docs/darklang-differences.md contain no Stream contract. docs/verification.md defines the full test and routine benchmark checks.

Outcome:

Update the language, stdlib, type-checking, reference-counting, and Dark-differences documentation with Stream syntax, API signatures, handle identity, lifecycle transitions, native ownership, error behavior, and classified divergences. Validate the complete suite and routine benchmark profile and record the resulting performance ratio.
