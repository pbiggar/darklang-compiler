---
format: 1
event-id: 019ffe1a6c437cadac65877cff6de00f
entity-id: 019ffe1a6c417b3982d80aa2b8db8604
entity-kind: issue
event-type: created
occurred-at: 2026-08-14T02:29:32.3558061+00:00
author: worker:65eb66d2e522:3848367:019ffe00c85f7c518c540b6db23ed6d0
batch: 019ffe1a6b8c7622a13eaafcfe62daf3
problem: 019ff54dc66279a38e5ae60d59e804a3
workflow: trial-first
---
# Fuse ARM64 pool and layout discovery

Reduce ARM64 Emit time by replacing two symbolic-instruction scans with one.

## Constraints

- Limit changes to ARM64 pool/layout discovery; leave direct-byte emission, binary layout, and compatibility encoders unchanged.
- Collect 10 paired alternating before/after -vv runs for every routine program, reporting raw ARM64 Emit and total timings, medians, and deltas.
- Require identical pools, label offsets, machine-code words, binaries, outputs, and instruction counts; run the full test suite and routine benchmark verification and report the performance ratio.
- Retain only a non-noisy total improvement and remove all exploratory profiling.

## Trial work

### Trial item 019ffe1a6c4173429adcdccc817c077a

Title: Measure discovery traversals

Category: ARM64 emission profiling

Evidence: ARM64 Emit totals 159.2 ms in the current sweep; emitBinary calls collectPools before encodeSymbolicWithPools separately calls computeSymbolicLayout over the same instruction list.

Outcome:

Attribute ARM64 Emit time and allocations to pool collection and symbolic layout calculation.

### Trial item 019ffe1a6c417282bd60cb49db2d1322

Title: Return pools and layout together

Category: ARM64 symbolic resolution

Evidence: Both traversals are read-only classifications of each symbolic instruction and their outputs are independent, making a fused summary exact and distinct from the recorded direct-byte trial.

Outcome:

Collect string and float literals, code size, and code-label positions in one order-preserving fold.
