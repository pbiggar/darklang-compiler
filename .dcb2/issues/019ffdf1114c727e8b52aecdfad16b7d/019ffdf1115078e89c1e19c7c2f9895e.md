---
format: 1
event-id: 019ffdf1115078e89c1e19c7c2f9895e
entity-id: 019ffdf1114c727e8b52aecdfad16b7d
entity-kind: issue
event-type: created
occurred-at: 2026-08-14T01:44:22.0965163+00:00
author: worker:65eb66d2e522:3725750:019ffd825d8d7ddfac574b9f8c608e82
batch: 019ffdf110d97e9e9b11d36e910d076e
problem: 019ffcba981b77209b745b1ac3cd7d26
workflow: trial-first
---
# Remove nqueen recursive caller saves

Avoid preserving caller-saved values that are dead after the recursive nqueenSolve call.

## Constraints

- First reconfirm the current SaveRegs/RestoreRegs operands and post-call liveness.
- Do not retry the rejected smaller stack-reservation experiment; executed saves and restores must disappear.
- Preserve argument parallel-move correctness and recursive call ABI behavior.
- Run focused register-allocation tests, the full test suite, and routine benchmarks; retain only a material nqueen gain without regressions.

## Trial work

### Trial item 019ffdf1114c7529967c0dc85b72e911

Title: Audit recursive-call liveness

Category: register-allocation analysis

Evidence: The current investigation shows X1/X2 stores and loads around each non-base recursive call, while the continuation only consumes the result and accumulator.

Outcome:

Prove whether X1 and X2 values saved around the hot call are dead in its continuation.

### Trial item 019ffdf1114c7449bc735bbd0cf950ba

Title: Trial call-edge lifetime shortening

Category: call lowering

Evidence: Shrinking the reserved stack changed no dynamic metrics; removing actual memory operations targets nqueen's excess data references and 2.11x instruction ratio.

Outcome:

End dead argument live ranges before call lowering so unnecessary save/restore pairs are not emitted.
