---
format: 1
event-id: 01a0241ca361750f99a60089c593abc2
entity-id: 019ffc309edd7d63a48ddb89bfcd8e90
entity-kind: issue
event-type: assistant-message
occurred-at: 2026-08-21T11:37:31.7453537+00:00
author: worker:65eb66d2e522:317139:01a02410871c770fa34dac650f2e7364
previous: 019ffc3515e67d6ba9525ad65517f635
reply-to: 01a0241b39667b8b9cef0033af3657aa
response-kind: investigation
---
The concrete residual exposure candidates are now clear.

- `Stdlib.File` publicly registers seven legacy operations in `src/DarkCompiler/Stdlib.fs:43-60`; they lower directly in `src/DarkCompiler/passes/2_AST_to_ANF.fs:152-164` and have dedicated compiler-only e2e coverage in `src/Tests/e2e/file_io.e2e`. `docs/cli-parity.md:63-74` says this legacy surface is outside the canonical CLI comparison.
- `Stdlib.Path.tempDir` remains a legacy public intrinsic (`Stdlib.fs:65-73`, lowering at `2_AST_to_ANF.fs:349`), alongside the canonical `Stdlib.Cli.Path` implementation.
- `Stdlib.Random.int64` is publicly registered (`Stdlib.fs:96-103`, lowering at `2_AST_to_ANF.fs:505`) and tested as a compiler extension (`src/Tests/e2e/stdlib/random.e2e`). It is also an implementation dependency of public `List`, `String`, and `Uuid` Dark modules, so removing its public identity requires an internal replacement rather than deleting the native capability.
- The registry also directly includes raw `__*` helpers (`Stdlib.fs` `rawMemoryIntrinsics` and `buildModuleRegistry`); their actual source reachability must be tested before classifying them as public extensions.

This narrows the aggregate inventory’s first actionable audit to exposure classification: canonical interpreter contracts, intentional AOT/native implementation helpers, and accidental legacy public names. No new behavioral mismatch has been demonstrated for `Builtin.unwrap`; its existing registration, type-checking, lowering, and basic e2e coverage remain intact.
