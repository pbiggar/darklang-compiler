---
format: 1
event-id: 01a001e3eb3c7d70a98f5e72e37d2628
entity-id: 019ffc309edd71d283c728ef8bc0d52b
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T20:08:29.2447795+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc3515e3767590cea9396942f058
base-commit: af047e7e0a3b29cdb004954627db7017099f7b61
constraints-hash: 40625716d77059f8b70caeed1a3e71d8904b5926f8122f3959e5b8af49ac48c4
revision: 019ffc3515e3767590cea9396942f058
---
# Implementation plan

One coordinated language-and-library implementation boundary. Compiler evidence is anchored at b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 and interpreter evidence at darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d; DCB1 8a402797 is historical evidence only.

## Work item 01a001e3eb3c74d5869a5285629197d3

Title: Create the revision-stamped list parity record

Category: other

Evidence: docs/interpreter-parity.md:336-362 at 8a402797 contains only the earlier high-level inventory; docs/comparison-parity.md demonstrates the repository's revision-stamped comparison format.

Outcome:

Add a list-specific parity matrix recording syntax, types, lowering, representation, equality, callbacks, public APIs, failures, focused probes, retained native equivalence, removed extensions, and intentional AOT failure-phase differences for the two evidence revisions.

## Work item 01a001e3eb3c7395951cb03aeeb54d40

Title: Align list literal grammar

Category: behavior

Evidence: Compiler passes/1_Parser.fs:2040-2072 accepts comma-only literals and passes/1_InterpreterParser.fs:2485-2522 has a different grammar; interpreter LibParser/Parser.fs:2199-2243 accepts comma, semicolon, and newline separators.

Outcome:

Make both parser modes accept empty and populated list literals with the interpreter's comma, semicolon, and newline separators, including trailing layout behavior, while producing one canonical ListLiteral AST form.

## Work item 01a001e3eb3c7ce0a87dd932604f4037

Title: Implement canonical cons-pattern syntax

Category: behavior

Evidence: Compiler passes/1_InterpreterParser.fs:1342-1368 already partially normalizes right-associated `::`, while passes/1_Parser.fs:1282-1313 has no `::`; interpreter LibParser/Parser.fs:998-1004 defines right-associated TCons patterns.

Outcome:

Parse `head :: tail` patterns in both parser modes with right associativity, normalize chained cons patterns without changing binding order, and retain PListCons solely as the internal representation of canonical cons patterns.

## Work item 01a001e3eb3c7f50929110523de58b95

Title: Implement list append syntax

Category: behavior

Evidence: Compiler AST.fs:55-80 and both parsers lack list append; interpreter LibParser/Parser.fs:1463 and 1515-1523 assigns right-associative precedence and desugars `@` to Stdlib.List.append.

Outcome:

Add right-associative `@` expression parsing at interpreter precedence and lower it to the canonical List.append call with homogeneous list typing and left-to-right operand evaluation.

## Work item 01a001e3eb3c7d508171f982b762d859

Title: Remove list spread syntax and representation

Category: behavior

Evidence: Compiler AST.fs:128,170 and both parsers encode `[head, ...tail]` and `[...tail]`; spread use is present across stdlib/List.dark, Base64.dark, Bytes.dark, Crypto.dark, Float.dark, Option.dark, Result.dark, String.dark, __HAMT.dark, benchmarks, and src/Tests/e2e/lists.e2e. Interpreter LibParser/Parser.fs:1114-1147 and 2199-2243 has no list spread form.

Outcome:

Delete expression ListCons and all `...` list construction or pattern grammar, reject former spread forms, and migrate compiler-owned stdlib, benchmark, and test sources to `::`, `@`, List.push/List.append, or explicitly private skew-list operations as appropriate.

## Work item 01a001e3eb3c7336b16f1337dedb9647

Title: Align list inference and homogeneous typing

Category: type

Evidence: Compiler passes/1.5_TypeChecking.fs:4255-4322 handles empty, populated, and ListCons typing; interpreter LibExecution/TypeChecker.fs:714-733 merges element ValueTypes and reports the first mismatched index, exercised by backend/testfiles/execution/stdlib/list.dark:21-24.

Outcome:

Preserve contextual polymorphic inference for `[]`, infer one element type for nonempty literals and cons patterns, reject heterogeneous construction and non-list cons tails statically, and document static rejection as the intentional AOT phase difference from interpreter runtime ValueType merging.

## Work item 01a001e3eb3c7f64ae52c5868db20e9b

Title: Preserve native construction with interpreter evaluation order

Category: behavior

Evidence: Compiler passes/2_AST_to_ANF.fs:3736 and 8780-8834 builds native skew lists but the compiler-only ListCons path evaluates the tail before right-to-left heads; interpreter ProgramTypesToRuntimeTypes.fs:642-658 lowers items in source order, and execution/language/collections/dlist.dark:1-10 observes the first failing element.

Outcome:

Retain the skew-list representation and native literal builder while ensuring every literal element and append operand is evaluated exactly once from left to right before structural assembly, including failure propagation and reference ownership.

## Work item 01a001e3eb3c72f088a39c857a7e2bc2

Title: Align exact, nested, and cons matching

Category: behavior

Evidence: Compiler passes/2_AST_to_ANF.fs contains separate list-pattern paths around 4981-7666 and src/Tests/e2e/lists.e2e exercises skew-node and tuple cases; interpreter LibExecution/Interpreter.fs:515-525 defines exact-list and nonempty-cons behavior.

Outcome:

Make every PList and PListCons lowering path implement exact-length matching, nonempty cons matching, nested element patterns, ordered bindings, and canonical tail values across all supported element shapes and both backends.

## Work item 01a001e3eb3c7c28b225ba82aa12f21d

Title: Align unmatched-list failures

Category: behavior

Evidence: Interpreter LibExecution/Interpreter.fs:1961-1963 raises MatchUnmatched and packages/darklang/prettyPrinter/runtimeError.dark:251-256 defines its text; compiler list matching is emitted through multiple ANF decision paths and RuntimeError nodes.

Outcome:

Route failed exhaustive list matches through the standard nonexhaustive-match failure with interpreter-equivalent visible text and value rendering, without introducing list-specific fallback cases.

## Work item 01a001e3eb3c74eea321116ec68e681f

Title: Integrate structural list equality privately

Category: behavior

Evidence: Compiler passes/1.5_TypeChecking.fs:1076,4565 synthesizes calls to public Stdlib.List.equals, implemented at stdlib/List.dark:16-33; interpreter Builtins.Pure/Libs/NoModule.fs:33-175 defines recursive DList equality and supported runtime categories.

Outcome:

Generate typed structural equality internally for lists, comparing length and elements recursively in order for every compiler-representable element category, while removing the public List.equals dependency and classifying interpreter-only value categories outside the compiler type universe.

## Work item 01a001e3eb3c7131949e46d91e53d056

Title: Expose List.empty as a polymorphic value

Category: type

Evidence: Interpreter packages/darklang/stdlib/list.dark:5 exposes polymorphic List.empty; compiler Stdlib.fs models callable module members and stdlib/List.dark has no public empty value, while only private Stdlib.__SkewList.empty() exists.

Outcome:

Model typed stdlib values explicitly in lookup, typechecking, and lowering so List.empty and Stdlib.List.empty resolve as a non-callable `List<a>` value backed by native empty-list construction.

## Work item 01a001e3eb3c7c52860473247a4aea36

Title: Normalize public integer signatures

Category: type

Evidence: Compiler stdlib/List.dark:13,132,195,213,227,261,269,277,309 currently exposes Int64 or Int32; interpreter packages/darklang/stdlib/list.dark:74-117,200-204,211-281,334-368,393-398,511-512,592-600 uses Int.

Outcome:

Change List.length, getAt, findFirstIndex, indexedMap indices, take/drop counts, range bounds, repeat counts, chunk sizes, and comparator results to public `Int`, using exact native conversions and bounds checks so negative, out-of-range, and larger-than-native indices produce canonical results without overflow defaults.

## Work item 01a001e3eb3c76f6986309026d844169

Title: Correct range and repetition contracts

Category: function

Evidence: Compiler stdlib/List.dark:260-274 implements end-exclusive Int64 range and value-first repeat returning List; interpreter packages/darklang/stdlib/list.dark:87-117 defines repeatUnsafe, Result-returning repeat, and inclusive range, with tests in execution/stdlib/list.dark:239-250.

Outcome:

Make List.range inclusive at both ends with descending bounds returning empty; add List.repeatUnsafe(times, value); and change List.repeat(times, value) to return Result<List<a>, String> with the interpreter's negative-count error and nonnegative behavior.

## Work item 01a001e3eb3c7babb1bbc6067165cb19

Title: Revalidate retained List operations

Category: function

Evidence: Compiler stdlib/List.dark:8-380 implements these operations with several existing native helpers; their canonical definitions and edge behavior are in interpreter packages/darklang/stdlib/list.dark:8-606 and backend/testfiles/execution/stdlib/list.dark.

Outcome:

Keep native implementations where equivalent and align empty, singleton, head, tail, append, push, pushBack, last, reverse, findFirst, member, length, fold, flatten, interpose, interleave, isEmpty, filter, filterMap, drop, dropWhile, dropLast, take, takeWhile, map, indexedMap, map2shortest, map2, zipShortest, unzip, getAt, and splitLast with the interpreter's results and edge cases.

## Work item 01a001e3eb3c740ca05ea0627491868c

Title: Match callback order and short-circuiting

Category: behavior

Evidence: Interpreter packages/darklang/stdlib/list.dark defines the recursive order for these functions and execution/stdlib/list.dark probes short-circuit and callback failures; compiler stdlib/List.dark uses independently written recursion and partition currently calls its predicate twice at lines 294-302.

Outcome:

Ensure callbacks are invoked once in the interpreter-observable order, stop at the same short-circuit point, and propagate the first callback failure for fold, map, indexedMap, map2 variants, filter/filterMap, find functions, all/any, dropWhile/takeWhile, sortBy, uniqueBy, groupByWithKey, partition, and iter; retain compile-time callback-signature rejection as an AOT phase divergence.

## Work item 01a001e3eb3c7482aaa08cde64bd0394

Title: Correct partition callback semantics

Category: behavior

Evidence: Compiler stdlib/List.dark:289-302 evaluates fn(h) twice and walks head-to-tail; interpreter packages/darklang/stdlib/list.dark:547-556 recursively partitions the tail before evaluating the head predicate.

Outcome:

Rewrite List.partition so each element is classified once, output order is preserved, and predicate calls occur from tail to head as in the interpreter implementation.

## Work item 01a001e3eb3c77b9bf45ed690d3bc23a

Title: Add sorting and uniqueness APIs

Category: function

Evidence: Compiler has only an Int32 insertion-sort sortByComparator at stdlib/List.dark:132-157; interpreter Builtins.Pure/Libs/List.fs:304-348 and packages/darklang/stdlib/list.dark:164-281 define uniqueness, sorting, merge-sort comparison order, and validation text.

Outcome:

Expose List.sort, sortBy, unique, and uniqueBy and align sortByComparator with the interpreter's stable merge-sort behavior, Int comparator contract, exact `-1/0/1` validation error, callback sequence, output ordering, and supported comparable element categories.

## Work item 01a001e3eb3c7954bcce80060d09fb48

Title: Correct pairwise and zip contracts

Category: function

Evidence: Compiler stdlib/List.dark:354-368 has map2shortest, map2, and zipShortest, while an incorrectly placed truncating List.zip returning a bare list exists in stdlib/Float.dark:254-258; interpreter packages/darklang/stdlib/list.dark:402-491 distinguishes all four contracts.

Outcome:

Keep map2shortest and zipShortest truncating to the shorter input, keep map2 returning None for unequal lengths, and expose canonical List.zip returning Option<List<(a,b)>> with None for unequal lengths.

## Work item 01a001e3eb3c7b7db283667a63c3d2a0

Title: Add grouping and iteration

Category: function

Evidence: Neither function exists in compiler stdlib/List.dark; interpreter packages/darklang/stdlib/list.dark:527-565 defines both contracts and execution/stdlib/list.dark includes grouping and side-effect-count probes.

Outcome:

Add List.groupByWithKey returning ordered key/list pairs with source-ordered callback evaluation and List.iter invoking a Unit callback once per element from left to right and returning Unit.

## Work item 01a001e3eb3c73f39cb3354375ef4ba5

Title: Add random element selection

Category: function

Evidence: Compiler Stdlib.fs:85-96 and Runtime.fs:2398-2448 provide native random Int64 generation but no List function; interpreter Builtins.Random/Libs/Random.fs:224-249 and packages/darklang/stdlib/list.dark:518-519 define randomElement.

Outcome:

Add List.randomElement with the canonical List<a> to Option<a> signature, returning None without requesting entropy for an empty list and selecting only valid elements for nonempty lists through the compiler's native random facility.

## Work item 01a001e3eb3c7a54a445d04d3f42881e

Title: Add chunking type and behavior

Category: type

Evidence: Compiler has no chunking type or function; interpreter packages/darklang/stdlib/list.dark:569-600 defines the helper, public error type, and function, with cases in execution/stdlib/list.dark:368-374.

Outcome:

Add public List.ChunkBySizeError with SizeMustBeGreaterThanZero and List.chunkBySize returning Error for nonpositive sizes or ordered, final-short chunk lists for positive sizes.

## Work item 01a001e3eb3c7837bc4b7782ec4802f4

Title: Remove compiler-only public List helpers

Category: function

Evidence: Compiler stdlib/List.dark:17,80,199,233 exposes the four additions; DCB1 docs/interpreter-parity.md:360-362 classifies them as compiler-only or differently named, and setAt is used by edigits, fannkuch, spectral_norm, and benchmark e2e sources.

Outcome:

Remove List.equals, flatMap, forAll, and setAt from public lookup, implement List.all directly, relocate or replace the misplaced zip, and migrate their compiler-owned consumers without exposing compatibility aliases; benchmark-only random updates may use the already-private __SkewList.setAt implementation detail.

## Work item 01a001e3eb3c7cc8a340d780ef726548

Title: Align list rendering and visible runtime errors

Category: behavior

Evidence: Compiler stdlib/List.dark:382-484 contains limited type-specific display helpers, while passes/1.6_ValueRendering.fs:141-217 generates recursive typed renderers; interpreter values and error formatting are defined by DList rendering and packages/darklang/prettyPrinter/runtimeError.dark.

Outcome:

Use recursive typed rendering for empty and populated lists of every compiler-representable scalar, tuple, nested-list, record, and enum element type; keep display helpers private and align heterogeneous, comparator, repetition, callback, and match failures wherever their phase is shared.

## Work item 01a001e3eb3c7f48a9dcadbe4dfe82da

Title: Build the focused parity regression suite

Category: other

Evidence: Compiler src/Tests/e2e/lists.e2e and src/Tests/e2e/stdlib/list.e2e cover substantial native behavior but encode spread syntax, Int64 contracts, end-exclusive range, old repeat, forAll, and setAt; interpreter backend/testfiles/execution/language/collections/dlist.dark and execution/stdlib/list.dark provide focused parity cases.

Outcome:

Add same-source e2e cases for literal separators, removed spread rejection, `::` and `@` associativity, empty inference, heterogeneous rejection, nested and cons patterns, construction order, callback order and short-circuiting, equality, comparable and renderable element categories, every public List signature, alias absence, and visible failures while preserving existing skew-list regression coverage.

## Work item 01a001e3eb3c7a3196fea3352a6c9a75

Title: Refresh list documentation and complete repository verification

Category: other

Evidence: docs/features/lists.md documents compiler spread syntax and the old API; docs/verification.md requires `./run-tests --ai` and `./benchmarks/run_benchmarks.sh --verify routine`, and list-heavy quicksort and spectral-norm benchmarks exercise the affected implementation.

Outcome:

Update list and language documentation to describe only canonical syntax and public APIs, preserve documented private skew-list architecture, and validate the completed scope with the full AI test suite and routine benchmark profile, including the resulting performance ratio.
