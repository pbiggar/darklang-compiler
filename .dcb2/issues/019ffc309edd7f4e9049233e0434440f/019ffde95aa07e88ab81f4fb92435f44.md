---
format: 1
event-id: 019ffde95aa07e88ab81f4fb92435f44
entity-id: 019ffc309edd7f4e9049233e0434440f
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T01:35:56.5766172+00:00
author: worker:65eb66d2e522:3725750:019ffd825d8d7ddfac574b9f8c608e82
previous: 019ffc3515e07ababe579947df3f9543
base-commit: b3e6b9bc938baf9642bf1d70095cc460baf71630
constraints-hash: d2c598d8fd37c875902539f007965e965222ac1a1d5cc282339cec44b6687bf2
revision: 019ffc3515e07ababe579947df3f9543
---
# Implementation plan

One contained delivery prepared at compiler HEAD 7578f3b2d7bcacf67109c08dea54ac9f87fe7413, using compiler b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 and darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d as the parity comparison. The pinned interpreter permits variable, wildcard, unit, and nested-tuple binders; rejects duplicate usable names and value/lambda annotations; evaluates a binding value before atomically exposing destructured names to its continuation.

## Work item 019ffde95aa07f5d80f18adb4f7da1f1

Title: Establish binding parity probes

Category: behavior

Evidence: Compiler coverage is scattered across src/Tests/e2e/variables.e2e, tuples.e2e, upstream/language/basic/elet.dark, upstream/language/elambda.dark, and SyntaxInteropTests.fs; interpreter probes exist in backend/testfiles/execution/language/basic/elet.dark and elambda.dark at 04fbe9d.

Outcome:

Add focused same-source tests for accepted and rejected let and lambda syntax, optional `in`, layout-delimited continuations, nested tuple destructuring, evaluation order, use-before-binding, duplicates, shadowing, mismatch failures, closures, and scope isolation across all owned expression regions.

## Work item 019ffde95aa07512b9f131c16efb65eb

Title: Introduce binding-specific AST types

Category: type

Evidence: src/DarkCompiler/AST.fs:145-167 uses the full match `Pattern` for destructuring, while Expr at 195-211 splits simple/pattern lets and restricts lambdas to typed names; interpreter ProgramTypes.fs defines a separate `LetPattern` shared by ELet and ELambda.

Outcome:

Replace the split `Let`/`LetPattern` representation with one non-recursive binding node containing a restricted let pattern, value, and continuation; represent lambda binders with the same pattern model while keeping inferred types and source annotations distinct from parsed binders.

## Work item 019ffde95aa07956b03c8c49b593eba0

Title: Preserve binding boundaries and locations

Category: type

Evidence: src/DarkCompiler/passes/1_InterpreterParser.fs reconstructs newline lets by trying token splits, while interpreter Parser.fs:636-700 and 817-846 uses spanned tokens and offside scopes to build blocks.

Outcome:

Retain the token location and layout information needed to delimit value blocks and continuations deterministically and to attach binding, annotation, and duplicate diagnostics to the relevant binder rather than guessing token splits.

## Work item 019ffde95aa07eca8281f00cf58593f1

Title: Adopt interpreter let grammar

Category: function

Evidence: src/DarkCompiler/passes/1_Parser.fs:1435-1454 requires `in` and calls general `parsePattern`; 1_InterpreterParser.fs:1619-1636 also reuses general patterns. Interpreter Parser.fs:1305-1355 has a dedicated restricted `parseLetPattern`, and 1357-1443 accepts optional `in`.

Outcome:

Make both compiler entry syntaxes parse the public let form in every expression position, with optional `in`, layout-delimited value and continuation blocks, and only variable, wildcard, unit, parenthesized, or nested-tuple let patterns; reject literal, constructor, list, record, and other match-only patterns.

## Work item 019ffde95aa07c7fa053e6934597278d

Title: Align lambda and local-function syntax

Category: function

Evidence: src/DarkCompiler/passes/1_Parser.fs:1365-1384 and 1945-1951 accepts `(x: T) =>`; 1_InterpreterParser.fs:2052-2107 accepts typed `fun` parameters. Interpreter Parser.fs uses `LetPattern` for lambdas but parses annotations on nested function parameters and returns; SyntaxInteropTests.fs currently asserts typed-lambda acceptance.

Outcome:

Accept interpreter `fun` binders, including nested tuple destructuring, without lambda annotations; retain parameter and return annotations only on interpreter-style local function declarations, and remove fat-arrow and typed-lambda binding forms.

## Work item 019ffde95aa07531a0f633a49b93d1ad

Title: Validate binders consistently

Category: function

Evidence: Interpreter Validation.fs:79-150 and 203-216 rejects duplicate usable binders before lowering. Compiler duplicate checking is match-specific in 1.5_TypeChecking.fs:4019-4032 and can be disabled through AST.WarningSettings and the E2E `warn_on_duplicate_pattern_bindings` flag.

Outcome:

Add one pure structural validator for let patterns, complete lambda parameter lists, and pattern-bound names that returns the complete ordered binder set or a diagnostic, rejects repeated usable names, and never exposes a partially validated set.

## Work item 019ffde95aa07152896a6fed0df774ad

Title: Resolve lexical binding identities

Category: function

Evidence: src/DarkCompiler/passes/1.5_TypeChecking.fs:5453-5504 currently threads sets of textual names through lets, patterns, branches, and sequences; interpreter WrittenTypesToProgramTypes.fs:591-611 converts the RHS before extending its local-binding context.

Outcome:

Resolve each accepted binder to a distinct local identity, resolve the value in the incoming environment, and resolve only the continuation in the extended environment so inner shadows cannot rewrite outer references or make a name available in its own value.

## Work item 019ffde95aa07f03a0e735d82dcc24db

Title: Type-check destructuring atomically

Category: function

Evidence: src/DarkCompiler/passes/1.5_TypeChecking.fs:2191-2225 rewrites `LetPattern` through `Match` and a known mismatch through `Let("__", ...)`; the interpreter retains ELet and its dedicated LetPattern through evaluation.

Outcome:

Infer and check the value in the incoming type environment, validate the complete unit or tuple shape and binder types, then type-check the continuation with all bindings present; remove the conversion of let destructuring into a general match and eliminate generated sentinel names.

## Work item 019ffde95aa077719e2c8097623e49df

Title: Lower bindings as guarded transitions

Category: function

Evidence: src/DarkCompiler/passes/2_AST_to_ANF.fs:5159-5379 extracts general match bindings, while 7995 requires LetPattern to have disappeared. Interpreter ProgramTypesToRuntimeTypes.fs:721-730 emits value, pattern check, then continuation; Interpreter.fs:1882-1898 commits extracted registers only after the whole pattern matches.

Outcome:

Lower the value exactly once, emit a complete destructuring check before projections become usable, extend the ANF environment only for the continuation, and route mismatch directly to the binding runtime-error path without evaluating the continuation.

## Work item 019ffde95aa07bf681759d325a62246f

Title: Preserve ownership at binding boundaries

Category: behavior

Evidence: Binding lowering feeds src/DarkCompiler/passes/2.5_RefCountInsertion.fs let-frame analysis; src/Tests/e2e/refcounting.e2e already covers tuple destructuring and lexical release timing but not failed atomic bindings.

Outcome:

Ensure evaluated values and destructured heap components have correct ownership on successful continuation exit and pattern failure, with no leaked, double-released, or prematurely released outer value when names are shadowed.

## Work item 019ffde95aa07e68892990021ae97a74

Title: Contain child expression scopes

Category: behavior

Evidence: Current scope handling is distributed across TypeChecking.checkExpr and resolveProgramReferences plus AST_to_ANF.toANF/toAtom; Sequence was added at AST.fs:199, while 2_AST_to_ANF.fs:8258-8276 separately handles lazy branches and rejects sequence atom lowering.

Outcome:

Use isolated child environments for conditional conditions and arms, match scrutinees, guards and cases, lambda bodies, nested operands, calls, and statement-sequence elements so bindings cannot escape failed, skipped, unselected, or completed nested regions.

## Work item 019ffde95aa07236b9527e3206005679

Title: Match nested shadowing and capture behavior

Category: behavior

Evidence: src/Tests/e2e/variables.e2e, closures.e2e, name-resolution.e2e, and upstream/stdlib/earg.dark cover fragments; interpreter basic/elet.dark, elambda.dark, and earg.dark at 04fbe9d demonstrate nested shadowing and definition-time closure capture.

Outcome:

Make nearest eligible binders win across lets, lambda parameters, match cases, and function arguments; capture the outer identity visible when a closure is formed, including rebinding cases where the new value references the old binding.

## Work item 019ffde95aa0749aaba6de5bf7479a50

Title: Remove obsolete syntax and overrides

Category: other

Evidence: ASTPrettyPrinter.fs:465-583 and 704-723 emits separate compiler/interpreter let and lambda forms; 34 current Dark or documentation files contain `=>`, and upstream/basic/elet.dark still contains last-wins duplicate tests with an opt-out.

Outcome:

Delete the superseded binding and lambda parser/pretty-printer paths and the duplicate-binding opt-out, then migrate affected repository-authored Dark tests, standard-library source, benchmarks, examples, and documentation without changing their results.

## Work item 019ffde95aa07b918bc752b3c2529fd0

Title: Document parity and AOT diagnostics

Category: other

Evidence: Current starting material is docs/darklang-differences.md, docs/name-resolution.md, docs/conditional-sequence-parity.md, and DCB1 report commit 8a402797; none provides a revalidated binding-specific conclusion.

Outcome:

Publish a binding parity table tying each syntax and behavior claim to its probe, describe the resulting lexical model, and classify remaining early type/name failures as AOT-only behavior or intentional divergence.

## Work item 019ffde95aa07facbe73526a8a4f8804

Title: Complete repository verification

Category: other

Evidence: docs/verification.md defines verification; AGENTS.md requires `./run-tests --ai` and `./benchmarks/run_benchmarks.sh --verify routine` for compiler changes.

Outcome:

Run the full AI test suite and routine benchmark profile, resolve warnings or failures, and record the benchmark performance ratio with the parity results.
