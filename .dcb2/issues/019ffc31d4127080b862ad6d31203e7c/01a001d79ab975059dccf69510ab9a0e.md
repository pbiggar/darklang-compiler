---
format: 1
event-id: 01a001d79ab975059dccf69510ab9a0e
entity-id: 019ffc31d4127080b862ad6d31203e7c
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T19:55:02.2018381+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc347e9572d685d97e776f8383ac
base-commit: 5a4178c643a959c0e521fd0756e1d200b15b3659
constraints-hash: 193ac11d192503f5f75ee8e23033670da2be72b5782504332c7e0c875deac0f9
revision: 019ffc347e9572d685d97e776f8383ac
---
# Implementation plan

One contained implementation at compiler C@5a4178c643a959c0e521fd0756e1d200b15b3659, revalidated against accepted evidence C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3 and interpreter I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d. The compiler has Blob and Int but no Stream type, HTTP stdlib/runtime registration, or native networking facilities; imported upstream HTTP fixtures are not currently executed.

## Work item 01a001d79ab97acf88e21bedb35af793

Title: Pin the HTTP parity contract

Category: other

Evidence: `docs/blob-parity.md` and `docs/cli-process-host-input-parity.md` establish the repository pattern; DCB1 `8a402797` only reports the historical absence.

Outcome:

Create a revision-stamped HTTP runtime parity document covering the public surface, exact observable contracts, target-specific transport differences, and an empty-by-default compiler-extension section. Classify protocol negotiation, DNS/TLS trust-store behavior, signal handling, bind-error wording, telemetry, and deterministic reference-count cleanup explicitly.

## Work item 01a001d79ab9714c91183e84cddda373

Title: Introduce the Stream type

Category: type

Evidence: `src/DarkCompiler/AST.fs:23-59` has no stream case; I@04f `packages/darklang/stdlib/stream.dark` and `backend/src/LibExecution/Stream.fs` define a lazy, single-consumer, non-persistable resource.

Outcome:

Add `Stream<T>` throughout both parsers, AST/type substitution, pretty-printing, name resolution, monomorphization, ownership analysis, and IR metadata. Represent it as an opaque managed resource descriptor, exclude it from structural equality, and close its source when the last owned reference is released.

## Work item 01a001d79ab97eb2830f1cbd1e112b2c

Title: Implement Stream pull semantics

Category: function

Evidence: I@04f `backend/src/Builtins/Builtins.Pure/Libs/Stream.fs`, `backend/src/LibExecution/Stream.fs`, and `backend/tests/Tests/Stream.Tests.fs`; the compiler has no `Stdlib.Stream` source or intrinsic.

Outcome:

Provide the portable Stream wrappers and native operations for list sources, unfolding, single-element pulls, list/blob drains, explicit close, map, filter, take, and concatenation. Preserve laziness, arbitrary-precision take clamping, closure callback errors, stable exhaustion, chunked byte drains, and exactly-once cleanup on EOF, close, failure, or final release.

## Work item 01a001d79ab971c6a562009f2516eb4d

Title: Port shared HTTP values

Category: type

Evidence: I@04f `packages/darklang/stdlib/http.dark`; `HttpServer.Handler` depends on `Http.Request` and `Http.Response`, while `src/DarkCompiler/stdlib` has no `Http.dark`.

Outcome:

Port the active request/response portion of `http.dark`, including the records and response, query, and header helpers needed by handlers and routing. Retain the pinned Dark implementation with only namespace/syntax adaptations required by the AOT compiler; do not claim the commented, incomplete cookie helper.

## Work item 01a001d79ab9714591eadf743679e0c7

Title: Expose the HttpClient language surface

Category: function

Evidence: I@04f `packages/darklang/stdlib/httpclient.dark`; `src/DarkCompiler/Stdlib.fs:202-219` and `CompilerLibrary.fs:1042-1103` contain no client registration or source load.

Outcome:

Define the client error algebras and buffered/streaming response records, then port the request verb wrappers, error rendering, authentication helpers, and content-type values. Route only transport-bearing calls through private typed intrinsics so portable policy remains in Dark.

## Work item 01a001d79ab97a56998efa98479b4366

Title: Match client validation and errors

Category: behavior

Evidence: I@04f `backend/src/Builtins/Builtins.Http.Client/Libs/HttpClient.fs:435-608,682-799`; existing `src/Tests/e2e/upstream/stdlib/httpclient.dark` probes invalid methods, URLs, headers, SSRF targets, timeouts, and TLS failures.

Outcome:

Implement the pinned method-token, absolute-URI, scheme, host, DNS-rebinding, metadata-header, empty-header, and content-type checks. Preserve disabled redirects/cookies/decompression, configured timeout behavior, duplicate-header flattening, and the baseline normalization of response header keys and values into the structured error algebra.

## Work item 01a001d79ab971fb915817fdf8fa95da

Title: Carry network effects through the compiler

Category: other

Evidence: CLI effects provide the existing pattern in `passes/2_AST_to_ANF.fs:150-164`, `ANF.fs`, `MIR.fs`, `LIR.fs`, `passes/3_ANF_to_MIR.fs`, `passes/4_MIR_to_LIR.fs`, `passes/5_RegisterAllocation.fs`, and both `6_CodeGen.fs` files.

Outcome:

Add explicit Stream, buffered-client, streaming-client, and server operations to ANF, MIR, and LIR; mark them effectful for optimization and tree shaking; define argument/result ownership; model call clobbers in register allocation; and lower them in both native backends without magic names or sentinel states.

## Work item 01a001d79ab97df486c034d6e1b1cffe

Title: Build the native network transport

Category: other

Evidence: `src/DarkCompiler/Platform.fs:76-136` currently exposes only file/time/random syscalls; generated Linux binaries have no networking or TLS layer, and Mach-O currently imports only libSystem with an empty symbol table.

Outcome:

Extend target configuration with typed socket, polling, shutdown, and signal facilities and implement DNS, TCP, TLS certificate validation, HTTP request serialization, and response framing for macOS ARM64, Linux ARM64, and Linux x64. Handle content-length, chunked transfer, bodyless responses, partial I/O, EINTR, timeouts, and descriptor cleanup without delegating to an external executable.

## Work item 01a001d79ab97221a3726c35634060b3

Title: Execute buffered client requests

Category: function

Evidence: I@04f `HttpClient.fs:529-608,682-799` buffers the response and maps timeout, URI, header, and network failures; no compiler equivalent exists.

Outcome:

Lower the buffered intrinsic to send the supplied Blob body, consume the complete response within the baseline buffering and timeout bounds, and construct normal managed `Result<Response,RequestError>` values. Close request, connection, TLS, and temporary buffers on every success, rejection, cancellation, parse error, and I/O failure path.

## Work item 01a001d79ab97db98758976908c7a2de

Title: Execute streaming client requests

Category: function

Evidence: I@04f `HttpClient.fs:625-672,867-1000` and `HttpClient.Tests.fs:667-754`; the interpreter source notes incomplete body-timeout and drain-error handling that the accepted lifecycle contract requires classifying.

Outcome:

Return status and headers once available while retaining a live `Stream<UInt8>` body backed by transport chunks. Define response/body ownership so EOF, explicit close, early consumer termination, cancellation, and drain failure release the connection exactly once; setup failures remain `RequestError`, while post-return failures use the documented Stream failure path.

## Work item 01a001d79ab97ce5840637d593066d17

Title: Port the SSE state machine

Category: function

Evidence: I@04f `packages/darklang/stdlib/httpclient.dark` module `Sse` and `backend/testfiles/execution/stdlib/sse.dark`; the same fixture is present locally but omitted from the executed upstream set.

Outcome:

Implement the pinned event and parser state plus line splitting, field folding, block emission, byte pulling, and lazy parsing in Dark. Verify every transport chunk split, multiline data joining, comments and unknown fields, blank or data-less blocks, ID persistence, multiple events, empty input, and trailing unterminated final blocks.

## Work item 01a001d79ab97179935b953f6c240087

Title: Port routing behavior

Category: function

Evidence: I@04f `packages/darklang/stdlib/httpserver.dark`; `matchRoute` requires equal segment counts and calls `matchSegment` with no remaining path, so its wildcard currently consumes one segment.

Outcome:

Copy the route types and matching helpers into `Stdlib.HttpServer`, preserving first-match ordering, case-sensitive method values, `*` method matching, case-insensitive lookup of `x-http-method`, GET fallback, query removal, variable extraction, and the pinned one-path-segment wildcard behavior.

## Work item 01a001d79ab97a5b994468cc775e790e

Title: Add server configuration and entrypoint

Category: function

Evidence: I@04f `httpserver.dark` module `Config` and `serve`; `HttpServer.fs:430-483` validates port range and nonnegative body limits and binds before `onListening`.

Outcome:

Define the server configuration record and defaults, including the 30 MiB body cap and feature switches, and connect `serve` to a typed native operation. Validate arbitrary-precision ports and limits before narrowing, invoke the listening callback only after a successful bind, and return plain bind failures through `Result`.

## Work item 01a001d79ab971fca43fa77012d2edfb

Title: Bridge native requests and responses

Category: behavior

Evidence: I@04f `backend/src/Builtins/Builtins.Http.Server/Http.fs` and `Libs/HttpServer.fs:63-267`; inherited AOT guidance rejects unnecessary interpreter-only runtime type decisions.

Outcome:

Construct managed `Http.Request` values with lowercase header keys, repeated headers retained, an injected method header, canonicalized URL when configured, and an independently owned Blob body. Invoke the compiled handler closure and serialize its `Http.Response`, using AOT type checking instead of interpreter-style dynamic shape checks while retaining runtime range/error handling that static typing cannot eliminate.

## Work item 01a001d79ab976b88903734290b10327

Title: Enforce body limits and response policy

Category: behavior

Evidence: I@04f `HttpServer.fs:30-267`; `HttpServer.Tests.fs` covers response conversion and concurrent ephemeral bodies but lacks complete exact-boundary coverage.

Outcome:

Accept bodies exactly at the configured boundary and emit 413 before handler execution for declared or chunked bodies that exceed it. Preserve forwarded-protocol canonicalization, standard-header injection without overriding handler headers, response content length, handler-failure 500 sanitization, request logging, and isolation of overlapping request bodies.

## Work item 01a001d79ab97ef781854ffbf3db34af

Title: Implement listener lifecycle and shutdown

Category: behavior

Evidence: I@04f `HttpServer.fs:286-429` stops its listener through cancellation but has platform-dependent signal and in-flight-task behavior; the compiler currently has no listener or signal-backed server loop.

Outcome:

Bind and accept with recoverable errors, serve requests without leaking per-connection resources, and block until supported termination signals arrive. Stop acceptance, close the listener, settle or cancel in-flight work predictably, remove signal hooks, and support repeated start/stop and occupied-port cases across all targets.

## Work item 01a001d79ab97a48817cb325fb67fea0

Title: Add executable conformance coverage

Category: other

Evidence: `src/Tests/test-suite-tooling/TestRunner.fs:235-302` omits `http.dark`, `httpclient.dark`, `stream.dark`, and `sse.dark`; `docs/verification.md` requires `./run-tests --ai` and `./benchmarks/run_benchmarks.sh --verify routine`.

Outcome:

Add deterministic loopback client/origin and compiled-server subprocess fixtures, with test-only transport policy selected through runner options rather than a Dark API. Activate the local upstream HTTP, client, Stream, and SSE sources; add focused routing, chunk scheduling, body-boundary, cancellation, early-close, bind, signal, cleanup, and cross-architecture probes; then gate the change with the full suite and routine benchmark verification, reporting the `RESULTS.md` performance ratio.
