---
format: 1
event-id: 01a00204431a7d30bb791e09fff6f057
entity-id: 019ffc309edd74eb93127dc9270bf9bf
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T20:43:48.8907215+00:00
author: worker:65eb66d2e522:453935:01a001182c6a71d3b50246ab48861cfc
previous: 019ffc3515e370a9874527c8e2e2a3fb
base-commit: 1986db19aed4288b206cca99b35f4d20125b4956
constraints-hash: f1c57af135663bc7f9f4780e8b9233b6dd14fbc01316775122eca4fc25edf2f3
revision: 019ffc3515e370a9874527c8e2e2a3fb
---
# Implementation plan

Read-only inspection at live compiler HEAD 1986db19aed4288b206cca99b35f4d20125b4956. Every parity comparison is pinned to compiler b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 and darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d; DCB1 8a402797 is inventory only. This is one contained implementation. Compile-time rejection of statically invalid record operations remains an intentional AOT phase divergence.

## Work item 01a00204431a7640a0243eb611817c8f

Title: Adopt the canonical record grammar

Category: function

Evidence: Current 1_Parser.fs:857-881,1948-2119 and 1_InterpreterParser.fs:885-913,2163-2398 duplicate record parsers, admit anonymous literals and empty updates, and discard parsed literal type arguments. I@04fbe9 Parser.fs:1634-1644,1945-1970,2063-2077,2245-2399 retains qualified type arguments and rejects anonymous records and empty updates.

Outcome:

Make declarations, qualified and generic constructions, postfix field access, and non-empty updates use the interpreter grammar in both parser entry paths. Preserve explicit construction type arguments, accept comma, semicolon, and newline field separators, and reject anonymous literals, empty updates, and obsolete record spellings at parsing or syntax validation.

## Work item 01a00204431a70fdae1f1d3f855a3db5

Title: Represent resolved record references explicitly

Category: type

Evidence: Current AST.fs:52,167,292-294 and 332 stores record names as strings and RecordLiteral has no type arguments. I@04fbe9 ProgramTypes.fs:479-493 uses resolved FQTypeName plus TypeReference arguments; RuntimeTypes.fs:984-990 stores source and runtime type names.

Outcome:

Replace string-only record references with a nominal record identity carrying the resolved qualified type name, source alias identity where required, and ordered type arguments. Make record expressions retain this reference instead of reconstructing identity from spelling or field shape.

## Work item 01a00204431a7f35a1668f386fc5ec87

Title: Preserve declared record metadata

Category: type

Evidence: Current TypeChecking.fs:581,743-765 and 6622-6623 stores only fields and infers TypeParams from their first occurrence; ValueRendering.fs:297-313 repeats that inference. I@04fbe9 ProgramTypes.fs:594-610 and RuntimeTypes.fs:1503-1513 retain declared type parameters and a non-empty record-field list.

Outcome:

Use one RecordInfo model containing nominal identity, declared type parameters in declaration order, a non-empty field sequence, keyed field lookup, and presentation order. Stop inferring generic parameter order from field occurrence or dropping phantom parameters.

## Work item 01a00204431a75699cf133ef916c62eb

Title: Match record declaration edge cases

Category: behavior

Evidence: Current recordInfo builds FieldTypes with Map.ofList while retaining duplicate entries in Fields, and summarizeTopLevelDeclarations discards declared parameters. I@04fbe9 Parser.fs:2760-2805 rejects empty declarations; TypeChecker.fs:1065-1084 searches the declaration NEList from the head, so the first duplicate definition governs lookup.

Outcome:

Reject empty record declarations, preserve declaration field order, and implement duplicate declaration names with the pinned first-declared field lookup rather than the compiler's inconsistent list/last-wins-map behavior. Validate type references and declared generic arity without silently replacing metadata.

## Work item 01a00204431a780cab848612bec70fd7

Title: Align construction validation and ordering

Category: behavior

Evidence: C@b2e1f3d1 TypeChecking.fs:3257-3372 collapses fields with Map.ofList before duplicate detection; AST_to_ANF.fs:4531-4569 evaluates source order but lays out declaration-order tuples. I@04fbe9 TypeChecker.fs:1093-1246 reports duplicate, missing, unexpected, empty, and wrong-type fields; ProgramTypesToRuntimeTypes.fs:1077-1102 evaluates initializers left-to-right.

Outcome:

Validate named construction against the resolved record definition: duplicate source fields fail, missing and unknown fields are distinguished, empty normalized keys are rejected, and each value has the declared substituted type. Evaluate valid initializers once in source order, then populate keyed presentation order independently of native slot order.

## Work item 01a00204431a7adebf79d98b3f1ef188

Title: Carry generic record arguments through all operations

Category: function

Evidence: C@b2e1f3d1 parsers discard explicit RecordLiteral type arguments, while TypeChecking.fs:657-681 and AST_to_ANF.fs:829-834 infer parameter order from fields. I@04fbe9 records.dark:11-23 exercises aliases and nested generics; TypeChecker.record preserves resolved ordered type arguments.

Outcome:

Support explicit and inferred record arguments, including unused parameters, nested generic fields, aliases with substituted arguments, and multi-level access and update. Require exact declared arity and use the same ordered substitution for construction, access, update, equality, rendering, and ownership metadata.

## Work item 01a00204431a7ffa89a6a23256d68e69

Title: Implement canonical record access

Category: function

Evidence: Current TypeChecking.fs:3925-3949 and AST_to_ANF.fs:5584-5609 use string registries and positional TupleGet. I@04fbe9 Interpreter.fs:1985-2002 distinguishes empty name, missing field, and non-record access; record-field-acess.dark:1-12 contains focused public cases.

Outcome:

Resolve each access through the receiver's nominal RecordInfo, substitute generic field types, and lower chained access to the correct slot without structural field-name inference or default types. Produce interpreter-shaped diagnostics for empty, absent, and non-record access at the earliest valid AOT boundary.

## Work item 01a00204431a76ce8c4e9ae3adac75c1

Title: Match immutable update semantics

Category: behavior

Evidence: Current AST_to_ANF.fs:5528-5578 already preserves base-then-update evaluation but rebuilds a tuple via Map last-wins. I@04fbe9 ProgramTypesToRuntimeTypes.fs:1105-1124 and Interpreter.fs:2260-2288 define evaluation and identity flow; records.dark:61-96 confirms duplicate-update last-write-wins and invalid-field behavior.

Outcome:

Evaluate the base record once, then update expressions once from left to right; apply duplicate updates last-write-wins; reject empty, unknown, and wrongly typed updates; preserve untouched fields, resolved identity, source alias identity, and generic arguments in the result.

## Work item 01a00204431a73f8aae252126bec0ffe

Title: Remove record pattern syntax and lowering

Category: behavior

Evidence: Current AST.fs:167, both parser parseRecordPatternWithTypeName functions, TypeChecking.fs PRecord branches, and extensive AST_to_ANF record-pattern paths implement a compiler extension. I@04fbe9 ProgramTypes.fs:270-301 and RuntimeTypes.fs:639-664 have no record pattern; packages/darklang/cli/docs/records.dark:23-26 explicitly documents no record destructuring.

Outcome:

Remove PRecord from the public AST, both parsers, binder validation, type checking, equality-helper generation, and ANF match lowering. Reject every record-pattern form uniformly, including partial, duplicate, missing, or unknown fields; records remain matchable through a variable followed by field access.

## Work item 01a00204431a74f29aced4e7c35747c0

Title: Introduce a nominal runtime record representation

Category: type

Evidence: Current AST_to_ANF.fs:5488-5603 lowers records entirely to TupleAlloc/TupleGet; ANF, MIR, LIR, and Runtime.fs expose no record value operation or identity. I@04fbe9 RuntimeTypes.fs:790-804,984-990 and Interpreter.fs:2236-2288 retain record-specific operations and DRecord identity.

Outcome:

Add explicit record allocation, projection, and clone operations through ANF, MIR, LIR, register allocation, and both backends. Store or reference an immutable descriptor containing resolved identity, source identity, concrete type arguments, field-name-to-slot metadata, and deterministic presentation order while retaining fixed native payload slots.

## Work item 01a00204431a7e6e99fb1540a1d4ea1a

Title: Update ownership for record descriptors and fields

Category: function

Evidence: Current docs/features/records.md:38-44 and AST_to_ANF.fs record lowering describe tuple-equivalent storage; ANF.fs and 2.5_RefCountInsertion.fs derive record field ownership from string registries. Adding identity changes payload offsets and all fixed-block size/release calculations.

Outcome:

Teach reference-count shape and release planning about the nominal record layout, descriptor slot, and concretely substituted field types. Preserve correct retain/release behavior for nested records and managed generic fields on ARM64 and x64 without treating ordinary tuples as records.

## Work item 01a00204431a79f0ba518d37da233cc1

Title: Make equality nominal and recursive

Category: behavior

Evidence: Current TypeChecking.fs:5422-5450 generates field-only comparisons against positional tuples and cannot inspect runtime identity. I@04fbe9 NoModule.fs:80-94 compares runtime type name, merged type arguments, field count, keys, and recursive values while ignoring source alias spelling.

Outcome:

Compare resolved record identity and compatible ordered type arguments before recursively comparing the same named field set. Treat aliases resolving to one record as equal, distinct nominal records as incompatible under AOT typing, and nested records and generic fields with their own equality rules.

## Work item 01a00204431a7c2bb9737616d658620e

Title: Complete interpreter-compatible rendering

Category: behavior

Evidence: Current ValueRendering.fs:292-329 sorts fields and renders recursively but infers parameters from fields and always emits one line. I@04fbe9 packages/darklang/prettyPrinter/runtimeTypes.dark:500-525 uses resolved identity, stored arguments, Dict key order, recursive rendering, and an 80-character layout decision.

Outcome:

Render the resolved qualified type name and concrete generic arguments, recursively render all fields in ordinal key order with `field: value` syntax, and implement the interpreter's single-line versus indented multiline threshold. Use runtime identity rather than inferred static spelling.

## Work item 01a00204431a770da42c1e55da8c12d6

Title: Migrate record-bearing sources and remove legacy claims

Category: other

Evidence: Current record-bearing Dark sources include stdlib/CliExecution.dark, CliHost.dark, CliPosix.dark, CliProcess.dark, CliStdinKeyRead.dark, CliStdinModifiers.dark, PackageManager.dark, and nbody; records.e2e and pattern_matching.e2e retain record patterns. The exact pinned upstream records.dark and record-field-acess.dark are present but not selected by TestRunner.

Outcome:

Rewrite compiler stdlib, benchmarks, examples, and tests to canonical declarations, literals, updates, and access. Replace record-pattern usages with variable matches plus field access, remove adapted legacy expectations, and keep the pinned upstream record fixtures unchanged where their surrounding language features are supported.

## Work item 01a00204431a7f95898d92dadcdc8849

Title: Establish record parity and regression coverage

Category: other

Evidence: C@b2e1f3d1 records.e2e covers ordinary construction, access, updates, and one record pattern but omits most invalid, identity, ordering, alias, and rendering cases. I@04fbe9 records.dark and record-field-acess.dark provide focused same-source construction, generic, nested, update, and access behavior.

Outcome:

Add focused failing E2E and syntax cases for canonical parsing, qualified and generic identity, aliases, phantom arguments, declaration and construction duplicates, missing and unknown fields, empty forms, field order, initializer and update order, duplicate updates, nested access and updates, nominal equality, rendering thresholds, pattern rejection, and managed-field lifetimes. Run the complete suite and routine benchmark verification after the implementation.

## Work item 01a00204431a7284940520e003954354

Title: Document the canonical record contract

Category: other

Evidence: Current docs/features/records.md:36-44,105-119 and docs/current-language-features.md:50 describe tuple storage and record patterns; current parity documents contain no dedicated revision-stamped record matrix.

Outcome:

Replace tuple-equivalence and record-pattern claims with the nominal record model, public grammar, distinct evaluation and presentation orders, generic and alias identity rules, validation behavior, runtime layout, equality, rendering, and the intentional AOT diagnostic-phase differences. List any surviving compiler-only behavior explicitly.
