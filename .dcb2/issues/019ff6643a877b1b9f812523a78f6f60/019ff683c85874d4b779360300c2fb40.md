---
format: 1
event-id: 019ff683c85874d4b779360300c2fb40
entity-id: 019ff6643a877b1b9f812523a78f6f60
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-12T15:07:39.4809802+00:00
author: worker:65eb66d2e522:40713:019ff640f1e77ef78c5242339394e833
previous: 019ff6643a88798fa9daf9b9aa9889fa
base-commit: d001b8d5565cd2f05515d8cde5e61fa74293b6d5
constraints-hash: a4c05e1626c3738ed4c3cf31e9db47f65668e7aa7fff89e07ec0b5a5f883f185
revision: 019ff6643a88798fa9daf9b9aa9889fa
---
# Work plan

ANF optimization currently performs separate whole-program TempId collection and branch-rewrite traversals even when no inverted Boolean-literal branch exists; the change is confined to the program-level rewrite gate and requires strict approval, profiling, equivalence, timing, test, and benchmark evidence.

## Work item 019ff683c8587f4c892262682295694b

Title: Bypass no-op inverted-branch rewriting

Category: function

Evidence: src/DarkCompiler/passes/2.3_ANF_Optimize.fs:1354-1419 builds a Set of all TempIds, derives its maximum, and then always rewrites every function and main expression; lines 1423-1427 invoke it whenever constant folding is enabled. CompilerLibrary.fs:461-474 exposes ANF Optimizations through -vv timing, while the final compilation total is printed at CompilerLibrary.fs:1540. src/Tests/optimization/anf.opt:1056-1069 already pins the inverted-branch rewrite and fresh TempId 2 across a parameter and main binding. benchmarks/profiles/routine.txt contains 19 programs; benchmarks/problems/primes/dark/main.dark:17 has the direct false/true target, while fasta has none by source inspection. docs/verification.md requires the full test suite and routine verification; benchmarks/RESULTS.md currently reports a 2.74x performance ratio.

Outcome:

First obtain explicit approval from an individual human; this plan is not approval. Before implementation, temporarily time the fresh-ID scan and rewrite traversal separately on fasta and proceed only if they materially explain the ANF Optimizations cost. Remove that instrumentation, then establish comparable 10-run baseline samples. Within rewriteInvertedBoolLiteralBranchesInProgram, replace the Set-building TempId collection and separate rewrite-needed check with one pure whole-program analysis that computes both the maximum TempId across parameters, bindings, and atom uses and whether any If has direct Return false/Return true branches. Return the original Program when no target exists; otherwise seed VarGen at maximum+1 and preserve the existing function-order, postorder rewrite traversal and output. Treat the measured fasta regression as the failing performance test and preserve the existing exact-ANF rewrite regression. Remove all exploratory instrumentation from retained code. Collect 10 comparable candidate fasta -vv runs and report raw ANF Optimizations and total-compilation values, medians, absolute deltas, and percentage deltas; abandon the candidate if results are neutral or noisy. For all 19 routine programs, report comparable before/after ANF Optimizations and total-compilation deltas and identify rewrite-bearing programs, including primes. Collect 10 comparable before/after ./run-tests --ai wall-clock runs with raw values, medians, and absolute and percentage deltas. Compare normalized optimized ANF and binary bytes before/after for every routine program. Finally run ./run-tests --ai and ./benchmarks/run_benchmarks.sh --verify routine, reporting pass/fail, every routine instruction count, and the final RESULTS.md performance ratio.
