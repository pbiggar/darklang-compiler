---
format: 1
event-id: 01a000f166347959bde834d6321299c9
entity-id: 019ffc31d4127a69925d9aa05d4a1f36
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-14T15:43:35.4769383+00:00
author: worker:65eb66d2e522:348439:01a000d3fd9976a49f0f6ded9b9ae91b
previous: 019ffc347e987c1c855cf3d87e77f256
base-commit: 3e2575512aca958360ba2bf45e7183f187fd0154
constraints-hash: 564f92b97b8469382ed1b106c88bb1608b696dffdd4871ecc3022c8b04897bb5
revision: 019ffc347e987c1c855cf3d87e77f256
---
# Implementation plan

Single contained slice, revalidated at compiler C@51093e0a8e31fe45a9aa79a317fbefd6b74fbcc3 and current checkout HEAD 6a4c6eb87fed3159e6e2d4b310c5e89b4658e6fc against interpreter I@04fbe9dcc995c6188757d583e273cbd30a3e2d3d; DCB1 8a402797 is locator evidence only. The pinned baseline deliberately emits ANSI and carriage returns to TTY and non-TTY alike, sends every Log level to stdout, and implements Spinner.run as a static message without timing or animation.

## Work item 01a000f166347c96bfc39840a61e5952

Title: Publish the pinned presentation contract

Category: other

Evidence: I packages/darklang/stdlib/cli/log.dark and cli/ui/{color,progress,prompt,spinner,table}.dark; C has no matching definitions; 8a402797:docs/interpreter-parity.md:505-541 is only broad starting evidence.

Outcome:

Add a parity matrix covering public signatures, byte output, destinations, newlines, return values, redirected input, EOF, and interruption; classify the absent compiler surface as gaps, AOT rejection as an intentional phase difference, and any legacy compiler syntax as a compiler-only extension.

## Work item 01a000f166347d20a4f036da89788f5a

Title: Register explicit presentation builtins

Category: type

Evidence: I backend/src/Builtins/Builtins.Cli/Libs/Output.fs:18-50 and Stdin.fs:394-408; C src/DarkCompiler/Stdlib.fs:181-205 registers no console functions.

Outcome:

Expose typed intrinsics Builtin.print(String): Unit, Builtin.printLine(String): Unit, and Builtin.stdinReadLine(Unit): String without runtime type dispatch or host-library linkage.

## Work item 01a000f16634712bb17aa5da98b7ef1c

Title: Lower stdout writes through every native backend

Category: function

Evidence: C ANF.fs:232-242 only models final-result Print and file I/O; LIR.fs:113-125 has result-oriented printers; x64 6_CodeGen.fs:2800-2821 makes PrintString exit, while Platform.fs:77-83 already supplies Write.

Outcome:

Add effectful stdout-write operations through ANF, MIR, LIR, IR printers, liveness, optimization, and ARM64/x64 code generation; write UTF-8 to fd 1, optionally append one LF, preserve ordering, handle partial writes and EINTR, and never reuse final-result print instructions that terminate execution.

## Work item 01a000f166347108af5ee760d96ec2f0

Title: Implement native stdin line reads

Category: function

Evidence: I Stdin.fs:394-408 maps Console.ReadLine null to empty string; C Platform.fs:81-83 and target tables already expose Read, but no language-level read operation exists.

Outcome:

Add stdin-read lowering across ANF, MIR, LIR, allocation/ownership, and all supported backends. Read UTF-8 from fd 0, recognize LF/CRLF line endings, omit the terminator, retain unread bytes for successive calls, return partial final lines, and return the empty string at immediate EOF.

## Work item 01a000f166347410b80c3e83f16216b1

Title: Make Unit results silent

Category: behavior

Evidence: C passes/2.6_PrintInsertion.fs:35-98 inserts Print for every return and passes/4_MIR_to_LIR.fs:1185-1191 prints Unit; I packages/darklang/cli/execution/eval.dark:5-18,38-42 represents Unit-like no-output results as None.

Outcome:

Suppress automatic rendering of a final Unit while continuing to render non-Unit results, so output functions do not acquire a trailing "()\n" and ask, confirm, select, and spinner still expose their returned values.

## Work item 01a000f1663473b0803982be6edfe392

Title: Preserve live terminal streams in run mode

Category: behavior

Evidence: C Program.fs:481-489 always calls the capturing executor and only reprints captured output in normal verbosity; CompilerLibrary.fs:1902-1935 redirects stdout/stderr and waits before returning.

Outcome:

Provide an attached execution path for the compiler CLI so child stdin/stdout/stderr remain connected and prompts and progress appear immediately; retain a separate capture path for tests, ensure quiet mode suppresses compiler chatter rather than program output, and propagate child termination status without swallowing partial output.

## Work item 01a000f166347bbaa3c6626b89948bf4

Title: Load the presentation package slice

Category: other

Evidence: C CompilerLibrary.fs:996-1061 loads a fixed flat file list; I sources declare Darklang.Stdlib.Cli.Log and Darklang.Stdlib.Cli.UI.{Color,Progress,Prompt,Spinner,Table}, with Color required by the other modules.

Outcome:

Add compiler-syntax adaptations of the six pinned Dark sources to the stdlib build in dependency order, using fully qualified definitions and private adapters where compiler List/String internals still use Int64 while keeping the public UI signatures based on Int.

## Work item 01a000f166347781a78fc3909bd99f6f

Title: Implement ANSI color and style helpers

Category: function

Evidence: I cli/ui/color.dark:7-68 defines the complete mapping; src/Tests/e2e/upstream/stdlib/cli-color.dark:1-18 already carries exact expected strings but is not enabled.

Outcome:

Provide UI.Color.esc plus red, green, yellow, blue, magenta, cyan, white, gray, bold, dim, italic, underline, strikethrough, bgRed, bgGreen, bgYellow, and bgBlue, each wrapping text as ESC+[code+m, text, ESC+[0m, including empty text and nested-reset behavior.

## Work item 01a000f16634712e9717405a46d7bbbf

Title: Implement stdout logging

Category: function

Evidence: I cli/log.dark:3-24 explicitly states no stderr builtin exists and implements every level with Builtin.printLine.

Outcome:

Provide Log.info, warn, error, debug, and success with colored [INFO], [WARN], [ERROR], [DEBUG], and [OK] prefixes, one separating space, the original message, and exactly one trailing LF; all five write only to stdout.

## Work item 01a000f166347074a16cafb7f5c280d5

Title: Implement progress-bar rendering

Category: function

Evidence: I cli/ui/progress.dark:8-28 contains the complete algorithm and no TTY check or cursor-erasure sequence.

Outcome:

Provide UI.Progress.bar(Int, Int, String): Unit with fixed width 30, # and - fill, truncating integer percentage, a leading carriage return, no newline while current is below total, and one newline when current is at least total; preserve the total-zero and out-of-range arithmetic behavior.

## Work item 01a000f1663473f8adb6f18dafb1b418

Title: Implement prompt interactions

Category: function

Evidence: I cli/ui/prompt.dark:5-38; I Builtins.Pure/Libs/Int.fs:359-379 uses BigInteger.TryParse, while C stdlib/Int.dark:403-412 currently accepts only an optional minus and decimal digits.

Outcome:

Provide Prompt.ask, confirm, and select with exact prompt text and flushing behavior. Confirm accepts only case-insensitive y or yes; select prints bold one-based choices, accepts the pinned Int.parse forms, retries with the exact red error message, and returns the chosen string. Align selection parsing for surrounding whitespace and a leading plus sign while keeping invalid indices on the retry path.

## Work item 01a000f166347025806b143440f0cf78

Title: Implement the baseline static spinner

Category: function

Evidence: I cli/ui/spinner.dark:4-12 explicitly says it is not a real spinner and uses no background thread, clock, TTY detection, or cursor control.

Outcome:

Provide generic Spinner.run(String, Unit -> a): a that writes "<message>... ", invokes the function once, writes green "done" plus LF only on normal return, and returns the untouched result; runtime failure or signal interruption must not fabricate the completion text.

## Work item 01a000f1663472f6ac37afec349f906d

Title: Implement table formatting

Category: function

Evidence: I cli/ui/table.dark:7-68 and I Builtins.Pure/Libs/String.fs:110-123; C stdlib/String.dark:19-22 reports byte length and its grapheme implementation is explicitly simplified at lines 335-402.

Outcome:

Provide Table.print(List<String>, List<List<String>>): Unit with per-column maximum EGC length, two-space separators, a bold header, hyphen separator, left padding, empty cells for short rows, and ignored surplus cells. Route measurement through interpreter-compatible EGC counting rather than UTF-8 byte length, terminal-cell width, or ANSI stripping.

## Work item 01a000f1663471679f490602b3d1479e

Title: Define redirected and interrupted behavior

Category: behavior

Evidence: The six I package files contain no terminal-capability or timing calls; I Stdin.fs:402-403 defines EOF, while Progress.dark:24-28 and Color.dark:7-8 emit control bytes unconditionally.

Outcome:

Keep ANSI and carriage-return bytes unchanged under redirection, make ask return empty and confirm return false at EOF, document select-at-EOF as the pinned retry loop, and retain default OS signal termination with already-written prompt/progress bytes observable. Do not add an isatty branch, animated timer, alternate-screen mode, or cleanup sequence absent from the pinned packages.

## Work item 01a000f1663476ccbaa31f7d9fe22269

Title: Add exact conformance coverage

Category: other

Evidence: E2EFormat.fs:13-34 has no stdin field; E2ETestRunner.fs:984-996 trims stdout/stderr; TestRunner.fs:465-480 omits cli-color from the upstream line allowlist; docs/verification.md defines repository-wide verification.

Outcome:

Represent test stdin as an explicit closed-or-bytes input and output matching as an explicit exact-byte mode instead of trimming. Activate all upstream cli-color cases; add focused E2E matrices for logs, progress boundaries, prompt input/retries, spinner ordering/failure, table shapes/Unicode, Unit silence, stdout/stderr separation, and optimization modes; record revision-stamped same-source redirected and SIGINT probes, then exercise the full suite and routine benchmark profile.
