---
format: 1
event-id: 019ffbfd66d676308f4a79b309a36341
entity-id: 019ff58b47c77dcaa4abc5287cf76501
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-13T16:38:35.9908160+00:00
author: worker:65eb66d2e522:3436513:019ffbf929d7713bbbadb2c500f56323
previous: 019ff7d76bc77acf9579b34ab6620461
base-commit: 30cf16cd8a9f0aa9f93e203ed40693a677eebf28
constraints-hash: e646027115bd63ae6634df307340bf0add1f07691802d085d332d9e6fd77a483
revision: 019ff7d76bc77acf9579b34ab6620461
---
# Breakdown

Independent parity-resolution boundaries for compiler b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 compared with darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d; historical findings at 8a402797 remain discovery leads only.

## Proposed issue 019ffbfd66d57e8d886124e0206f5070

Title: Primitive literals and values

Outcome:

Make unit, Boolean, integer, float, character, and string literals exhibit the interpreter’s accepted forms, types, values, and failures.

Constraints:

- Treat lexing, escape decoding, literal typing, constant lowering, and runtime representation as one boundary.
- Cover numeric range edges, malformed literals, Unicode, and special floating-point values where the language exposes them.

## Proposed issue 019ffbfd66d67cf2a1ed353af2901725

Title: Identifier and keyword grammar

Outcome:

Align identifier acceptance, reserved words, capitalization rules, and qualified-name parsing with the interpreter.

Constraints:

- Keep lexical acceptance distinct from whether a parsed name resolves.
- Include ambiguous token boundaries and keyword use in binding and type positions.

## Proposed issue 019ffbfd66d67deca765f81f04c89074

Title: Operators, precedence, and pipelines

Outcome:

Make operator parsing, associativity, precedence, desugaring, and pipeline behavior match the interpreter.

Constraints:

- Cover operators in prefix, infix, partial-application, and pipeline positions.
- Keep primitive operator evaluation in the same child when syntax determines invocation semantics.

## Proposed issue 019ffbfd66d67f279e4a6e6230712fd7

Title: Bindings, scopes, and shadowing

Outcome:

Align local binding visibility, nested scopes, shadowing, and duplicate-binding behavior with the interpreter.

Constraints:

- Include destructuring bindings and scope across conditional and match branches.
- Do not absorb recursion semantics unless ordinary scope rules alone cause the difference.

## Proposed issue 019ffbfd66d679708c68513248589d95

Title: Recursion and recursive groups

Outcome:

Support the interpreter’s recursive function and value-binding behavior, including mutually recursive declarations where applicable.

Constraints:

- Model recursive availability explicitly through name resolution, typing, and lowering.
- Keep tail-call optimization out except where it changes observable stack behavior.

## Proposed issue 019ffbfd66d67c16b5a1c97467556827

Title: Functions and application

Outcome:

Align lambda syntax, parameter handling, currying, partial application, arity checking, and call evaluation with the interpreter.

Constraints:

- Include annotated and destructured parameters when supported by the baseline.
- Keep named-function lookup separate from closure capture and environment representation.

## Proposed issue 019ffbfd66d67f00b3ce4dde9b423b00

Title: Closure capture semantics

Outcome:

Make closures capture, retain, and invoke lexical environments with interpreter-equivalent behavior.

Constraints:

- Cover nested closures, shadowed captures, returned closures, and captures crossing recursive calls.
- Include observable lifetime and aliasing behavior without prescribing a backend representation.

## Proposed issue 019ffbfd66d6785689c6f64f01e4ad7b

Title: Conditional and sequential evaluation

Outcome:

Align condition typing, branch typing, evaluation order, short-circuiting, and observable failure timing.

Constraints:

- Include side-effecting builtins only as probes of order and branch selection.
- Keep diagnostics text outside scope unless programs can observe it.

## Proposed issue 019ffbfd66d67b188090d0372ff1f12a

Title: Qualified names and callable resolution

Outcome:

Resolve local, module-qualified, package, builtin, constructor, and type names with the interpreter’s precedence and ambiguity behavior.

Constraints:

- Represent name kinds explicitly rather than inferring them from spelling.
- Include collisions between values, functions, constructors, and types.

## Proposed issue 019ffbfd66d674aea19a87a8ea6f249e

Title: Tuple language support

Outcome:

Align tuple construction, typing, access, destructuring, matching, and runtime behavior with the interpreter.

Constraints:

- Cover unit and grouping ambiguities at the parser boundary.
- Keep tuple arity and element-order behavior consistent through ANF and runtime lowering.

## Proposed issue 019ffbfd66d67949ae0ce2b2e369e13f

Title: List language support

Outcome:

Align list literals, cons forms, typing, matching, equality, and evaluation behavior with the interpreter.

Constraints:

- Cover empty-list inference, heterogeneous rejection, nested patterns, and construction order.
- Treat library collection functions as separate callable inventory unless syntax or core list representation requires them.

## Proposed issue 019ffbfd66d67e41a4f9c07e1dd39f1e

Title: Record language support

Outcome:

Align record declaration, construction, field access, update, typing, equality, and pattern behavior with the interpreter.

Constraints:

- Cover field order, duplicate and missing fields, nominal versus structural identity, and nested access.
- Keep each record feature connected across parser, type checker, lowering, and runtime representation.

## Proposed issue 019ffbfd66d67b6ea12838c99bfe374e

Title: Algebraic data types and constructors

Outcome:

Align user-defined type declarations, constructor resolution, payload typing, construction, and runtime identity with the interpreter.

Constraints:

- Cover nullary and payload constructors, generic parameters, duplicate constructors, and cross-type name collisions.
- Keep constructor representation internal unless it changes equality, matching, or printed observable values.

## Proposed issue 019ffbfd66d6762f98d1ba30d543506b

Title: Pattern matching

Outcome:

Make supported patterns, binding rules, guards, exhaustiveness behavior, branch selection, and match failures agree with the interpreter.

Constraints:

- Include literals, variables, wildcards, tuples, lists, records, constructors, nesting, and alternative patterns only where the baseline supports them.
- Preserve left-to-right matching and guard evaluation where observable.

## Proposed issue 019ffbfd66d67b19ad3a0b4ce3ccd5c9

Title: Generic types and polymorphism

Outcome:

Align type-variable inference, generalization, instantiation, annotations, and generic user-type behavior with the interpreter.

Constraints:

- Cover value restriction or equivalent generalization boundaries if present.
- Keep monomorphization as an implementation detail while preserving source-level polymorphism.

## Proposed issue 019ffbfd66d678e4b57b549f75292dcf

Title: Equality, ordering, and comparability

Outcome:

Make equality and comparison acceptance and results match the interpreter for every supported value category.

Constraints:

- Define behavior for nested collections, records, constructors, functions, and floating-point edge cases.
- Reject unsupported comparisons during the same language phase as the baseline when phase is observable.

## Proposed issue 019ffbfd66d67e51a304a5e9aaac673a

Title: Program declarations and entry structure

Outcome:

Align accepted top-level declarations, declaration visibility, ordering, entry-point selection, and whole-program validation with the interpreter.

Constraints:

- Cover interaction among top-level values, functions, types, and modules.
- Keep command-line packaging and native executable layout outside this language boundary.

## Proposed issue 019ffbfd66d67e04bde2f8cdc12d3348

Title: Builtin and package callable surface

Outcome:

Provide interpreter-equivalent callable availability, signatures, arities, type behavior, and observable results for the language-facing builtin surface.

Constraints:

- Group overloads belonging to one semantic callable family in one resolution child.
- Split platform services only when they form independently testable language modules rather than individual missing functions.

## Proposed issue 019ffbfd66d674fa884cdddd5e651ba3

Title: Observable runtime failure semantics

Outcome:

Align language-visible failures such as invalid arithmetic, non-exhaustive matches, and invalid runtime operations with the interpreter.

Constraints:

- Distinguish typed rejection from failures produced by a well-typed running program.
- Do not require identical internal exception mechanisms, process diagnostics, or stack traces unless source programs can observe them.

## Proposed issue 019ffbfd66d67b20a4621e9413060df1

Title: Compiler extension dialect boundary

Outcome:

Keep selected compiler-only language extensions available only through an explicit dialect while preserving an interpreter-compatible public surface.

Constraints:

- Classify extensions by syntax and semantics rather than individual examples.
- Ensure dialect selection occurs before parsing and cannot silently change within a source unit.
- Remove extensions with no retained use case instead of assigning them implicit fallback behavior.
