---
format: 1
event-id: 019ffc28606e7d0492b6cf485b1957d7
entity-id: 019ff58b47c77dcaa4abc5287cf76501
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-13T17:25:32.3988013+00:00
author: worker:65eb66d2e522:3456704:019ffc1c5d2e72c2b5e7572e39b03b33
previous: 019ff7d76bc77acf9579b34ab6620461
base-commit: c3e10fb2ed79d8955d0545134e96839225ee3fe7
constraints-hash: e646027115bd63ae6634df307340bf0add1f07691802d085d332d9e6fd77a483
revision: 019ff7d76bc77acf9579b34ab6620461
---
# Breakdown

Re-audit compiler b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899 against darklang/dark 04fbe9dcc995c6188757d583e273cbd30a3e2d3d, using the interpreter’s public syntax and behavior as the sole parity surface. DCB1 report 8a402797 and existing parity documents are discovery leads only. Each child owns its feature across parser, resolution, typing, lowering, runtime, focused e2e coverage, and migration of affected compiler-authored programs; legacy compiler syntax is removed rather than retained as a dialect.

## Proposed issue 019ffc28606e7f3e978b36ad2a4c3c61

Title: Primitive literals and values

Outcome:

Replace compiler literal forms and behavior with the interpreter’s unit, Boolean, integer, float, character, and string syntax and semantics.

Constraints:

- Keep lexing, escape decoding, parsing, typing, constant lowering, and runtime representation in this boundary.
- Cover numeric limits, malformed tokens, Unicode and escapes, and interpreter-exposed floating-point special values.
- Use src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, src/DarkCompiler/passes/2_AST_to_ANF.fs, and src/DarkCompiler/Runtime.fs as compiler evidence anchors.
- Convert literal-bearing e2e, stdlib, example, and documentation sources; delete compatibility parsing for superseded compiler forms.

## Proposed issue 019ffc28606e7cd6aac4df8892439c6e

Title: Identifier and keyword grammar

Outcome:

Accept only the interpreter’s identifier, reserved-word, capitalization, and qualified-name grammar.

Constraints:

- Keep lexical acceptance separate from resolution of the resulting name.
- Cover identifiers in value, binding, parameter, type, constructor, field, and qualification positions, including ambiguous token boundaries.
- Anchor compiler evidence in src/DarkCompiler/passes/1_Parser.fs and focused parser-facing e2e cases.
- Convert affected compiler-authored sources and remove legacy identifier or keyword aliases.

## Proposed issue 019ffc28606e70bd9b6181ab981fe0dd

Title: Operators, precedence, and pipelines

Outcome:

Replace compiler operator syntax and desugaring with the interpreter’s operators, precedence, associativity, partial application, and pipeline behavior.

Constraints:

- Treat prefix, infix, parenthesized, partially applied, and pipeline positions as one grammar boundary.
- Keep primitive operator invocation semantics here when parsing determines argument order or evaluation behavior.
- Anchor compiler evidence in src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/2_AST_to_ANF.fs, and operator runtime definitions.
- Convert all affected expressions and remove legacy operator tokens, precedence rules, and desugarings.

## Proposed issue 019ffc28606e7ad2a97052245ed06634

Title: Bindings, scopes, and shadowing

Outcome:

Implement interpreter-syntax bindings with matching visibility, destructuring, shadowing, and duplicate-binding behavior.

Constraints:

- Cover nested expressions and scope boundaries in conditionals, matches, lambdas, and sequential expressions.
- Exclude recursive availability except where ordinary non-recursive scope rules determine the result.
- Anchor compiler evidence in src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, and src/DarkCompiler/passes/2_AST_to_ANF.fs.
- Convert binding syntax throughout the repository and remove compiler-specific binding forms.

## Proposed issue 019ffc28606e7f7d852f3313dba447cc

Title: Recursion and recursive groups

Outcome:

Implement the interpreter’s recursive declaration syntax and observable recursive and mutually recursive binding semantics.

Constraints:

- Represent recursive availability explicitly through resolution, typing, and lowering rather than spelling conventions or sentinels.
- Cover functions, values, mutual groups, shadowing, and invalid recursive definitions exactly where the interpreter exposes them.
- Treat stack behavior as parity evidence only when it changes whether a valid program completes.
- Anchor compiler evidence in the parser, type checker, ANF conversion, and recursion e2e corpus; migrate and remove legacy recursive syntax.

## Proposed issue 019ffc28606e7f70b3353086183767ef

Title: Functions, application, and closures

Outcome:

Replace compiler function syntax and call behavior with the interpreter’s lambdas, declarations, currying, partial application, arity rules, and lexical closures.

Constraints:

- Keep parameter parsing, annotations, destructuring, application typing, closure capture, and invocation in one deliverable boundary.
- Cover nested and returned closures, shadowed captures, recursive calls, over-application, under-application, and evaluation order.
- Anchor compiler evidence in src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, src/DarkCompiler/passes/2_AST_to_ANF.fs, and closure/runtime support.
- Convert function declarations, lambdas, and calls across tests, stdlib, examples, and docs; remove legacy function syntax and call fallbacks.

## Proposed issue 019ffc28606e77bd99aff48497316fbc

Title: Conditional and sequential evaluation

Outcome:

Implement the interpreter’s conditional and sequencing syntax with equivalent typing, branch selection, evaluation order, and failure timing.

Constraints:

- Cover condition typing, branch unification, nested conditionals, sequence result typing, and skipped-branch behavior.
- Use effectful or failing builtins only as focused probes of order and branch selection.
- Anchor compiler evidence in the parser, type checker, ANF conversion, Runtime.fs, and focused e2e cases.
- Convert affected source expressions and remove compiler-only conditional or sequencing forms.

## Proposed issue 019ffc28606e7fafaa108fd022edf615

Title: Qualified names and callable resolution

Outcome:

Resolve interpreter-syntax local, module, package, builtin, constructor, value, function, and type names with matching precedence and ambiguity behavior.

Constraints:

- Model name kinds explicitly and cover collisions among namespaces without spelling-based fallbacks.
- Keep qualified-name token grammar in the identifier child while owning semantic lookup of parsed names here.
- Anchor compiler evidence in src/DarkCompiler/passes/1.5_TypeChecking.fs, src/DarkCompiler/passes/2_AST_to_ANF.fs, src/DarkCompiler/Stdlib.fs, and resolution e2e cases.
- Remove compiler-only resolution shortcuts and convert affected qualifications and callable references.

## Proposed issue 019ffc28606e70588b4722f9c18781ec

Title: Tuple language support

Outcome:

Implement interpreter tuple syntax and equivalent construction, typing, access, destructuring, matching, equality, and runtime values.

Constraints:

- Own tuple arity and element order through parsing, typing, ANF, matching, lowering, and runtime representation.
- Cover grouping ambiguity here while leaving the unit literal’s value semantics in the primitive-literal child.
- Anchor compiler evidence in the parser, type checker, ANF conversion, runtime representation, and tuple e2e cases.
- Convert tuple-bearing repository sources and remove superseded tuple syntax or access forms.

## Proposed issue 019ffc28606e7c8f905634646ee82aa9

Title: List language support

Outcome:

Implement the interpreter’s list and cons syntax with equivalent typing, construction, matching, equality, and evaluation.

Constraints:

- Cover empty-list inference, heterogeneous rejection, nested patterns, cons associativity, and construction order.
- Keep library collection callables outside this boundary unless core list representation or syntax requires them.
- Anchor compiler evidence in the parser, type checker, ANF conversion, Runtime.fs, and list e2e cases.
- Convert list-bearing repository sources and remove compiler-only list or cons forms.

## Proposed issue 019ffc28606e7fc794293cfcbbad8bb9

Title: Record language support

Outcome:

Implement interpreter record syntax and equivalent declaration, construction, access, update, typing, equality, patterns, and runtime identity.

Constraints:

- Cover field order, duplicate and missing fields, nested access and update, and nominal versus structural identity.
- Keep parser, type checker, lowering, matching, equality, and runtime representation changes together.
- Anchor compiler evidence in the parser, type checker, ANF conversion, Runtime.fs, and record e2e cases.
- Convert record-bearing sources and remove legacy record declaration, literal, access, update, or pattern syntax.

## Proposed issue 019ffc28606e7018bf180009b3302866

Title: Algebraic data types and constructors

Outcome:

Implement the interpreter’s user-defined type and constructor syntax with matching resolution, payload typing, construction, and runtime identity.

Constraints:

- Cover nullary and payload constructors, generic parameters, duplicate definitions, and constructor collisions across types.
- Keep internal representation unconstrained except where equality, matching, or observable value rendering differs.
- Anchor compiler evidence in src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, src/DarkCompiler/passes/2_AST_to_ANF.fs, and ADT e2e cases.
- Convert type and constructor syntax throughout the repository and remove compiler-specific declaration or construction forms.

## Proposed issue 019ffc28606e7750b64e78554d1f9baa

Title: Pattern matching

Outcome:

Implement interpreter pattern and match syntax with equivalent bindings, guards, branch selection, exhaustiveness behavior, and match failures.

Constraints:

- Inventory only patterns supported at the pinned interpreter revision, including applicable literal, variable, wildcard, tuple, list, record, constructor, nested, and alternative forms.
- Preserve observable left-to-right matching, binding availability, and guard evaluation.
- Anchor compiler evidence in the parser, type checker, ANF conversion, Runtime.fs, and match e2e cases.
- Convert match expressions and patterns across repository sources and remove unsupported compiler-only pattern syntax.

## Proposed issue 019ffc28606e7f12b9aa293e35fe43a7

Title: Generic types and polymorphism

Outcome:

Match the interpreter’s type-variable syntax and source-level inference, generalization, instantiation, annotation, and generic user-type behavior.

Constraints:

- Cover generalization boundaries, recursive definitions, constrained uses, annotations, and generic constructors as exposed by the interpreter.
- Keep monomorphization internal while preserving all observable polymorphic behavior.
- Anchor compiler evidence in src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, src/DarkCompiler/passes/2_AST_to_ANF.fs, and generic e2e cases.
- Convert type annotations and parameter syntax and remove compiler-only generic syntax or implicit fallback rules.

## Proposed issue 019ffc28606e7ebea63357e862da7c00

Title: Equality, ordering, and comparability

Outcome:

Make interpreter-spelled equality and comparison operations accept and evaluate the same supported value categories.

Constraints:

- Cover primitives, floating-point edge cases, tuples, lists, records, constructors, nested values, and function values.
- Match the interpreter’s rejection phase when a comparison is invalid and that phase is observable.
- Anchor compiler evidence in the type checker, operator lowering, Runtime.fs, and focused comparison e2e cases.
- Use only interpreter operator syntax in migrated probes and remove compiler-specific comparison aliases.

## Proposed issue 019ffc28606e7e02be068956dbbc3a49

Title: Program declarations, modules, and entry structure

Outcome:

Accept the interpreter’s program-level syntax and match declaration visibility, ordering, module structure, entry selection, and whole-program validation.

Constraints:

- Cover interactions among top-level values, functions, types, constructors, modules, and package-qualified references.
- Exclude native packaging and executable-file layout unless they affect source-level entry behavior.
- Anchor compiler evidence in src/DarkCompiler/CompilerLibrary.fs, src/DarkCompiler/passes/1_Parser.fs, src/DarkCompiler/passes/1.5_TypeChecking.fs, and whole-program e2e cases.
- Convert repository programs to interpreter declaration and module syntax and remove legacy top-level forms and implicit entry fallbacks.

## Proposed issue 019ffc28606e7e9994da8d61b20204e2

Title: Builtin and package callable surface

Outcome:

Expose the interpreter’s callable names and application syntax with matching availability, signatures, arities, type behavior, results, and language-visible failures.

Constraints:

- Inventory callable families at the pinned revisions; treat individual missing functions, types, overloads, and result differences as contained work within this child.
- Group platform services only by independently testable language module, not by individual callable.
- Anchor compiler evidence in src/DarkCompiler/Stdlib.fs, src/DarkCompiler/Runtime.fs, the type checker, and builtin/package e2e cases.
- Convert repository call sites to interpreter names and syntax; remove compiler-only callable aliases unless classified as a non-public internal facility.
- Keep performance-only implementation differences out of the callable inventory.
