---
format: 1
event-id: 019ff66981d879e7a2aa61b822b6c1fd
entity-id: 019ff64a76b0791a8c7e62ff3cd36f64
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-12T14:38:57.4960093+00:00
author: worker:65eb66d2e522:40713:019ff640f1e77ef78c5242339394e833
previous: 019ff64a76b275868b818161133bbaf7
base-commit: 46dad963cd3b9dee7e7f87290ef84ab25efd20a1
constraints-hash: 98d1cfda34da7489d2bdbe84dedcef95e0eb3f4deea629a6047eb69fcb8d8d4c
revision: 019ff64a76b275868b818161133bbaf7
---
# Work plan

MIR DCE currently performs two full CFG analysis scans per fixed-point iteration: one builds destination-to-use lists and one gathers side-effect/control-flow roots. The requested optimization is precise, confined to `3.5_MIR_Optimize.fs`, and needs no child issue.

## Work item 019ff66981d77fad88ad01648c09c9fa

Title: Fuse MIR DCE input collection into one CFG traversal

Category: function

Evidence: `3.5_MIR_Optimize.fs:930-955` contains separate `Map.fold`/instruction scans in `buildDefUseMap` and `collectRootUses`; `collectLiveDestinations` immediately consumes both at lines 958-984, and DCE runs inside the optimizer fixed point at line 1895. `CompilerLibrary.fs:240-260` exposes `-vv` MIR Optimizations timing, while compilation total is emitted near line 1540. `MIROptimizeTests.fs:300-369` already exercises cyclic dead-phi liveness. The routine profile lists 19 programs, including fasta. Historical non-ancestor commit `11b29ca3` demonstrates the same traversal fusion for the older Set-based representation, so it must be adapted to the current fold/list implementation rather than copied unchanged.

Outcome:

After separate human approval, temporarily phase-time DCE input collection, live-set propagation, and rewriting on fasta; remove that instrumentation and proceed only if analysis is material. Capture uninstrumented baselines, then replace `buildDefUseMap` and `collectRootUses` with one private functional collector that traverses each block and instruction once, invokes the existing use fold once per instruction, preserves the current `Map<VReg,VReg list>` contents and use ordering, accumulates roots only from side-effecting instructions plus terminators, and leaves the live-destination worklist and removal semantics unchanged. Prove identical live-destination sets with temporary comparison instrumentation, then remove it. Require byte-identical optimized MIR dumps and binaries plus identical benchmark output. Under comparable warmed, non-concurrent conditions, collect 10 raw before/after fasta `-vv` MIR Optimizations timings and 10 raw before/after `./run-tests --ai` wall-clock timings; report medians, absolute and percentage deltas, and reject neutral or noisy results. Also report before/after MIR Optimizations and total-compilation deltas for every routine-profile program. Finally run `./run-tests --ai` and `./benchmarks/run_benchmarks.sh --verify routine`, reporting every instruction-count result and the performance ratio from `benchmarks/RESULTS.md`; retain no profiling or exploratory instrumentation.
