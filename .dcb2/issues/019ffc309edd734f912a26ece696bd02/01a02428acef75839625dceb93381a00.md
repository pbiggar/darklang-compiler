---
format: 1
event-id: 01a02428acef75839625dceb93381a00
entity-id: 019ffc309edd734f912a26ece696bd02
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-21T11:50:40.6236806+00:00
author: worker:65eb66d2e522:317139:01a02410871c770fa34dac650f2e7364
previous: 019ffc3515e07bcabc344cb3f8af41c6
base-commit: 8f05b3d12cf936223c085e8f43b318d39b6e5cc2
constraints-hash: d01b8dad1b8c061fc41699cd97b45db6374c3eec6bbafb4d534f2f0148b6d6e5
reply-to: 01a024256b1a7cc9b0e5a47157d09c30
revision: 019ffc3515e07bcabc344cb3f8af41c6
---
# Implementation plan

Contained operator-expression parity work, using compiler `b2e1f3d1e4ce0338d4c4662db9a1326f2e2cb899` and interpreter `04fbe9dcc995c6188757d583e273cbd30a3e2d3d` as historical evidence revisions; current revalidation was performed at compiler HEAD `8f05b3d12cf936223c085e8f43b318d39b6e5cc2`.

## Work item 01a02428acef74b0bdde687d40f9e2f7

Title: Create a pinned operator parity corpus

Category: tests

Evidence: Current `src/Tests/e2e/list_language_parity.e2e:15,27` already covers right-associated append and left-first failure; `src/Tests/e2e/closures.e2e:313-345` covers ordinary partial application; `src/Tests/e2e/stdlib/tuple2.e2e:30-79` retains obsolete partial-pipe limitations. Interpreter `backend/src/LibParser/GRAMMAR.md:92-115,198-211` and `Parser.fs:757-815,1444-1526,2109-2133` define the source baseline.

Outcome:

Add focused same-source e2e/parser coverage for accepted and rejected tokens; precedence and associativity; unary-minus/application grouping; ordinary two-argument operator values; partial application; valid and invalid pipe segments; supplied-argument order; left-to-right evaluation; arity; and observable failure timing. Each comparison records the exact compiler HEAD and interpreter revision, and migrated stdlib expressions replace documented unsupported partial-pipe workarounds.

## Work item 01a02428acef76fca13ed8c278703b3c

Title: Align the public operator parser and section forms

Category: behavior

Evidence: Compiler `src/DarkCompiler/passes/1_Parser.fs:1669-1809,1811-1880,2174-2192` immediately rewrites pipes, makes comparisons non-associative, retains bitwise tiers, and implements only one-argument `&&`/`||` sections. Interpreter `backend/src/LibParser/GRAMMAR.md:94-118` and `Parser.fs:1447-1526,1857-1886,2078-2133` specify the contrasting grammar. Current compiler `parseListAppend` is already right-recursive, so no append associativity rewrite is required.

Outcome:

Implement one interpreter-aligned precedence table: left-associative pipe, boolean, comparison, additive, and multiplicative operators; right-associative `@` and `^`; and application tighter than unary minus where nonliteral `-f x` groups as `-(f x)`. Parse every retained `(op)` as a two-argument value, and recognize `x |> (op) y` as infix application with `x` as the left operand. Reject expression-level `<<`, `>>`, `&`, `|||`, `~~~`, and `!` as reserved unsupported syntax; preserve internal IR/runtime bit operations that are not reachable from those tokens.

## Work item 01a02428acef7d9aa704a8712874d8e4

Title: Canonicalize operator and pipeline desugaring before ANF

Category: behavior

Evidence: Compiler `1_Parser.fs:1680-1705` accepts arbitrary RHS expressions through `Apply`; interpreter `Parser.fs:767-815` uses structured pipe segments and emits `PARSE-PIPE-SEGMENT` otherwise. Compiler `src/DarkCompiler/passes/2_AST_to_ANF.fs:2647,4039-4075,4519-4526,5411-5421,9359-9369` names `^` as `BitXor`, dispatches only `Int` to `Stdlib.Int.power`, and retains multiple legacy primitive paths. Interpreter `Parser.fs:58-75` maps `^` to `ArithmeticPower`; its Builtins/ProgramTypes power mapping is the invocation evidence.

Outcome:

Replace parser-specific call/TypeApp/Apply pipe rewrites with one validated desugaring path that preserves interpreter pipe-segment eligibility, first-argument insertion, generic arguments, capture-once partial application, and left-to-right evaluation. Give `^` one power representation through type checking and both ANF conversion paths; remove source-level legacy bitwise aliases and primitive dispatches while retaining named AOT-supported functions and backend-only primitives. Keep equality and ordering value semantics delegated to comparability.

## Work item 01a02428acef7e19ab66e85ff24ded00

Title: Record remaining AOT-only distinctions and update operator guidance

Category: documentation

Evidence: `docs/current-language-features.md:25-36` still advertises legacy bitwise expression operators; `docs/darklang-differences.md:259-288` lists extensions that require classification; `src/Tests/e2e/stdlib/tuple2.e2e:30-79` contains stale partial-pipe guidance. `src/DarkCompiler/Runtime.fs` and `src/DarkCompiler/Stdlib.fs` must be cited for any retained primitive/named-function surface.

Outcome:

Update the operator-facing language and parity documentation with the interpreter token table, desugaring contract, rejected legacy syntax, retained named-function alternatives, and any explicit AOT-only static rejection timing that cannot match interpreter runtime timing. Remove stale comments that describe partial-pipe syntax as unsupported.

## Conversation response

I narrowed the earlier proposal against current HEAD `8f05b3d12cf936223c085e8f43b318d39b6e5cc2`. `@` is already parsed right-associatively, and `BitXor` already dispatches `Int` operands to `Stdlib.Int.power`; the remaining work is to make the public grammar and all lowering paths consistently reflect that interpreter contract, while removing source-level legacy operators without disturbing backend-internal bit operations.
