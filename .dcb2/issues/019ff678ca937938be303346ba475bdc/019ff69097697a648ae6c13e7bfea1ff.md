---
format: 1
event-id: 019ff69097697a648ae6c13e7bfea1ff
entity-id: 019ff678ca937938be303346ba475bdc
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-12T15:21:38.9214977+00:00
author: worker:65eb66d2e522:40713:019ff640f1e77ef78c5242339394e833
previous: 019ff678ca997c80a4e31950d6706a84
base-commit: 0d0dcd70e2aecb7ec71d36e36e2740e5ecda3489
constraints-hash: a0fd572e73dadb115a208b1e601b6dc16822a9d9fb1cca7ea70d661e0b886b7c
revision: 019ff678ca997c80a4e31950d6706a84
---
# Work plan

MIR_to_LIR.toLIR currently runs whole-program parameter and call-limit checks before initTempState separately scans each function for VReg maxima; diagnostics prioritize the first parameter error globally, then the first call error in function, Map block, and instruction order.

## Work item 019ff69097697e0590f39477d9f5c28e

Title: Fuse MIR→LIR preflight traversal

Category: behavior

Evidence: src/DarkCompiler/passes/4_MIR_to_LIR.fs defines maxVRegIdFromInstr/maxVRegIdFromTerminator and initTempState at lines 1591–1732, separate checkParameterLimits/checkCallArgLimits at lines 1898–1944, and invokes all three from toLIR at lines 1948–1963. CompilerLibrary.fs records and prints the MIR -> LIR pass and total compilation timings; IRSymbolTests.fs already directly exercises MIR_to_LIR.toLIR but has no register-limit ordering coverage; verification.md requires ./run-tests --ai and ./benchmarks/run_benchmarks.sh --verify routine, whose committed RESULTS.md contains 19 routine instruction-count baselines.

Outcome:

After separate human approval, add characterization coverage for register maxima and every parameter/direct/tail/indirect/closure call limit diagnostic, including global parameter-before-call and integer-before-float precedence. Replace checkParameterLimits, checkCallArgLimits, and the CFG portion of initTempState with one deterministic per-function preflight fold seeded from typed parameters and preserving the existing FloatRegs-derived FP temporary boundary. Accumulate separate first parameter and first call errors so toLIR retains current whole-program diagnostic ordering, and pass the computed TempState directly into conversion. Retain no exploratory timers or instrumentation. Compare byte-identical LIR dumps, exact diagnostic text/order, binaries and program results; run the full test and routine benchmark verification with unchanged instruction counts. On the same warmed, otherwise-idle setup, report 10 raw before/after fasta -vv MIR→LIR samples with medians and absolute/percentage deltas, before/after MIR→LIR and total-compilation timings and deltas for all 19 routine programs, and 10 raw before/after wall-clock ./run-tests --ai samples with medians and absolute/percentage deltas. Reject and remove the implementation if results are neutral, overlapping/noisy, or regressive.
