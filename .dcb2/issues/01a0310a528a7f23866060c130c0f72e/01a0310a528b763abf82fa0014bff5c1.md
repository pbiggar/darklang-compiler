---
format: 1
event-id: 01a0310a528b763abf82fa0014bff5c1
entity-id: 01a0310a528a7f23866060c130c0f72e
entity-kind: issue
event-type: created
occurred-at: 2026-08-23T23:52:35.2115118+00:00
author: worker:65eb66d2e522:3367166:01a03103e1f27b488855a027865db44f
batch: 01a0310a508d7c26a1ffc5aa4d0da25e
problem: 019ffcba981b77209b745b1ac3cd7d26
workflow: trial-first
---
# Specialize uniquely-owned edigits chunk lists

Remove persistent-list traversal and path-copy churn from edigits’ closed integer update loops without changing List semantics.

## Constraints

- Confirm the current packed edigits IR still performs SkewList getAt/setAt in divideByN and addWithCarry before changing code.
- Limit specialization to statically proven uniquely-owned monomorphic Int64 lists in the closed edigits call graph; retain ordinary List lowering for aliases, escapes, unknown calls, returns, and managed elements.
- Do not add a language-visible mutable Array API or alter observable List identity, ordering, bounds, ownership, or generic behavior.
- Use focused ANF/MIR/LIR and assembly evidence to show direct indexed buffer operations replace the targeted traversals and path copies.
- Create failing focused tests first; run the full test suite and routine benchmark profile, retaining only a material edigits improvement with no meaningful regressions.
- Record retained or rejected evidence in the edigits investigation and rejected-experiments notes, and remove temporary instrumentation and inputs.
- Execution preference: frontier

## Trial work

### Trial item 01a0310a528a7e2992e204627c31568f

Title: Prove unique buffer eligibility

Category: escape and ownership analysis

Evidence: Current results show edigits at 4,528,733,705 instructions (332x Rust), while its source repeatedly performs getAt/setAt on packed Int64 chunks; active notes cover fusion of those operations but not a uniquely-owned buffer representation.

Outcome:

Establish the exact edigits list lifetimes, aliases, and call boundaries that permit private mutable-buffer lowering.

### Trial item 01a0310a528a7a96ab761bce672ad3b2

Title: Trial private Int64 buffer lowering

Category: ANF-to-MIR specialization and code generation

Evidence: The current investigation identifies persistent SkewList lookup and update as the dominant remaining cost; a prior single-traversal read-update fusion regressed, so this trial tests the distinct representation-level cause.

Outcome:

Lower only proven edigits chunk-list update loops to direct bounds-checked indexed loads and stores, then measure generated-code reduction.
