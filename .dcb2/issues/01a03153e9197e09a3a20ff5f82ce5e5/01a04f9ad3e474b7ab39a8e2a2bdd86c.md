---
format: 1
event-id: 01a04f9ad3e474b7ab39a8e2a2bdd86c
entity-id: 01a03153e9197e09a3a20ff5f82ce5e5
entity-kind: issue
event-type: trial-result
occurred-at: 2026-08-29T22:19:01.9885245+00:00
author: worker:65eb66d2e522:1294542:01a04f91895b7e56b8124b4784cbdbbb
previous: 01a03153e91975678c9e9f0f14962c22
attempt: 01a04f99235a717a969dc8b48c848f95
constraints-hash: 557caf190fe93137693ebc2e50acd48484027a09b97ef8589859345bfbb3df95
result: no-improvement
revision: 01a03153e91975678c9e9f0f14962c22
---
# Trial result

The reconstructed trial confirms that constants 6, 10, and 12 provide no instruction-count improvement over the pinned baseline.

## Evidence

## Problems

Lower additional profitable Int64 constant multiplications without changing wrapping semantics.

## Solution

The pinned baseline recognizes only 2^k ± 1 constants and lowers them to shift plus add/sub. Constants 6, 10, and 12 require two shifted terms, so lowering them needs at least two arithmetic/shift operations and does not reduce instruction count. The focused LIR suite passed. No coherent positive candidate existed, so no checkpoint, documentation change, or commit was created.

## Implementation

- **No implementation retained** — **Reason:** exact wrapping signed-digit forms require at least two operations, matching the existing materialized-constant multiply path.

### Algorithm

1. Match a materialized Int64 constant immediately followed by multiply. 2. Accept only exact wrapping identities. 3. For 6, 10, and 12, decompose into two powers of two. 4. Preserve operand order and live-temporary constraints. 5. Reject the transformation because its minimum instruction count is not below baseline.

### Example

For `x * 6`, the baseline is `Mov k, 6; Mul dst, x, k`, requiring two core instructions. An exact alternative such as `(x << 2) + (x << 1)` requires two shifted terms and cannot reduce that count; therefore the candidate is neutral.

## Results

### Evidence gates

| Gate | Status |
| --- | --- |
| Focused measurement | no measured improvement |
| Exact-candidate repository verification | Not run |
| Canonical routine benchmark | missing |

### Optimization proof

| Case | Command | Property | Baseline | Candidate | Result |
| --- | --- | --- | ---: | ---: | --- |
| signed-digit constants 6, 10, and 12 | ./run-tests --ai --filter='LIR peephole tests' | Focused peephole correctness remains valid while candidate instruction accounting is compared against the pinned baseline. | Pinned baseline: materialized constant plus multiply, two core instructions. | Candidate: two shifted terms plus arithmetic combination, at least two core instructions. | neutral |

### Benchmarks

| Benchmark | Metric / command | Before | After | Change | Result |
| --- | --- | ---: | ---: | ---: | --- |
| Not separately reported | — | — | — | — | not reported |

### Focused tests

| Test | Command | Result | What it proves |
| --- | --- | --- | --- |
| LIR peephole unit suite | ./run-tests --ai --filter='LIR peephole tests' | passed | 13/13 tests passed, including live-temporary exclusion coverage. |
| generated ARM64/x64 candidate tests | Not run | not required | No candidate lowered behavior survived the deterministic instruction-count proof. |

### All tests

| Command | Result | Details |
| --- | --- | --- |
| ./run-tests --ai | Not run | The trial retained no candidate change. |

### Trial work items

| Work item | Result | Summary | Evidence |
| --- | --- | --- | --- |
| Trial signed-digit multiply lowering | neutral | Signed-digit lowering for uncovered common constants did not improve instruction count. | Constants 6, 10, and 12 require two shifted terms; focused LIR tests passed 13/13. |

## Notes

Worktree is clean. The prior temporary fixture was not recovered because it was removed after the neutral result. Repository-wide validation remains for DCB.
