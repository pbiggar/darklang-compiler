---
format: 1
event-id: 019ff695907e75848673b5241b52e20a
entity-id: 019ff678ca937dc787e7db497477267a
entity-kind: issue
event-type: analyzed
occurred-at: 2026-08-12T15:27:04.8303534+00:00
author: worker:65eb66d2e522:40713:019ff640f1e77ef78c5242339394e833
previous: 019ff678ca987c3092e7c9f40b62f8a9
base-commit: f860659cdbb7e2355d548055a58768cd1a357fa8
constraints-hash: c69decfe76e6887e077ccf40be3c0cc5682ac0c4238e713ec19735c471f6d225
revision: 019ff678ca987c3092e7c9f40b62f8a9
---
# Work plan

ANF→MIR’s existing whole-program TempId scan traverses every function to size the shared type lookup, but function conversion then rescans each function to initialize fresh MIR registers.

## Work item 019ff695907e7175b554593bdcb3bdcd

Title: Reuse program-scan TempId maxima

Category: function

Evidence: src/DarkCompiler/passes/3_ANF_to_MIR.fs:249-265 computes only a scalar program maximum after scanning every function; :1983-2005 repeats parameter and body maximum scans inside convertANFFunction; :2114-2127 and :2190-2202 perform the program scan before converting those same functions. The convertANFFunction TypeMap parameter at :1973 is otherwise unused. src/Tests/passes/anf2mir provides exact MIR snapshots; CompilerLibrary.fs:325-342 exposes ANF→MIR timing and :1540 total compilation timing; docs/verification.md requires the full test and routine benchmark commands, and benchmarks/RESULTS.md records 19 routine instruction-count baselines.

Outcome:

Treat the current duplicate traversal as the failing performance case. Temporarily profile the whole-program scan and conversion-side rescans on fasta, predeclare a materiality/noise rule, remove the instrumentation, and stop if the rescans are immaterial. If material, obtain explicit individual human approval; this plan is not approval. Then introduce a private immutable ANF→MIR scan summary pairing each function value—not its potentially duplicate name—with its exact maximum across parameters, bindings, and uses, while retaining the program/main maxima needed for the dense type table and _start. Compute that summary once in both toMIR entry points, pass each recorded maximum into convertANFFunction, and remove its parameter/body rescan and unused TypeMap argument. Preserve function order, duplicate-name handling, RegGen(maximum+1), SourceTempIdMax boundaries, _start behavior, and exact MIR. Retain focused fast characterization coverage for parameter-dominant and body-dominant maxima if existing pass snapshots do not cover both. Compare exact MIR dumps, byte-identical binaries, and outputs before/after. On the same warmed, idle host and toolchain, report 10 raw before and 10 raw after fasta -vv ANF→MIR timings with medians and absolute/percentage deltas; report comparable before/after ANF→MIR and total-compilation values and deltas for all 19 routine programs; and report 10 raw before/after external wall-clock ./run-tests --ai results with medians and absolute/percentage deltas. Finally require ./run-tests --ai and ./benchmarks/run_benchmarks.sh --verify routine to pass, report every unchanged routine instruction count and the RESULTS.md performance ratio, and remove/revert the candidate if results are neutral, overlapping/noisy, or regressive.
