// Interpreter-syntax E2E tests

let inc = fun x -> Stdlib.Int64.add x 1L in inc 5L = 6L

type PairRec =
  { a: Int64
    b: Int64 }

(PairRec
  { a = 5L
    b = 7L }).b = 7L

((Ok 5L)
 |> Stdlib.Result.map (fun x -> x + 1L)) =
  Ok 6L
