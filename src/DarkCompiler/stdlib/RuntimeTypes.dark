// RuntimeTypes.dark - minimal canonical runtime hash identity used by ValueSearch.

module Darklang.LanguageTools.RuntimeTypes

type Hash = Hash of String

let hashToString(hash: Darklang.LanguageTools.RuntimeTypes.Hash) : String =
    match hash with
    | Hash(value) -> value
