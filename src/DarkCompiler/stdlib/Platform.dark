// Stdlib.Platform.dark - Stdlib.Platform definitions
//
// Split from stdlib.dark for modular stdlib loading.

module Stdlib.Platform

// Stdlib.Platform - Platform detection (constant-folded at compile time)

// Check if running on macOS
let isMacOS() : Bool = false

// Check if running on Linux
let isLinux() : Bool = false

