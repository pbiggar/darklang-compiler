# Module Function Affinity Context

## Initial Context And Prompt

The file `playbooks/modules-fns.md` describes a loop for selecting one interpreter-supported/compiler-unsupported module/function row, proving the compiler differs from interpreter behavior, adding or importing a small E2E test, and implementing the smallest compiler or stdlib change. Use that core concept to build and improve this agent's `definition.md`.
