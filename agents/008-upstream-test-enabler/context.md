# Upstream Test Enabler Context

## Initial Context And Prompt

The file `playbooks/enable-upstream-dark-tests.md` describes a workflow for enabling one skipped upstream `.dark` test case at a time, preserving upstream semantics, fixing compiler/runtime/parser behavior when needed, and proving that the test runner actually executes the enabled case. Use that core concept to build and improve this agent's `definition.md`.
