{
  "id": 8,
  "slug": "upstream-test-enabler",
  "name": "Upstream Test Enabler",
  "autonomyLevel": "initial-design",
  "enabled": true,
  "lifecyclePhase": "workflow-discovery",
  "purpose": "Enable one skipped upstream Dark test at a time while preserving upstream semantics and proving the enabled test executes.",
  "summary": "Uses the repository inventory tool to identify skipped upstream cases for focused compiler coverage.",
  "tools": [
    {
      "name": "upstream-test-enabler-inventory",
      "command": "scripts/agent-tool upstream-test-enabler inventory",
      "purpose": "Produce a concise ranked inventory for this agent; retain full matches in the reported artifact.",
      "role": "inventory",
      "maxOutputBytes": 4096
    }
  ]
}
