# Parser Pretty Roundtrip Context

## Initial Context And Prompt

The file `playbooks/parser-pretty-roundtrip.md` describes a workflow for running the parser/pretty-printer roundtrip corpus, taking the first failing case, adding a minimal regression test, and fixing parser or pretty-printer behavior without weakening the corpus. Use that core concept to build and improve this agent's `definition.md`.
