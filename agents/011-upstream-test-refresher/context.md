# Upstream Test Refresher Context

## Initial Context And Prompt

The file `playbooks/update-upstream-dark-tests.md` describes a workflow for refreshing `src/Tests/e2e/upstream/` from `darklang/dark`, replaying the repository's intentional local patch, updating runner wiring, regenerating the expected delta, and validating the result. Use that core concept to build and improve this agent's `definition.md`.
