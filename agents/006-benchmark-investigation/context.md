# Benchmark Investigation Context

## Initial Context And Prompt

The file `playbooks/benchmark-investigation.md` describes a workflow for choosing one benchmark, comparing current compiler output against existing investigation notes, and updating those notes when new optimization evidence is found. Use that core concept to build and improve this agent's `definition.md`.
