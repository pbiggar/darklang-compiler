{
  "id": 12,
  "slug": "benchmark-fixer",
  "name": "Benchmark fixer",
  "autonomyLevel": "Guided",
  "enabled": true,
  "lifecyclePhase": "workflow-discovery",
  "purpose": "Achieving benchmark parity with obvious implementations and compiler optimizations",
  "summary": "Selects one benchmark parity gap, preserves its straightforward implementation and workload, and fixes exposed compiler or runtime performance issues instead of working around them in benchmark code.",
  "tools": [
    {
      "name": "benchmark-fixer-inventory",
      "command": "scripts/agent-tool benchmark-fixer inventory",
      "purpose": "Produce a concise ranked inventory for this agent; retain full matches in the reported artifact.",
      "role": "inventory",
      "maxOutputBytes": 4096
    }
  ]
}
